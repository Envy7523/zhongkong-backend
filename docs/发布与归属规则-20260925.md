# 中控与收银报表机器人：发布和数据归属规则（2026-09-25）

## 唯一事实来源

- 中控业务数据库以云端 `/home/ubuntu/app/data/database.sqlite` 为准。不得用本地数据库覆盖云端；任何结构或配置变更先备份，再执行有明确目标和校验条件的迁移。
- 中控源代码以 Git 仓库的提交为准。本轮运行版本是开发分支 `codex/reconcile-prod-pos-daily-20260925` 的 `c19ce01745e384382f862614e8a72fdf0b0ba534`。原交接点为 `55f985e`，`37b5c74` 仅是云端源代码对齐快照，不表示已完成功能验收。
- 云端当前服务工作目录由 `/etc/systemd/system/zhongkong.service.d/codex-release.conf` 指向 `/opt/zhongkong/releases/c19ce01`。`/home/ubuntu/app` 保留为旧代码与共享数据目录，不能把它误认为当前运行版本。
- 独立收银报表机器人负责采集网页、观察下载、校验文件和调用中控导入 API；中控负责业务入库、分析、日报渲染、Webhook 目标和发送。机器人不直接写中控数据库，也不持有日报群 Webhook。

## 修改与发布

1. 开发只在 Git 分支和隔离环境进行。提交前检查差异、敏感信息、单元测试、前端构建，并用只读方式对照云端源代码，确认没有别的 AI 未归档的改动。
2. 推送开发分支。发布只使用明确提交号，建立新的云端发布目录；不要在正在运行的目录里直接编辑源代码。
3. 云端数据库、配置、上传目录与旧工作目录共享；切换前停服务并备份数据库，切换后验证进程工作目录、HTTP、定时器与核心配置。保留前一发布目录用于回退。
4. 若有人直接改动云端源码，先停止发布，读取差异并把需要保留的改动移入 Git；不得用本地版本覆盖未知变更。
5. “发布完成”不等于“业务闭环完成”。每个平台、每份报表单独验收采集、导入、对账、展示与推送。

## 收银系统日报当前闸门

- 自动同步为每日北京时间 `01:05`，日报计划时间为 `09:00`，但日报计划当前关闭。
- `pos_daily` 消息路由默认关闭、未绑定机器人；绝不隐式回退到企业设置中的 Webhook。现有企业设置机器人应进入统一可分配列表，由管理员显式选择它承担哪类消息职责。
- 报表图片只包含收银系统记录视角；外卖和团购记录不是第三方平台结算实收，不含菜品销量。
- 2026-09-25 用户确认：坂田店、上排店已停业，宜章店为放养门店。三店只从本日报范围排除，不修改门店主档；其余纳入门店如缺收银数据，自动推送必须拒绝。
- 日报样式已按用户确认的第二版：两层表头，前三名／动态中位三家／倒数三家之间隔行，单张全门店总览图。2026-09-26 用户修订机器人规则：不按“新旧”划分职责，Nameless 也可以由管理员指定承担收银日报。正式自动发送仍需绑定并核对目标群，然后分别启用消息路由与日报计划；一次性测试发送须显式确认，不改变自动计划。

## 本次回退入口

- 切换前数据库备份：`/opt/zhongkong-sync-bot/backups/zhongkong-db-before-c19ce01.sqlite`。回退代码不应自动回退数据库；先核对新表和期间业务写入，避免覆盖云端新数据。
- 前一工作目录：`/opt/zhongkong/releases/e53e7f5-archive`；更早代码仍在 `/home/ubuntu/app`。修改 systemd 工作目录后需 `daemon-reload` 与重启，并重新核对计划开关。
