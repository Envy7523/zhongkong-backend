# 中控后台 · 改动与部署记录

> 本文档记录本项目相对"基础版"的所有自定义改动，以及云服务器部署信息。
> **更新项目/重新部署前先读本文档**，避免被旧版本覆盖丢失功能。

> 📌 **接手「菜品核算」功能请看 → [`docs/poultry-accounting-handover.md`](docs/poultry-accounting-handover.md)**
> 那份是自包含的交接文档：领域知识（一只鹅怎么拆）、料篮算法、已确认口径、18 个接口、
> 页面结构、11 个踩坑记录、待办清单、验证脚本使用方法、安全须知。
> 验证脚本在 `tools/poultry-verify/`。

---

## 一、云服务器部署（上云）

### 服务器信息
| 项目 | 值 |
|------|-----|
| 公网 IP | 134.175.41.247（腾讯云轻量，实例 lhins-c2q2pyy7） |
| 访问地址 | http://134.175.41.247（nginx 80 → node 3456） |
| SSH | 端口 22，用户 ubuntu |
| 项目目录 | `/home/ubuntu/app` |
| 数据目录 | `/home/ubuntu/app/data`（database.sqlite） |
| 服务 | systemd `zhongkong.service`（崩溃自动重启） |

### 连接工具（本地）
- 目录：`server-access/`（私钥 `zhongkong_company`、`sshrun.py`、备份 `backup/`）
- 私钥等同服务器钥匙，**切勿外传**
- 快捷连接：`ssh -i server-access\zhongkong_company ubuntu@134.175.41.247`

### 部署 / 更新步骤（重要）
```bash
# 1. 上传源码（每次改完代码都做）
scp -i server-access\zhongkong_company server.js package.json package-lock.json config.json stores.json ubuntu@134.175.41.247:/home/ubuntu/app/
scp -i server-access\zhongkong_company -r lib css js tools demo ubuntu@134.175.41.247:/home/ubuntu/app/

# 2. 上传前端（先本地 npm run build）
scp -i server-access\zhongkong_company -r frontend\src frontend\dist frontend\public ubuntu@134.175.41.247:/home/ubuntu/app/frontend/
scp -i server-access\zhongkong_company frontend\package.json frontend\package-lock.json frontend\vite.config.js frontend\index.html ubuntu@134.175.41.247:/home/ubuntu/app/frontend/

# 3. 上传数据库（仅在需要同步数据时）
scp -i server-access\zhongkong_company data\database.sqlite ubuntu@134.175.41.247:/home/ubuntu/app/data/database.sqlite

# 4. 云端重启
ssh -i server-access\zhongkong_company ubuntu@134.175.41.247 "cd /home/ubuntu/app && sudo systemctl restart zhongkong"
```

### 云端注意事项
- 服务器上没有 git，直接覆盖部署
- 覆盖前建议备份云端文件：`ssh ... "cp /home/ubuntu/app/data/database.sqlite /home/ubuntu/app/data/database.sqlite.bak-$(date +%Y%m%d%H%M%S)"`
- 云端旧数据库已备份到本地 `server-access/backup/server-database.sqlite`

---

## 二、2026-08-28 · 全国地图修复（DataV 防盗链）

### 问题
云端地图完全空白，控制台报：
```
Failed to load resource: 403
Uncaught (in promise) SyntaxError: Unexpected token '<', "<!DOCTYPE "... is not valid JSON
```
本地正常，上云后失效。

### 根因
`StoreMap.vue` 画省份色块用的 GeoJSON 来自**阿里云 DataV**：
`https://geo.datav.aliyun.com/areas_v3/bound/*.json`
DataV 校验浏览器 **Referer 防盗链**：本地 localhost 放行，云域名被拒 → 403 HTML → 前端 JSON 解析崩溃 → 地图空白。
（**不是高德问题**——高德 key 白名单已配好，脚本加载正常）

### 修改文件
1. **`frontend/src/views/store/StoreMap.vue`**
   - `GEO_BASE` 从 `https://geo.datav.aliyun.com/areas_v3/bound` 改为 `/api/geo/bound`
   - 3 处 fetch 改为带参形式：
     - `` fetch(`${GEO_BASE}?path=100000_full.json`) ``
     - `` fetch(`${GEO_BASE}?path=${adcode}_full.json`) ``（2 处）
2. **`server.js`**
   - JWT 中间件放行 `/api/geo/bound`（地图边界数据无需登录）
   - 新增代理接口 `GET /api/geo/bound?path=xxx_full.json`：
     - 后端转发到 DataV（服务器请求无 Referer 限制，实测 200）
     - 路径白名单校验（只允许 `xxx_full.json`，拦截目录穿越）

### 验证
- 本地/云端 `GET /api/geo/bound?path=100000_full.json` → 200，582KB GeoJSON
- 非法路径 → 400

---

## 三、历史功能改动清单（防止更新覆盖丢失）

> 以下功能都是在本项目基础版上新增的，**更新代码时务必保留这些改动**。

### 1. 菜品分类（菜品管理 → 菜品分类）
| 文件 | 改动 |
|------|------|
| `lib/db.js` | 新增 `menu_categories` 表 + 历史分类迁移 |
| `server.js` | `/api/menu-category/*` CRUD、`/api/menu-categories` 改造 |
| `frontend/src/views/menu/MenuCategory.vue` | 新增页面 |
| `frontend/src/views/menu/MenuModuleHeader.vue` | 导航加「菜品分类」 |
| `frontend/src/App.vue` / `stores/app.js` / `router/index.js` | 菜单与路由 |

### 2. 菜品销售明细（dish_sales 表 + 数据）
| 文件 | 改动 |
|------|------|
| `lib/db.js` | 新增 `dish_sales` 表（门店/菜品编码/名称/规格/数量/金额/优惠/收入/时间/退款/订单号/结账方式/退款金额）+ 索引 |
| `server.js` | `/api/dish-sales`（分页查询）、`/api/dish-sales/stats`、`/api/dish-sales/analytics`（菜品分析聚合，支持门店筛选/分页） |
| 数据 | 已导入 190,437 条菜品销售明细（5/1–8/9），门店名称对齐后台 23 家 |
| `frontend/src/views/analysis/BusinessAnalytics.vue` | 总数据视角新增「菜品销售分析」面板 |

### 3. 堂食菜品绑定（总数据视角）
| 文件 | 改动 |
|------|------|
| `lib/db.js` | 新增 `dish_sales_mappings` 表，唯一键 `(product_code, product_name, spec)`（**规格参与唯一键，上庄/下庄不混绑**） |
| `server.js` | `/api/dish-sales/mappings` 列表（含 mapped 状态筛选）、`POST` 绑定、`POST /batch` 批量、`POST /auto-bind` 一键、`DELETE` 解绑 |
| `frontend/src/views/analysis/DishSalesBinding.vue` | 新增页面（筛选/多选批量/一键绑定/规格区分） |
| 路由/菜单 | `/analysis/total/binding`、「堂食菜品绑定」入口 |

### 4. 团购/外卖菜品绑定
| 文件 | 改动 |
|------|------|
| `lib/business-analytics.js` | 商品分析返回 `mapping_id`；新增 `deleteProductMapping` |
| `server.js` | `DELETE /api/business-analytics/mappings/:id`、`POST /batch`、`POST /auto-bind` |
| `frontend/src/views/analysis/ProductBinding.vue` | 绑定页（筛选/多选批量/一键绑定） |
| 路由/菜单 | `/analysis/group-buy/binding`、`/analysis/delivery/binding` |

### 5. 其他
- `lib/db.js` 的 `save()`：写盘失败重试 3 次（Windows 防病毒瞬时锁文件）
- `frontend/index.html`：加载高德 JS API（key `95fc48463fa6945f4c5a2380dd549d7b`，需在高德控制台配域名白名单，含云 IP）

---

## 四、更新项目时的注意事项（防覆盖）

1. **不要用旧版覆盖这些文件**（含上述所有自定义功能）：
   - `lib/db.js`、`lib/business-analytics.js`、`server.js`
   - `frontend/src/views/analysis/*`、`frontend/src/views/menu/MenuCategory.vue`
   - `frontend/src/App.vue`、`stores/app.js`、`router/index.js`、`api/index.js`
2. **前端 dist 每次更新需重新构建**：`cd frontend && npm run build`（dist 是构建产物，别用旧 dist）
3. **数据库**：`data/database.sqlite` 是核心数据（88MB，19 万条销售明细），**不要用旧库覆盖**
4. **上云后**：按「一、部署/更新步骤」重新上传修改过的文件并重启服务
5. 高德白名单：云 IP `134.175.41.247` 需在 key 的域名白名单里（已配）
6. `config.json` 含真实凭据（corpid/botSecret/AI key），部署时保留

---

*记录日期：2026-08-28*

## 2026-09-14（下午）按「以云为准」把云上整库合并到本地（含本地标定值的保全）

背景：用户在**云上**导入了 2026-09-10~09-13 的外卖账单（美团/京东/淘宝闪购）与记账本，本地落后，
要求"数据源更新，一切以云上的数据为准"。

### 关键结论：**不能盲目整库覆盖**
用户在**本地**手工做出的业务判断，云上**没有对应版本**，一覆盖就永久丢失。本次靠
「先逐表逐行比对 → 再覆盖 → 再写回本地独有值」保住两项：

- **① 鸡出成系数**：用户 2026-09-14 12:50 手工标的 `鸡/双拼量`=10、`鸡/三拼量`=14（云上仍是 12/18）。
  用户原话「我本地调整的，发现调整后更接近真实消耗量」→ 以本地为准，**不要改回 12/18**。
- **② 套餐成本构成**：`#451/#452/#455/#456` 的 8 行 `menu_cost_components` + 对应 `menu_items.cost`
  （8.8 / 14.68 / 16.56 / 3.06），云上这 4 个套餐 cost 全为 0。

### 拿到的云上数据
外卖 09-10~13 六类明细（美团订单/商品/门店、京东账单/商品/门店、淘宝闪购账单/商品/门店）、
记账 +366 条（到 09-13）、菜单 +2 菜（#461 叉烧汁、#462 酸梅酱）+1 分类「外卖特殊菜品」、
外卖对账 +201 条、`business_product_sales` +4948、`business_import_batches` 57 → **64**。

### 结果
| 项目 | 换库前 | 换库后 |
|---|---|---|
| 表数 / 总行数 | 62 / 504,215 | 62 / **511,466** |
| 导入批次 | 57 | **64** |
| 记账流水 | 7,807（到 09-10） | **8,173（到 09-13）** |
| 菜单档案 / 分类 | 160 / 11 | **162 / 12** |
| 外卖对账记录 | 3,173 | **3,374** |
| 采购记录（孤儿） | 0 | 0（换库带回 15 条已清） |

只读回归：`verify-risk-control.js` **16/16 通过**；`crosscheck-poultry.js` 总量一致（269,135.14 份）；
三类孤儿行均为 0；`integrity_check=ok`。核算全区间 05-01~09-10 = **10,939.02 只**
（鹅 6832 / 鸭 2545.99 / 鸡 1561.03），比云上的 10,820.5 多 118.52 只 —— 差值正好等于用户标定系数的影响。

### 新增工具（都在 `_tmp/`，已 gitignore）
`pull-cloud-db.py`（等 SSH 解封→SFTP 拉库→sha256 校验）、`inspect-db.js`（换库前体检）、
`diff-db.js`（两库逐表逐行比对）、`local-only-summary.js`（补「行数不同」表的行级比对，**必需**）、
`local-only-rows.js`、`gap-report.js`、`swap-to-cloud-db.js`（备份→清孤儿→写回本地独有值→落盘→复核）、
`local-overrides.json` + `apply-local-overrides.js`、`trace-coef.js` / `when-changed.js`（追查系数改动时刻）。
完整流程与对照基线写进了 `docs/poultry-accounting-handover.md` §10.1。

### 顺带修掉的
云上那份库仍带着 15 条孤儿采购（12000 只幽灵只数）；本地已清，**以后在云上跑验证脚本要注意**。

## 2026-09-14 修掉两个验证脚本缺陷（金丝雀假绿灯 / 孤儿采购）+ 清理 15 条幽灵采购

背景：接手 AI 在交接文档 §9.8 / §9.9 里记录了「未改」的两个隐患 ——
金丝雀脚本会在**非空库**上改坏真实菜品配置却报告全 PASS，验证脚本每次运行残留 1 条孤儿采购。
本日两个都修掉，并做了只读回归验证。**代码已改，尚未上云**（按约定上云需用户单独批准）。

### ① 金丝雀脚本（`tools/poultry-verify/canary-safe-teardown.ps1`）全量重写
- **不再碰任何真实菜品**：原来把测试数据埋死在真实菜品 206「招牌烧鹅饭」上，且收尾是「清空」
  而非「还原」，跑一次就抹掉它的配置（欧景店 76.5 → 62.59 只）。现在只用自己的
  `金丝雀验证鹅-临时` + `金丝雀验证菜-临时`，收尾只删自建行。
- **断言换成真正该测的东西**：运行前给**全部已配置菜品**（38 个）拍逐菜品快照
  （菜品 id → 排序后的 `yield_id:usage_qty`），运行后**逐项比对**，任何一处变动即 FAIL。
  旧版只检查「自己埋的数据还在不在」，对真实配置被改坏完全瞎 → 假绿灯。
- 断言不再假设空库，改为与运行前基线比较；`try/catch` 包住被调脚本，即使它中途抛异常也会走完比对与收尾。
- 末尾打印「全部通过，真实数据未被改动」或「有 N 项 FAIL，真实数据可能已被破坏」，退出码随 FAIL 数。
- **实测**：跑完 `verify-poultry.ps1` + `verify-poultry-batch.ps1` →「已比对 38 个既有菜品，全部与运行前一致」，
  206 配置完好（`鹅肉 ×1`，891.45 只没丢），退出码 0。

### ② 孤儿采购（15 条 = 12,000 只幽灵采购）
- **接口层**：`lib/poultry-accounting.js` 的 `listPurchases` 由 `JOIN poultry_birds` 改 `LEFT JOIN`，
  并返回 `is_orphan` —— 孤儿行再也藏不住（这正是它们此前既回收不掉、UI 也看不见的原因）。
- **行为层**：`deleteBird()` 现在连带回收该品种名下采购行，返回值新增 `purged_purchases`；
  新增 `purgeOrphanPurchases()`。**即使调用方顺序写错也不会再产生孤儿**。
- **脚本层**：`verify-poultry.ps1` / `verify-poultry-batch.ps1` 的第 0 步与收尾都改为
  「先回收采购、再删品种」，并按 `remark` **或** `is_orphan=1` 回收；各加一条「自己没有留下孤儿采购」断言。
- **存量清理**：15 条历史孤儿（博罗店 / 2026-08 / 每条 800 只 / remark=`自动化验证临时数据`）已删。
  删前核对备份库：`poultry_purchases` 共 15 行**全部**是孤儿，真实录入 0 条 → 清理没丢任何用户数据。
- **实测**：`_tmp/verify-fixes.js` 7/7（建临时品种 → 录采购 → 删品种 → `purged_purchases=1`、全库孤儿 0）。

### 回归验证（只读）
- `verify-risk-control.js` 14/14 通过；`crosscheck-poultry.js` 总量不变（269,135.14 份 / 覆盖 62,839）。
- 全库孤儿巡检：采购 / 耗用 / 出成 三类均为 0；无「临时」残留数据。
- 核算抽查 2026-09-01~09-10：总 2,220.79 只（鹅 1,204.5 瓶颈腿 / 鸭 1,016.29 瓶颈身体），禽类覆盖率 80.12%。

### 文档
- `docs/poultry-accounting-handover.md`：§9.8 / §9.9 标记已修并补完整修法；§9.7 补孤儿采购影响；
  验证脚本表、接口清单、踩坑表（新增第 12、13 条）；§11 不变量第 3 条按**资源约束模型**改正
  （原文仍写着旧的料篮口径，与代码不符）。

## 2026-09-02 淘宝闪购“账单汇总”真实到账导入（真实实收口径修正）

背景：此前导入的“营业额收入单量”实收未扣第三方费用（8月=182,995.64），平台真实到账需扣除推广/保障/余额变动等费用。
- 新增表 taobao_flash_bills（账单汇总原始行：结算入账ID/门店ID/门店名称/账单日期/结算金额/结算日期/账单类型 + 整行 raw_json，未来新增列自动收录；UNIQUE(门店ID,账单日,账单类型)，可重复导入覆盖）
- lib/business-analytics.js：importTaobaoFlashWorkbook 自动识别“账单汇总”子表（仅该子表入库）→ importTaobaoFlashBillWorkbook：每店每日净额=当日全部账单类型金额之和，覆盖写入 business_revenue_records（channel=taobao_flash, actual=真实实收, gross=外卖结算金额, 有效订单数延续旧记录）；对“整月商品销量汇总”文件给出明确报错不入库；日期范围已有账单汇总时拒绝旧“营业额收入单量”误覆盖
- 前端 BusinessDataImport.vue：淘宝闪购入口新增“真实到账·账单汇总（推荐）”卡片与字段说明，导入结果显示 净额合计/原始明细
- 2026-08 导入结果：账单汇总 1449 行、18 门店全匹配、479 条每店每日净额、净额合计 ¥169,393.83（旧口径 ¥182,995.64 − 第三方费用 ¥13,601.81）

## 2026-09-02 记账本覆盖更新（删除 ≥8/10 旧记账 → 导入 Sheet1 权威数据）
- 新增 lib/bookkeeping-import.js + POST /api/bookkeeping/import-replace：解析含 日期/门店名称/大类/小类/金额 的记账流水子表（按有效数据行数自动选主表，仅 Sheet1 这类流水表；门店按名称匹配本地 stores；大类/小类按名复用或自动创建；早于 replace_from 的行拒绝）
- 处理：先 DELETE bookkeeping_entries date>=2026-08-10（删除 733 条旧数据），再导入 记账本更新92.xlsx Sheet1 全部 2396 条（8/10~8/31，21 家门店全匹配，0 跳过）
- 核验：本地记账总量 6852（<=8/9 保留 4456 + >=8/10 新 2396）；8/10-8/31 各大类合计与文件逐项一致（销售收入 +1,451,877.22 / 原材料 -420,650.02 / …），合计 609,090.25

## 2026-09-02 美团外卖“真实到账”结算账单导入（与淘宝闪购同法）
- 新表 meituan_delivery_bills：按(美团门店ID, 账单日期, D交易类型)汇总 R商家应收款（含交易笔数/描述快照，未来新列自动收录）；来源“订单明细”导出（表头不固定在第1行亦可识别）
- lib/business-analytics.js：importMeituanDeliveryWorkbook 自动识别“订单明细”结算表 → importMeituanDeliveryBillWorkbook；rebuildMeituanDeliveryRevenue：营业额/优惠后收入/单量来自美团营业额日报，真实到账=当日全部交易类型商家应收款之和，费用按类型归入 推广/保险/退款/服务；只重建 source_type='platform'，保留收银机(pos)记录
- 补充绑定：美团外卖 platform_id 保利上城(26)=34251847、未来花园(27)=34251852（档案原为空）
- 2026-08 导入结果：1918 条(门店,日,类型)账单、18 店全匹配、521 条每店每日净额；真实到账合计 ¥218,536.74（与官方“账单明细”按店日汇总 521/521 完全一致），费用合计 ¥17,832.19
- 前端：美团外卖入口新增“真实到账 · 结算账单”卡片与字段说明，导入结果显示 优惠后收入/平台费用/真实实收

## 2026-09-10 总数据视角「菜品销量」替换为「菜品销售分析」（去重复）
背景：总数据视角（品牌/门店）里，页签「总数据菜品销量」与「经营总览」底部的「菜品销售分析」面板功能重合。按用户决定，保留字段更完善的菜品销售分析，删掉总数据菜品销量。
- 前端 frontend/src/views/analysis/BusinessAnalytics.vue：
  - 页签「菜品销量」在总数据视角改名为「菜品销售分析」（外卖/团购平台看板仍叫「菜品销量」，未受影响）
  - 该页签内容换成菜品销售分析口径：菜品数/总销量/销售额/菜品收入/优惠金额/退款金额/预估成本(已绑定 x/y)/净收入 + 排序(按收入/销量/金额) + 服务端分页
  - 并入原总数据菜品销量的两项独有能力：来源订单明细展开（按 菜品编码+名称 从收银机品项明细取数）、行内绑定本地菜品 SKU（写 dish_sales_mappings，与「堂食菜品绑定」同表同唯一键）
  - 删除「经营总览」内重复的菜品销售分析面板（时段订单走势图保留）
  - 保留赠品维度：新增「赠品数量 / 赠品金额」两列与两张指标卡。dish_sales 表没有赠品列，故按 (菜品编码,菜品名称) 从同一批收银机品项明细（来源订单明细）汇总到菜品行上；口径与总数据菜品销量一致——赠品已含在销量/销售额内，只标注不重复相加
- server.js：GET /api/dish-sales/analytics 的列表行补充 mapping_id/menu_item_id/menu_sku_label/unit_cost 等已绑定 SKU 字段（原字段与 estimated_cost、net_income 口径不变），供行内展示与改绑使用
- 核验：_tmp/verify-dish-analytics-fields.js 直连库跑新 SQL（5/1-8/9 区间 mapped 行返回 sku="招牌烧鹅饭 · 常规"、unit_cost=13.5）；_tmp/verify-gift-join.js 核对赠品行级取数（9/10 超爽柠檬茶 11 份 / ¥88.00，与汇总一致）；_tmp/probe-total-dish.cjs 经 CDP 渲染总数据品牌/门店视角，页签=菜品销售分析、16 列表头（含赠品数量/赠品金额/净收入/退款金额）、展开列与绑定选择器各 20 个、来源明细可展开、无 console 错误；外卖平台视角仍为「菜品销量」

### 追加修正：本地菜品自动识别（不再要求「本地菜品绑本地菜品」）
问题：上线后总数据视角 361 行全部显示「选择本地菜品 SKU」——没有手工记录时只认 dish_sales_mappings，把本来就取自本地菜品库的品项当成未关联。
- server.js `/api/dish-sales/analytics` 改为两段式：SQL 只做分组汇总（不设 LIMIT，分组数在千级），本地 SKU 识别 / 排序 / 翻页在 JS 完成
- 识别优先级（与旧「总数据菜品销量」同一套规则）：手工绑定 → 菜品编码=menu_items.skuid（名称互校，防错归）→ 名称+规格唯一 → 名称唯一；手工绑定可指向已下架菜品，另按 id 补查
- 预估成本 / 净收入 /「已关联 x/y 款」随识别结果计算（口径不变：数量 × 单位成本；订单量仍走 SQL 全区间去重，不由分组相加）
- 排序：已关联在前，未关联自动沉到列表末尾（与外卖/团购看板对齐），再按收入/销量/金额倒序
- 前端：绑定列显示识别出的 SKU 名称，自动识别的按钮提示「已按菜品编码/名称自动识别，点击可改绑」；指标卡文案改为「已关联 x/y 款 · 含自动识别」
- 9/10 实测：361 分组自动识别 126 个（35%），预估成本 ¥23,242.20、净收入 ¥40,392.81（改前均为 0）；其余 208 个是外卖套餐/别名（如「限量!招牌烧鹅腿饭 [一人份]」「【太公烧鹅饭】含时蔬+海咸鸭蛋 [1人份]」），本地菜品库无此名称，**故意不做模糊自动绑**（会把套餐成本算错），仍走智能推荐人工确认
- 附带发现：部分已识别菜品在菜品管理里成本为 0（叉鸭双拼饭 / 太公烧鹅·半只 / 招牌烧鹅（约100克）），这几行净收入=收入，需补维护成本
- 核验：_tmp/verify-dish-automatch.js 直连库统计识别覆盖率；CDP 渲染三条路由各自独立会话：总数据品牌/门店页签=菜品销售分析且顶部行显示「招牌烧鹅饭 · 常规」「太公烧鹅 · 下庄 · 份」，外卖平台仍为「菜品销量」，均无 console 错误

## 2026-09-11 新增「菜品管理 → 菜品核算」（选门店+时间段反推禽类消耗只数）
背景：需要知道某门店某时间段大概消耗了多少只鹅/鸭/鸡。系统里只有菜品销量，没有任何「一份菜用掉多少只禽」的数据，所以先补三层基础档案再算。
> 位置说明：按需求暂挂「菜品管理」下，功能打通后整体迁往「成本核算」。

### 数据模型（lib/db.js 新增 4 张表）
三层模型，全部以「只」为最终锚点：
- `poultry_birds` 禽类档案：一只禽的品种/单只净重(kg)/单只成本/状态
- `poultry_yields` 整只出成拆解：一只禽出哪些部位、每个部位出几个（`parts_per_bird`）、单份克重
- `poultry_dish_usage` 菜品耗用：每份该菜品吃掉几个该部位（`usage_qty`），UNIQUE(menu_item_id,yield_id)，**一菜可挂多行 → 双拼/套餐/鸡鸭双拼天然支持**
- `poultry_purchases` 实际采购只数：手动录入，UNIQUE(store_id,month,bird_id)，用于理论 vs 实际对比

公式：`消耗只数 = Σ(菜品销量 × 每份耗用部位数 ÷ 一只出该部位数)`；折重 = 只数 × 单只净重；理论成本 = 只数 × 单只成本。

### 口径（已与业务确认）
- 退款行整体排除：`refunded IN ('部分退','是','1')` 不计入（报表只有退款金额、无退菜数量，无法精确扣减）
- 渠道由 `dish_sales.payment_detail` 关键字判定，优先级 **团购 > 外卖 > 堂食 > 其他**（「店内销售|收银POS|POS|抖音团购」属团购核销，不算纯堂食）
- ⚠️ 实测约 65% 销量（17.5 万份）的 payment_detail 形如「扫码支付-微信 25.00」，**不含渠道信息**，归入「其他/未分类」并在页面单独标注，不参与渠道筛选
- 只数保留 2 位小数；只数先定稿再由它推折重/成本，保证页面上「只数 × 净重/单价」能对上（行级舍入 vs 合计舍入会差出 0.6 元那类错位）

### 菜品匹配三级兜底（提升覆盖率）
① `dish_sales_mappings` 绑定 → ② 名称 + 归一化规格直配 → ③ 名称唯一命中 → ④ 计入「未绑定」清单。
两个易踩的坑已处理：
- **空菜名不得参与匹配**（报表/档案里的空名行会互相误挂）
- **孤儿绑定视为未绑定**：库里有 10 条 `dish_sales_mappings` 指向已被删除的菜品（旧库遗留，FK 未生效），否则缺口清单会出现没有菜品名、也无法去配置的空行；现已退回名称兜底，兜不住才按未绑定报出

### 后端
- 新模块 `lib/poultry-accounting.js`（三层档案 CRUD + 核算 + 采购对比 + Excel 模板/导入）
- `server.js` 新增 17 个 `/api/poultry/*` 接口：species CRUD、yields 整表保存、dish-usage（单菜品配置 + 全菜品总览 + 已配置列表）、accounting 主核算、accounting/purchase-comparison、purchases CRUD、template、import
- Excel 模板**预填本地全部菜品行**（只需补品种/部位/每份耗用），导入逐行容错：非法禽类值报错跳过、不存在品种/菜品报错回报、预填空行静默跳过

### 前端
- 新增页面 `frontend/src/views/menu/MenuAccounting.vue`：筛选条（门店/日期默认本月/渠道/禽类）→ 只数汇总卡（鹅鸭鸡各多少只+折重+成本）→ 覆盖率提示条（覆盖销量占比、未绑定/未配出成缺口下钻、>1只/份 复核提醒、渠道构成）→ 按菜品下钻表（每份折合只数、消耗只数、占比）→ 理论 vs 实际采购对比表 + 采购录入 → 基础档案抽屉（禽类档案/整只出成/菜品耗用三页签 + 模板下载 + 导入）
- 5 处注册：`router/index.js`（`/menu-management/accounting`）、`App.vue`（COMPONENT_MAP + 弹窗菜单「菜品数据」组 + **WORKSPACE_TAB_ROUTES** + **workspaceRouteTabId**）、`stores/app.js`（SUB_LABELS）、`MenuModuleHeader.vue`（navItems）
- 新增页面漏注册会「地址栏正确但内容区空白/落到数据概括」，详见下方修复第 1 条
- **迁移说明**：只需改 tab id 前缀 `menu-management-accounting` → `cost-accounting-accounting`、路由 path、文件目录，业务代码与接口无需改动（文件头已写迁移步骤）

### 同期修复的两个既有缺陷
1. **`App.vue` `workspaceRouteTabId` 漏登记独立静态路由**（本次由 UI 探针发现）：`/menu-management/accounting`（以及**既有的 `/menu-management/category`**）走的是静态路由而非 `:section` 通配，反查不到 tab id → 地址栏正确但**刷新/直接打开/分享链接时内容区落到数据概括**。已在 `workspaceRouteTabId` 的简单页面映射里补上这两条，并加注释提醒后续新增同类页面要同步登记。
2. 核算模块自身修掉的缺陷：采购对比按月录入却按 `YYYY-MM-01` 查询（永远查不到，对比恒为 0）；页面在「档案已加载但核算未返回」时会读空 `summary` 报错（已加结果守卫）；导入把非法禽类值（如「猪」）静默改写成「鹅」。

### 验证（`_tmp/verify-poultry.ps1`，43 项全部 PASS，跑完自动清理）
- 公式对照：招牌烧鹅饭 9806 份 ÷ 出成 16 = **612.88 只**；折重 2757.96kg = 612.88×4.5；成本 ¥73,545.60 = 612.88×120
- **独立交叉核对**（`_tmp/crosscheck-poultry.js` 直连库用 sql.js 复算）：博罗店 201 份 → 12.56 只、全区间总销量 269,135.14、四个渠道 174801.1/62711.48/24635.57/6987 与接口逐项完全一致
- 门店/渠道筛选、采购同键覆盖与整月判定、Excel 三页签真实导入（含错误行回报）、一菜挂鹅+鸭两行（鸭 9806÷2 = 4903 只）
- 数据库已验证回到空态（species/yields/configured/purchases 全 0），未残留测试数据
- ⚠️ **未经真实浏览器渲染验证**：本机沙箱阻止 Chrome（CDP 端口起不来、headless 无法写文件），页面渲染只在构建层面确认过（vite 编译通过、产物含全部页面与接口路径、路由返回 200、无 console 异常无从取得）。UI 探针脚本 `_tmp/probe-accounting-ui.cjs` 已写好，在非沙箱环境可直接跑（先以 `--remote-debugging-port=9335` 启动 Chrome）

### 追加优化：菜品耗用改为「分组选菜 + 批量关联」（2026-09-11 同日）
问题：160 个菜品平铺在一个列表里逐个配耗用，实际同一分组的菜（如「烧鹅·濑粉」30 个）通常用同一套部位耗用，逐个点太慢。
- 后端 `listUsageOverview` 增加**分组（菜品分类）筛选**并返回每个分组的「已配置/总数」进度 + `all_total`；分组进度基于全部菜品统计，不受关键词/分组筛选影响，左侧分组条进度始终稳定
- 新增 `POST /api/poultry/dish-usage/batch`（`lib/poultry-accounting.js` 的 `batchSaveUsage`）：把同一套「部位 × 每份耗用」套用到多个菜品
  - `mode=replace` 覆盖：先清空这些菜品的原有配置（适合整组用同一套部位），并**回报被清掉的条数 `removed` / 受影响菜品数 `replaced_dishes`**，避免静默删掉手工配置
  - `mode=append` 追加：upsert 保留原配置（适合给已有配置的菜再加一条双拼部位）
  - 校验：空菜品列表 / 空耗用行 / 无效部位 / 耗用量≤0 / 同一部位重复 / 菜品不存在 均按 400 拒绝；单次上限 300 菜品、30 条关系
- 前端 `MenuAccounting.vue`「菜品耗用」页签：**分组选择条**（每个分组显示 `已配置/总数`，配满打 ✓）→ 关键词 + 状态筛选 → 表格加 **多选列** → 勾选后出现**批量操作条**（已选 N 个 / 选中本组全部 / 清空选择 / 批量关联禽类耗用）→ 批量弹窗（**应用方式：覆盖 / 追加** 用 el-segmented，逐行配部位×耗用，实时折算「每份 X 只」，底部预览将套用到哪些菜品）。单个菜品仍可「编辑」精调
- 细节：批量弹窗会**取选中项里第一个已配置的菜品作为初值**，减少重复录入；切换分组/筛选会**自动清空勾选**，避免把上一次的选择连同新条件一起提交
- 验证：`_tmp/verify-poultry-batch.ps1`，**31 项全部 PASS**（自清理），覆盖分组统计=160、分组筛选无越界、覆盖/追加两种模式、覆盖时 `removed`/`replaced_dishes` 回报、6 项参数校验、以及批量结果参与核算与手算对照（1273.15 份 ÷ 出成 4 = 318.29 只）；原 `_tmp/verify-poultry.ps1` 43 项回归仍全部 PASS

### ⚠️ 事故与修复：验证脚本曾整表清空用户数据（2026-09-11）
现象：验证脚本为了「可重复执行」，收尾时执行了**整表删除**（所有禽类档案 / 所有已配置的菜品耗用 / 所有采购记录），把用户在此期间手工录入的数据一并删掉了。
- 已定位责任范围：**只影响 `poultry_birds` / `poultry_yields` / `poultry_dish_usage` / `poultry_purchases` 四张新表**。`menu_categories`（菜品分类）与 `menu_items` 全程未被本功能的任何代码写入或删除 —— 与 12:30 备份逐行比对，160 个菜品及其分组完全一致，分类仅少一个 `#24 外卖菜品`（非本功能所致，本功能没有任何删除分类的调用路径；排序接口还带「必须提交全部分类完整排序」的校验且只 UPDATE 不 DELETE）。
- 已修：两个验证脚本改为**最小回收** —— 只删按名称前缀（`验证用*` / `批量验证用*` / 金丝雀）自己建的行，采购按 remark 标识回收；对真实菜品的耗用配置改为**先快照、收尾原样还原**；断言由「整表为空」改成「与基线一致」。
- 已证明：`_tmp/canary-safe-teardown.ps1` 先埋一份「用户数据」（禽类+出成+挂在 206 上的耗用+采购），再跑两个验证脚本，结果金丝雀**全部存活且数值未变**（禽类/出成/耗用 usage_qty=2/采购 555 均保留），脚本自身数据全部回收。
- 教训：**验证脚本绝不允许整表 DELETE**；沙箱里脚本可能被中断在收尾之前，所以「收尾」不能是唯一的清理屏障，删除范围必须一开始就限定在自己创建的行上。

### 部署记录：菜品核算上线云端（2026-09-11 17:05）
- 脚本 `_deploy-poultry.ps1`（logs: `_deploy_poultry_log.txt`）。上传：`server.js`、`lib/db.js`、`lib/poultry-accounting.js`（新增）、`lib/business-analytics.js`、`PROJECT_CHANGES.md`、`frontend/dist`（先清旧目录）、`frontend/src`。
- **刻意未上传**：`config.json`（云端凭据）、`data/database.sqlite`（云端业务数据）。云端新表由服务启动时的 `createTables()` 自动创建。
- 云端备份：`server.js.bak-20260911170553`、`lib/db.js.bak-…`、`lib/business-analytics.js.bak-…`、`PROJECT_CHANGES.md.bak-…`、`data/database.sqlite.bak-cloud-20260911170553`。
- 上线前差异核对：先用 md5 + 行级 diff 确认云端没有本地缺失的实现（云端多出的 11 行 server.js / 6 行 business-analytics.js 经核对都是**已被本地重写掉的旧版本片段**）；`db.js`、`PROJECT_CHANGES.md`、`frontend/src` 云端独有内容为 0。**结论：本地为严格更新的版本，覆盖安全。**
- 上线后验证：服务 active；4 张表已建且唯一键 `UNIQUE(menu_item_id, yield_id)` 正确；公网 `GET /api/poultry/{species,dish-usage,accounting,template}` 全部 200 且返回正常（分组 11 个 / 菜品 160 个 / 模板 77,668 base64）；页面 `/menu-management/accounting` 200 且引用新分包 `MenuAccounting-BmQr6aPu.js`；**15 个既有接口（门店/菜品/销售分析/绑定/成本构成/记账/协同/人事/用户/看板/业务分析/推送/机器人）全部正常**，未影响线上。
- ⚠️ **云端数据缺口（需业务决定）**：云端 `dish_sales` 仅覆盖 **2026-05-01 ~ 2026-08-09**（190,437 行），而本地已到 **2026-09-10**（229,613 行）。因此菜品核算在云端目前只能算到 8/9，算 8 月整月或 9 月会缺数据。**不要直接上传本地 database.sqlite 覆盖云端库** —— 云端的用户、协同事项、记账等是线上持续写入的，覆盖会丢（本项目历史上就出现过上云后要单独恢复云端用户的情况）。建议只补 8/10–9/10 的销量数据（走收银 POS 导入，或做一次只针对 `dish_sales` 的定向同步）。

### 修复：追加出成部位后，「菜品耗用」选不到新部位（2026-09-11）
现象（用户报告）：把一只鹅拆成 4 个规格并绑好菜品后，返回「整只出成拆解」追加 2 个规格，再回到「菜品耗用」，部位下拉里**看不到新增的 2 个规格**。
- 根因（纯前端缓存）：`MenuAccounting.vue` 里部位下拉的数据源是 `yieldOptions`，它只在页面挂载时加载一次；`saveYields()`（保存出成）后只刷新了出成表格与禽类档案，**没有刷新 `yieldOptions`**；而 `openUsageDialog` / `openBatchUsageDialog` 又写成「仅当列表为空时才去拉取」，导致一旦加载过就永远是旧快照。
- 修复（4 处，让数据源始终同步）：
  1. `saveYields()` 保存后追加 `await loadYieldOptions()`
  2. `openUsageDialog` / `openBatchUsageDialog` 改为**每次打开都重新拉取**（不再依赖空判断）
  3. `openArchive()` 打开抽屉时刷新部位下拉
  4. 新增 `onArchiveTabChange`：抽屉内切到「整只出成」重读出成表、切到「菜品耗用」重读部位下拉 + 耗用总览 —— 覆盖「在抽屉内来回切页签」这条路径
  5. 附带：`removeBird` 删品种后会级联删出成，一并刷新部位下拉，避免留下指向已删部位的失效选项
- 顺带优化：部位下拉改为按「禽类 · 品种」分组的 `el-option-group`（一个品种挂十来个部位时平铺很难找），并同步用于单个配置与批量关联两个弹窗
- 验证：`_tmp/verify-yield-append.ps1` 复现用户场景（自建临时禽类+临时菜品，收尾全回收），**11 项全部 PASS**：4 规格保存 → 绑定菜品 → 追加到 6 规格 → 接口回读 6 个部位 → **下拉数据源含全部 6 个且新规格 id/出成有效** → 原有绑定未丢失 → 新规格可直接绑定（双拼式两行）。结论：后端数据完全正确，问题只在前端缓存。
- 已部署：云端只更新 `frontend/dist` 与 `frontend/src`（后端无需改动），新分包 `MenuAccounting-J3jY6qQr.js`，公网校验通过。

### 口径重构：料篮模型（2026-09-11，用户反馈「鹅头带颈 + 战斧饭 = 3 只鹅」不合理）
问题（用户实测）：一只鹅身上同时产出 2 份上庄 / 2 份下庄 / 2 条腿 / 2 只翅 / 1 个头颈 / 11 份肉，但旧公式是 `Σ(各菜品销量 × 每份耗用 ÷ 一只出成)`，等于**每个菜品各自买一只鸟**，把同一只鸟的多个部位重复计了鸟。实测 11,000.26 只，按料篮算只有 6,586.74 只（多算 1.67 倍）。

- **正确模型**：
  - **身体部位**（上庄/下庄/腿/腩/切片肉/整只/半只）＝ 同一块身体的不同卖法，互相抢鸟 → 需求**相加**
  - **副产品**（翅/头颈/掌/杂）＝ 随鸟附带产出，不额外占鸟 → 需求**取最大**；只有超过身体需求时才成为瓶颈
  - `只数 = max(身体各部位折鸟数之和, 各副产品折鸟数)`
  - 边界：全部取最大也会错（把互斥的整只/半只当成同一批鸟，7,497 只严重偏低）
- `lib/db.js`：`poultry_yields` 新增 `part_kind`（身体/副产品），并按部位名自动预判做迁移（翅/战斧/头/颈/掌/爪/杂/肝/肠/汁 → 副产品），用户无需重录
- `lib/poultry-accounting.js`：核算改为按「禽 + 部位」汇总需求再套料篮模型；返回新增
  - `summary.birds[].body_birds / byproduct_birds / governing / governing_part`（说清瓶颈在身体还是副产品）
  - `summary.birds[].birds_count_linear`、`summary.total_birds_linear`（旧口径参考值）
  - `parts[]` 部位汇总（部位/类别/一只出成/总需求/折鸟数/是否瓶颈）
  - 明细行带 `part_kind` 与 `bird_id`
- 页面：筛选条公式说明改为料篮口径；新增「按部位汇总 · 瓶颈在哪」面板（含**口径对照条**：新口径主数 + 旧口径参考值及多算倍数）；「整只出成」表格新增**部位类别**下拉 + 类别含义图例
- **验证**：`_tmp/verify-poultry.ps1` 重写为**非破坏性**（只用自建临时禽类 + 临时菜品，核算公式改为只读校验内部不变量），**49 项全部 PASS**：每行只数 = 销量×每份耗用÷一只出成（59 行一致）、部位折鸟 = 总需求÷出成（12 个一致）、`只数 = max(身体相加, 副产品最大)`（逐禽一致）、合计 = 逐禽之和、每种禽唯一瓶颈标注、旧口径 ≥ 新口径、占比合计 100%、临时数据不污染结果；`_tmp/verify-poultry-batch.ps1` **29 项全 PASS** 回归

### 顺带修掉项目级隐患：ON DELETE CASCADE 实际未生效
排查删品种被拦时发现：`lib/db.js` 里的 `PRAGMA foreign_keys=ON` **没有真正生效**（实测 `PRAGMA foreign_keys` 返回 0），因此全库所有 `ON DELETE CASCADE` 都不会执行。这是**既有问题**，也解释了之前 `dish_sales_mappings` 里那 10 条指向已删菜品的孤儿绑定。
- 影响本功能：删除菜品后 `poultry_dish_usage` 会残留孤儿行，导致 ① 品种的「挂菜数」虚增 ② 删品种时报「已被 N 个菜品引用」而那个菜品其实已不存在
- 处理（不改动全局，避免波及其它模块）：新增 `purgeOrphanUsage()`，在 `deleteBird` / `deleteYield` 前显式清理孤儿行；`listBirds` 的挂菜数改为 JOIN `menu_items` 只算还存在的菜品；`configured_dish_count` 同样过滤
- **建议后续单独处理**：全库启用外键（或给 `menu_items` 的删除接口补显式清理），并清理历史遗留的孤儿 `dish_sales_mappings`

### 部署记录（2026-09-11 17:4x，料篮模型 + 部位类别）
- 上传：`lib/db.js`、`lib/poultry-accounting.js`、`PROJECT_CHANGES.md`、`frontend/dist`（先清旧目录）、`frontend/src`；后端无 `server.js` 改动
- 云端新表由启动时 `createTables()` 自动补 `part_kind` 列并执行迁移
- 上线前同样先做差异核对，`_deploy-poultry.ps1` 已保留全部备份路径可回滚

---

## 2026-09-12 覆盖率口径拆分：新增「禽类菜品覆盖率」+ 判定规则可维护（交接文档 §9.1 已修）

### 问题（用户提出的口径缺陷）
覆盖率原本 = 已配系数销量 ÷ **全部**销量，分母含柠檬茶、白米饭、打包盒等**永远不需要配禽类系数**的品类，
所以永远到不了 90%，拿它当风控可信度门槛没有意义。

实测欧景花园店 2026-09-01~09-10：
| 口径 | 数值 |
|---|---|
| 全量分母 | 2,079.15 份（含超爽柠檬茶 328、太公白米饭 80 …） |
| 禽类菜分母（含鹅/鸭/鸡） | 1,173 份 |
| 已配系数销量（分子） | 675 份 |
| 全量覆盖率 | 32.47% |
| **禽类菜品覆盖率** | **57.54%** |
| 只数 | 76.5 只（门店记录 80，口径未变） |

### 做法：拆成两个数，判定依据做成可维护配置
- `summary.poultry_covered_rate` **禽类菜品覆盖率** = 已配销量 ÷ **禽类菜销量** → 风控可信度看这个
- `summary.covered_rate` **全量覆盖率** 保留作参考；页面并列展示并注明分母差异
- 页面所有「覆盖率 < 90% 告警」与指标卡 tone 改用**禽类口径**（否则告警永远亮着，等于没有告警）

### 为什么不把「哪些菜算禽类菜」写死在代码里
名称判定会同时**误收**与**漏收**：
- 误收：`咸鸭蛋`（含「鸭」但是蛋制品）、`牛肉滑蛋饭`（含「蛋」）
- 漏收：`太公三宝饭` 这类名称不含禽类字、但实际含烧鹅的菜

所以判定依据做成用户可维护的配置，遇到边界不用改代码：
- `poultry_scope_keywords` 关键词表：include（算）/ exclude（不算，**优先级更高**）/ 可停用 / 带备注
- `poultry_scope_dishes` 显式名单：逐个菜品指定，支持「强制算 / 强制不算 / 清除标记」

**判定优先级（后端固定顺序：配置可改，顺序不可改）**
1. 显式名单 `in_scope=1` → 算
2. 显式名单 `in_scope=0` → 不算（用户明确意志，压过关键词与已配状态）
3. **已配禽类耗用 → 算**（它确实在消耗禽类；否则覆盖率分子会大于分母）
4. 名称命中 include 且不含 exclude → 算（档案名与报表名**任一**命中即可；任一名命中排除词则否掉）
5. 其余 → 不算

### 口径说明（重要，避免误读）
- 禽类菜口径**不受 `animal` 筛选影响**：与 `covered_quantity` 保持同一口径（都是「已配 / 禽类菜全量」），
  否则分子分母会不一致。按禽种细分覆盖率留作后续（需按菜品所挂禽类分别统计）。
- 默认关键词 `鹅/鸭/鸡` + 排除词 `蛋`，**首次启动才写入**（表非空时不覆盖，用户改过的配置优先）。
  默认口径下欧景店分母是 1,173 份；若只想看鹅，把「鸭」「鸡」停用即可（分母变 791 → 85%）。
- 关键词表被清空或全部停用时，后端回退内置默认，页面显示提示。

### 接口（+4，共 22 个）
| 接口 | 说明 |
|---|---|
| `GET /api/poultry/scope` | 关键词表 + 显式名单 + 生效规则 |
| `PUT /api/poultry/scope/keywords` | 关键词整表保存（至少留一条启用中的 include，否则 400） |
| `POST /api/poultry/scope/dishes` | 批量标记（`in_scope`: true / false / null=清除标记） |
| `GET /api/poultry/scope/preview` | 规则预览（全部菜品 × 全区间销量，带判定依据，用于核对规则） |

### 核算返回新增
- `summary.poultry_covered_rate / poultry_covered_quantity / poultry_scope_quantity / poultry_uncovered_quantity / poultry_scope_dish_count / poultry_scope_rules`
- `coverage.poultry_scope.{ include_keywords, exclude_keywords, source_summary, dishes[], unbound[], no_usage[] }`
  —— 其中 `unbound` / `no_usage` 是**禽类菜范围内**的缺口，比原来混着饮料/包材的全量缺口清单更可直接拿去补配
  （欧景店实测：金牌烧鸭饭 170、蜜汁鸡翅 43、限量!招牌烧鹅腿饭 9 …）

### 前端
- 覆盖率面板：主徽标 →「禽类菜品覆盖 X%」，并列「全量覆盖 Y%」（注明分母含无需配系数的品类）
- 文案带上当前判定依据（`名称含 鹅 / 鸭 / 鸡，排除 蛋`），一眼知道数字怎么来的
- 新增「维护禽类菜范围」弹窗：关键词增删改停（include/exclude 分栏）+ 规则预览统计（判定菜品数/销量/
  来源构成/被排除词挡掉数）+ 菜品级判定明细表（可搜索、可只看禽类菜）+ 批量改判（标为禽类菜/不算/清除）
- 指标卡新增「全量覆盖」一格

### 验证
- 新增 `tools/poultry-verify/verify-poultry-scope.ps1` —— **41 项全 PASS**：关键词读取/新增/停用/回读、
  4 项参数校验、显式名单三种模式与优先级（含「名含禽类词也能被手动排除」）、已配耗用强制计入、
  双覆盖率 7 项自洽校验（分子÷分母、≤100%、分母≤全量、禽类≥全量、未覆盖=分母−分子、缺口为子集）、
  「只数与改造前一致」、收尾还原与基线断言
- **关键词表快照自愈**：脚本先把关键词表快照到 `_tmp/scope-keywords-backup.json`，收尾**原样还原**；
  若上次异常中断留下备份，下次启动会**先自动还原再测试** —— 落实「收尾不能是唯一屏障」的教训
- 回归全绿：`verify-poultry.ps1` 49/49、`verify-poultry-batch.ps1` 29/29、`verify-risk-control.js` 12/12、
  `verify-yield-append.ps1` 13/13、`crosscheck-poultry.js` 独立复算与接口逐项一致（总销量 269,135.14、
  四渠道 174801.1/62711.48/24635.57/6987、博罗店 201 份 → 12.56 只）
- 交接文档 §11 的 **7 条核算不变量全部仍成立**（`verify-poultry.ps1` 第 5 节逐项断言，只数 76.5 与改造前一致）

### ⚠️ 顺带发现的既有脚本缺陷（本次**未改**，待确认后处理）
`canary-safe-teardown.ps1` 收尾断言写成「清理后禽类档案=0 / 已配置菜品=0」，即**假设数据库为空**。
现在库里有真实档案（禽类 1 + 已配置 22），该断言必然 FAIL；但：
- 5 项金丝雀存活检查**全部 PASS**（禽类/出成/206 耗用 usage_qty=2/采购 qty=555 全部存活且数值未变）
- 实测清理结果恰好等于「埋金丝雀之前」的状态（禽类 1 / 已配置 22 / 采购 0）→ 清理是干净的
- 该脚本本次**未被改动**（`git diff` 为空），FAIL 来自断言假设过时，与本改动无关
- 建议改为「与埋金丝雀前的基线一致」而不是「归零」

### ⚠️ 事故与恢复：`canary-safe-teardown.ps1` 在非空库上会破坏真实配置（本次踩到，已恢复）
在本地库（已填充真实档案）上运行该金丝雀脚本后：
- **菜品 206「招牌烧鹅饭」的耗用配置（`鹅肉 usage_qty=1`）被清空** → 欧景店核算只数 76.5 → **62.59**
- 根因：脚本第 31 行 `PUT /api/poultry/dish-usage/206`（整表替换）**覆盖**了 206 的真实配置，
  第 79 行收尾 `PUT rows=@()` 是**清空**而不是还原；第 82 行断言还假设「库为空」
  （`禽类档案=0 且 已配置=0 且 采购=0`）—— 该脚本编写时数据库是空的
- **它的 5 项金丝雀存活检查全 PASS 却有误导性**：只验证「自己埋的数据还在」，不验证「自己动过的真实配置有没有被改坏」
- **恢复方式**：从原始数据包库读出 206 原始配置（`menu_item_id=206, yield_id=52, usage_qty=1`）后写回，
  只数回到 **76.5**；同时清理了验证脚本残留的 3 条孤儿采购（`remark='自动化验证临时数据'`、bird 已删）
- **核对方式改进**：只比总数不够（总数不变但内容被换掉），应拿原始库与当前库**逐表逐行比对**
  `poultry_birds` / `poultry_yields` / `poultry_dish_usage` / `poultry_purchases`
- **建议修法（待确认，未擅自改）**：① 加守卫：库非空时拒绝运行；或 ② 先快照 206 原配置、收尾原样还原，
  并把断言改成「与运行前基线一致」。**该脚本不要在有真实数据的库上运行**。详见交接文档 §9.8
- 附带发现（脚本既有缺陷，未擅自改）：`verify-poultry.ps1` 收尾**先删禽类、再回收采购**，
  而 `listPurchases` 会 JOIN `poultry_birds` 过滤孤儿行 → 采购一旦失去禽类就**查不到**，回收必然失败。
  实测每次运行残留 1 条孤儿采购（本次共 3 条，已手工清理）。
  建议改成「**先回收采购、再删禽类**」，或回收时按 `remark` 直连库查询而不是走列表接口。

### 安全（仓库卫生）
- 补充 `.gitignore`：忽略 `data/*.sqlite-wal`、`data/*.sqlite-shm`
  —— sql.js 开了 `journal_mode=WAL`，运行时会生成这两个附属文件，同样含业务数据片段，
  此前会出现在 `git status` 里，容易被 `git add -A` 带进公开仓库
- **本次未部署云端**（云端仍是旧版；上云必须逐次获得用户明确同意）

---

## 2026-09-12 堂食菜品绑定接入「智能推荐」（复用菜品销量的识别规则）

### 需求
用户在「堂食菜品绑定」页手工逐个选 SKU 太慢，希望**直接复用「外卖/平台视角 → 菜品销量」里那套智能识别**来辅助关联菜品。

### 问题根因
两个页面各有一套匹配逻辑，能力差距很大：
| 页面 | 匹配方式 | 效果 |
|---|---|---|
| 菜品销量（`/api/dish-sales/analytics`） | 手工绑定 → 编码=skuid（名称互校）→ 名称+规格唯一 → 名称唯一，且**菜名归一化去掉【】（）[] 与空白** | 能识别 `【太公推介】…` 这类带前后缀的菜名 |
| 堂食菜品绑定（`/api/dish-sales/mappings`） | 只看 `dish_sales_mappings` 手工记录 | 全部显示「未绑定」，逼用户「本地菜品绑本地菜品」 |
| 「一键绑定」（`auto-bind`） | 只按**严格同名**（`by_name` 完全相等）+ 规格精确 | 带 `（外）`/`限量!`/`(迷你)` 前后缀的一律跳过 |

### 做法：抽成共享函数，规则只维护一处（不是复制代码）
`server.js` 新增（放在 `withDishMenuSku` 之后）：
- `dishMenuKey(value)` —— 菜名归一化：`replace(/[【】\[\]（）()\s]/g, '')`
- `buildDishMenuIndex(extraIds)` —— 载入在售菜品（排除员工餐），建 `menuById / menuBySkuid / menusByName / menusByNameSpec` 四个索引；`extraIds` 补入已下架但被手工绑定的菜品
- `resolveDishMenuSmart(index, group)` —— 返回 `{ menu, reason }`，`reason ∈ manual | skuid | name_spec | name | ''`
- `RECOMMEND_REASON_LABELS` —— 依据中文标签

**`/api/dish-sales/analytics` 改为调用同一套函数**（删掉内联的重复实现）→ 以后改识别规则只改一处，不会两页漂移。

### 接口
1. `GET /api/dish-sales/mappings` 每行新增推荐字段（**仅未绑定行返回**）：
   `recommend_reason / recommend_reason_label / recommend_menu_item_id / recommend_menu_name / recommend_sku_label / recommend_sku_variant / recommend_sku_price_summary / recommend_category / recommend_cost`
   并新增全量统计（不受分页/搜索影响）：`recommend_summary { pending, recommendable, unmatched }`
2. 新增 `POST /api/dish-sales/mappings/auto-bind-smart` —— 用同一套规则批量绑定未识别项，
   回报 `bound / skipped / reasons{skuid,name_spec,name,unmatched} / samples(前 20 条样例)`
   - **老接口 `auto-bind` 保持不动**（严格同名版），两个入口并存、各司其职
   - 事务保护（BEGIN/COMMIT/ROLLBACK + 失败回滚）

### 前端（`DishSalesBinding.vue`）
- 「对应本地 SKU」列三态：**已绑定**（蓝卡）/ **有推荐**（绿卡：推荐 SKU + 依据标签 + 成本 + 「采用推荐」/「另选」）/ **无推荐**（提示名称差异大，给「手工绑定」）
- 顶部按钮拆成两个：「**严格同名绑定**」（原逻辑，最保守）与「**⚡ 智能采用全部推荐（N）**」（N 为后端全量统计的可绑定条数，0 时禁用）
- 确认弹窗写清规则（忽略括号前后缀 / 编码互校 / 名称或规格唯一；**只做精确匹配不做模糊匹配**）
- `loadData` 保存 `recommend_summary` 供按钮显示条数

### 实测（本地全量）
- 待绑定分组 **1,345** 个 → **268 个可智能推荐**（`name_spec` 190 / `name` 77 / `skuid` 1），**1,077 个仍需手工**
- 典型生效：`打包盒`、`太公白米饭`、`烧鹅双拼饭`、`超爽柠檬茶`、`自选双拼饭`（名称+规格匹配）
- 仍无法识别的大项：`烧鹅饭` 9,710、`烧鸭饭` 5,083、`至尊鹅肶饭` 3,517（档案里叫「招牌烧鹅饭」等，**名称差异真实存在**）
  → 按项目既定方针（§9.3）**不做模糊自动绑**，保留人工处理；后续可选做「疑似候选提示」（只提示不自动绑）

### 验证
新增 `tools/poultry-verify/verify-dish-binding-recommend.ps1` —— **18 项全 PASS**：
- 推荐统计自洽（可推荐 + 无推荐 = 待绑定）、未绑定行带推荐、已绑定行**不**返回推荐
- 识别规则只读断言：**编码匹配必须编码+名称双一致**（名称互校防错绑）、名称类推荐与档案名归一化后一致
- 单行采用 → 变已绑定 → 解绑还原（用自建临时菜品「验证临时绑定菜-智能推荐」）
- **批量智能绑定快照回滚**：跑前记录全部绑定键 → 跑（实测新增 268 条）→ **只删本次新增** → 断言回到基线 54 条且无多删
- 收尾回收临时菜品；脚本自带**自愈**（上次中断残留会在下次启动时先清理）

> 安全：写操作只作用于自建临时菜品与「本次新增的绑定行」，用户的 54 条真实绑定全程未变。

**本次同样未部署云端。**

---

### 追加：疑似候选（2026-09-12 深夜，用户反馈「搜太公三宝饭还是老样子」后补做）
**现象**：用户在绑定页搜「太公三宝饭」，6 行全是「无自动推荐」→ 体感"功能没生效"。
实际是那些行的销售名（`【太公三宝饭】招牌经典烧鹅三拼`、`太公三宝饭 太公三宝饭`）与档案名
`太公烧鹅三宝饭` **归一化后仍不相等**，精确规则按设计不给推荐 —— 页面于是退化成手工绑定。

**做法：精确推荐之外再加一层「疑似候选」（只提示、绝不自动绑定）**
- 新增 `dishNameSimilarity(a, b)` —— 四项加权，对中文菜名稳定可解释：

  | 因子 | 权重 | 作用 |
  |---|---|---|
  | Jaccard（字符集交并比） | 0.45 | 基础相似度 |
  | containment（交集 ÷ 短名长度） | 0.25 | 销售名常带套餐描述，Jaccard 会被长名稀释 |
  | 最长公共连续子串占比 | 0.15 | **顺序信息**，区分「烧鹅饭 ⊂ 招牌烧鹅饭」与「烧肉拼烧鹅饭」 |
  | 长度比 | 0.15 | **防短名噪声**，否则「…含时蔬+海咸鸭蛋+例汤」会把「咸鸭蛋」当候选 |

  整体包含时额外 +0.1（最强信号）；阈值 0.6
- `suggestDishCandidates()`：候选名长度 < 销售名长度 × 0.4 时跳过；排除员工餐菜品；同分按名称短者优先
- `GET /api/dish-sales/mappings` 每行新增 `recommend_candidates[]`（最多 3 条，含 `menu_item_id/menu_name/sku_label/category/cost/score`）
  与 `recommend_candidate_label` —— **精确推荐与疑似候选互斥**（有精确推荐就不给候选）
- `POST /api/dish-sales/mappings/auto-bind-smart` 新增回报 `written_keys[]`（本次实际写入的绑定键，供精确回滚）
- `GET /api/dish-sales/mappings` 的 `ORDER BY` 补唯一键次级排序 → 原先同收入行顺序随机，**翻页会漏行/重复**（既有缺陷，已修）

**实测效果**
| 销售名 | 结果 |
|---|---|
| `太公三宝饭 太公三宝饭` | 疑似 → **太公烧鹅三宝饭(0.741)** > 【套餐】太公三宝饭（靓汤+柠檬茶）(0.613) |
| `【太公三宝饭】招牌经典烧鹅三拼` | 疑似 → **太公烧鹅三宝饭(0.658)** |
| `【太公三宝饭】含时蔬+海咸鸭蛋+例汤` | 无候选（`咸鸭蛋` 噪声已被长度比挡掉 ✅） |
| `烧鹅饭` | 疑似 → **招牌烧鹅饭(0.86)** > 烧肉拼烧鹅饭(0.845) > 烧鹅双拼饭(0.71) |
| `烧鸭饭` | 疑似 → **金牌烧鸭饭(0.86)** > 金牌烧鸭腿饭(0.65) |
| `【新鲜出炉】明炉烧腊双人餐(含时蔬/例汤)` | 疑似 → **【套餐】明炉烧腊双人餐(0.6)** ✅ 正是该绑的目标 |
| `太公白米饭` / `超爽柠檬茶` | 精确推荐自身（互不干扰） |

**前端**（`DishSalesBinding.vue`）：无精确推荐但有候选时显示**橙色「疑似」卡片**
（候选名 + 相似度 + 分类 + 成本 + 「采用」/「其他候选」下拉/「另选」）；
`openBindDialog` 打开时**自动预选**（已绑定 → 精确推荐 → 首个疑似候选），省去手敲搜索（下拉仍可改）。

**验证**：`verify-dish-binding-recommend.ps1` 扩到 **23 项全 PASS**，新增候选断言：
结构完整 / 不含员工餐 / 按相似度降序 / 与精确推荐互斥 / 相似度均在阈值之上。

### ⚠️ 事故与修复：验证脚本批量绑定曾残留 152 条（2026-09-12 深夜）
- **现象**：两次跑批量智能绑定后，`dish_sales_mappings` 从 80 条涨到 232 条，其中 **01:34 那批 152 条是脚本残留**
- **根因（两个）**：
  1. 首轮回滚用「总数差」推断新增键 —— 用户在页面上并发绑定（01:30–01:33 他自己绑了 16 条）污染差值，回滚范围错
  2. 拼接回滚键时用了接口返回的**原始 spec**，而接口写入用**归一化 spec**（`''` 与 `'--'` 等价）→ 键对不上 → 漏删
- **修法**：① 接口回报 `written_keys[]`，脚本据此**精确回滚**（不靠总数差）；② 脚本加 `NormSpec()` 与后端口径对齐；
  ③ 断言改为**并发容错**（只断言「本次写入的已删干净」+「基线行一条没丢」，不断言总数相等）
- **清理**：按 `created_at` 时间窗（01:34 同一秒写入 152 条，与用户操作时段可区分）精确删除，
  **删前已备份**到 `G:\zhongkong-backend\tools\mapping-residue-backup-2026-09-12_0134.json`；清理后总数回到 **80 条**（用户真实数据完好，其中 27 条是用户当晚自己用新功能绑的）
- **教训**：① 验证脚本**不要用总数差推断自己写了什么**，要让接口回报写入键；
  ② 键匹配必须与后端**同一归一化口径**；③ 跑写库验证前先确认用户是否正在页面上操作

### 追加：整只部位示意图 + 份量自检（2026-09-12，用户提议）

**用户三条反馈与处理**
| # | 反馈 | 处理 |
|---|---|---|
| 1 | 从缺口明细「去绑定」绑完后回来，缺口清单没刷新，已绑的菜还在 | ✅ **已修**（bug，见下） |
| 2 | 鹅胚重 6.6 斤（3.3kg）→ 烤制失水出汁 → 成品约 2kg，是否有参考价值 | ⏸ **暂缓**：有价值（出成率≈60.6% 可用于损耗监控 + 部位克重交叉校验），但**用户尚未拿到具体克重资料**，改了没法填会让测试失败 —— 等资料齐了再做 |
| 3 | 能否做鹅的 demo，在图上点选部位 | ✅ **已做第一步**（本次） |

**修复：缺口明细不实时刷新**
- 根因两处叠加：① `App.vue` 用了 `<KeepAlive :max="30">`，从绑定页返回核算页时组件不重新 mount，`onMounted` 的取数不再执行；②「查看缺口明细」按钮只做 `gapVisible = true`，不重新取数
- 修法：新增 `onActivated()`（每次页面激活即重取核算）＋「查看缺口明细」改走 `openGapDialog()`（开弹窗前先刷新）

**新增组件 `frontend/src/components/PoultryBodyMap.vue`（整只部位示意图）**
- **SVG 俯视示意**：头+颈 / 翅（左右） / 上庄 / 下庄 / 腩 / 腿（左右） / 底部横条「切片肉区」（鹅肉·小料·双拼·三宝饭 共用）＋ 整只/半只虚线外框
- **配色即语义**：身体类=蓝、副产品（翅/头颈）=橙、切片肉区=绿、**未配置=灰虚线**
- **点选联动**：点图上部位 → 高亮下方表格对应行（`:row-class-name`）＋ 顶部提示「已定位：鹅腿（一只出 8 份）」；未配置区域提示去「新增部位」
- **份量自检**（右侧面板，本次核心价值）：
  1. **上庄 2 块 + 下庄 2 块 = 一整只身体**（应恰好 100%，>100% 说明把「数块数」当成份量口径）
  2. **腿 2 条占下庄的比例**（正常 30~70%；≥80% 提示"检查是否该填一只出 8"；≤15% 提示系数填大了）
  3. 副产品标注「数块数口径，不参与份量守恒」
- 部位名归类用关键词（头/颈、翅/战斧、上庄、下庄、腩、腿/肶、整只/半只、肉/小料/双拼/三宝/片），**兼容用户自取的名字**
- 集成位置：基础档案 → **② 整只出成拆解** 页签顶部（表格上方）

**自检有效性实测**（无需浏览器，用真实配置复算）：
| 配置 | ① 上下庄合计 | ② 腿占下庄 |
|---|---|---|
| 当前（鹅腿 = 8） | 100% ✅ | 50% ✅ 正常 |
| 旧值（鹅腿 = 4） | 100% ✅ | **100% ⚠ → 提示"检查是否该填 8"** |
→ 若当初有此面板，填 4 时就能当场报警，不必等算完只数才发现那 +15% 偏差

**本次同样未部署云端。**

---

## 2026-09-12 用门店日报校准系数：鹅腿「一只出 4 → 8」（欧景店偏差 15.2% → 1.9%）

### 背景（用户提供的关键业务事实）
- **「上庄自带鹅翅，下庄自带鹅腿」** —— 翅/腿不是独立于上下庄的部位，而是它们的组成部分
- **「一份鹅腿饭用掉一整个鹅腿」**
- **「门店日报是今天共消耗了多少只鹅，不存在口径误差」** —— 80 只是可靠的校准基准

### 偏差现象
欧景店关联完鹅类菜品后（鹅类菜覆盖率 95.27%），9/1–9/10 算出 **92.13 只**，门店日报 **80 只**，偏差 **+15.2%**。

### 定位方法：单系数敏感性分析（以 80 只为基准）
只动「鹅腿」的「一只出几份」，其余配置不变：

| 鹅腿 | 总只数 | 偏差 |
|---|---|---|
| 4（原值） | 92.13 | +15.2% |
| 6 | 85.05 | +6.3% |
| **8** | **81.51** | **+1.9%** ✅ |
| 10 | 79.38 | −0.8% |
| 12 | 77.96 | −2.6% |

### 结论与依据
**鹅腿应为 8** —— 物理推导：

```
一只鹅 = 1 只份量
├─ 上半身 1/2 只 → 2 个上庄（每个 1/4 只，各含 1 只翅）
└─ 下半身 1/2 只 → 2 个下庄（每个 1/4 只，各含 1 条腿）
                  └─ 腿约占下庄一半份量 → 一条腿 = 1/4 × 1/2 = 1/8 只
⇒「一份腿饭 = 一整条腿」⇔「一份占 1/8 只」⇔ 系统里填 8
```
填 4 等于说"一份腿饭吃掉半个下庄"，把腿的份量算成了 **2 倍** —— 这就是那 15% 虚高的来源。
（历史脉络：用户最初填 2 属"数条数"口径 → 算 118.5 只；改 4 后 76.5 只，方向对但未到位；**8 才与物理一致**。）

### 改动（2026-09-12 经用户确认后执行）
- `poultry_yields`：`bird_id=18`（鹅·鹅）的「鹅腿」`parts_per_bird` 由 **4 → 8**，其余 11 个部位不变
- 改前快照备份：`G:\zhongkong-backend\tools\yields-backup-bird18-before-leg8.json`（可原样回退）
- 结果：欧景店 9/1–9/10 **92.13 → 81.51 只**（门店日报 80，偏差 **+1.9%**），瓶颈由「鹅腿」变为「**鹅肉**」

### 校准后的部位贡献（欧景店 9/1–9/10）
| 部位 | 一只出 | 需求 | 折鸟 |
|---|---|---|---|
| 鹅肉 | 11 | 205 份 | 18.64 只 ★瓶颈 |
| 下庄 | 4 | 63 份 | 15.75 只 |
| 上庄 | 4 | 46 份 | 11.50 只 |
| 鹅腿 | **8** | 85 份 | 10.63 只 |
| 半只 / 三宝饭 / 双拼 / 小料 / 整只 / 腩 | 2 / 29 / 23 / 17.5 / 1 / 4 | — | 7 / 5.79 / 4.61 / 4.34 / 2 / 1.25 只 |
| 鹅头带颈（副产品，不占鸟） | 1 | 32 份 | 32 只 |

### 遗留待办（已与用户说明）
1. **鹅肉「一只出 11」是新瓶颈**（18.64 只，占 23%）—— 需用户按实际单份给量复核
2. **剩余 40 份缺口**（`烧肉拼烧鹅饭` 24 份、`招牌烧鹅腿（单）` 16 份）补配后只数还会略涨（预计 ~83 只）
3. **禽类档案的「单只净重 / 单只成本」仍为 0** → 折重与理论成本显示 0，需补填
4. 副产品的「一只出」目前按**数块数**填（头颈 1、战斧 2）—— 在「取最大」逻辑下不影响总数 ✅，
   但风控对账会用 `只数 × 一只出` 推算"应有份数"（80 只鹅应有 80 个头颈 vs 实际卖出 32 份），属风控的预期行为
5. **切片肉四个部位的互斥关系待确认**：`鹅肉 11` / `小料 17.5` / `双拼 23` / `三宝饭 29` 各自都是"一只出 N 份"，
   若它们取自同一块切片肉（仅装盒不同），当前"相加"会偏高；若为不同等级/部位则相加正确
6. 数据边界：`dish_sales` 只到 **2026-09-10**，9/10 之后的销量尚未导入

**本次同样未部署云端。**

---

## 2026-09-12 模型升级：料篮模型 → 资源约束模型（四期）+ zone_code / yield_mode 落库

### 起因：外部评审指出的模型缺陷
1. **副产品「取最大」过于宽松** —— 一只鹅 2 只翅，战斧卖 120 份本需 60 只鹅，但当身体需求更大时，翅的约束被完全忽略
2. **一个字段混两套口径** —— `parts_per_bird` 同时承载"份量倒数"（上庄 4）与"物理件数"（头颈 1）
3. **整只/半只/切片肉的资源关系模糊**

### 用户确认的业务规则（建模依据）
- **「上庄自带鹅翅，下庄自带鹅腿」**
- **「一份鹅腿饭用掉一整个鹅腿」**
- **「卖 2 个下庄 + 2 份腿饭 = 消耗 4 条腿 = 2 只鹅」**；**「下庄可拆成鹅肉和鹅腿，但鹅腿不能反推成下庄」**
- **「一只自然是一整只鹅，半只就不包含鹅头带颈」**
- **「切片肉四部位同源」**
- 门店日报 = 当天实际消耗整鹅数，**无统计误差**（校准基准）

### 新模型：一只禽 = 若干固定容量的资源池，各算需求只数取 MAX
| 资源池 | 每只容量 | 谁消耗它 |
|---|---|---|
| 身体 | 1 只（份量） | 整只 1 / 半只 1/2 / 上庄 1/4 / 下庄 1/4 / 腿 1/8 / 腩 / 切片肉 1/N → **相加** |
| **腿** | **2 条** | 下庄(自带)、腿饭、半只、整只(2) |
| **翅** | **2 只** | 上庄(自带)、战斧、半只、整只(2) |
| **头颈** | **1 个** | 头颈菜品、整只（**半只不含**） |

```
N = max(身体份量合计, 腿需求÷2, 翅需求÷2, 头颈需求÷1)    ← 瓶颈即取到最大值的那一池
```
**用用户例子验证**：2 个下庄(各 0.25 只身体 + 1 条腿) + 2 份腿饭(各 0.125 只 + 1 条腿)
→ 身体 0.75、腿 4÷2 = **2 只** ✅ 与用户口径完全一致。

### 数据结构（`lib/db.js` 新增两列 + 自动迁移）
- **`zone_code`**：图示区域枚举（whole/half/upper/lower/leg/wing/head_neck/belly/meat/other）
  —— 「正则负责猜、枚举负责真相」：核算靠它判定消耗哪些限量资源，改名不影响；正则只在新增部位时做推荐
- **`yield_mode`**：口径类型（fraction 份量型 / count 件数型 / weight 重量型）
  —— fraction 与 count 计算式相同（需求 ÷ N），**引入它不改变任何结果**，只把语义显性化
- 迁移：按部位名预判写入（实测 12 个部位全部正确归类）

### 后端（`lib/poultry-accounting.js`）
- 新增 `ZONE_LIMITED_CONSUME`、`RESOURCE_CAPACITY`、`RESOURCE_LABELS`、`normalizeZoneCode()`、`normalizeYieldMode()`
- `calculate()`：`partTotals` 增加 `limited` 累加；`parts[]` 新增 `zone_code / is_byproduct / body_birds / limited`；
  `perBird` 改为资源池汇总 → `constraints{body,leg,wing,head_neck}` → `N = max(...)`
- 返回字段变更：**移除 `byproduct_birds`**；新增 `constraint_birds{body,leg,wing,head_neck}`、
  `resource_demand{leg,wing,head_neck}`、`governing_resource` / `governing_resource_label`（governing 现为"腿/翅/头颈/身体"）
- `saveYields()` 支持保存 `zone_code` / `yield_mode`

### 前端
- 只数卡说明改为：`瓶颈：腿（鹅腿）—— 身体 81.51 ｜ 腿 83 ｜ 翅 37 ｜ 头颈 34 只`
- 「按部位汇总」页脚改为资源池约束文字
- 「整只出成拆解」表格新增 **「图示区域」「口径类型」两列**（可手工修正）
- 示意图 `PoultryBodyMap.vue` 改用落库的 `zone_code`（缺列时才回退按名称猜）

### 核算不变量调整（先报告、经用户确认后才改）
| 原 | 现 |
|---|---|
| 只数 = max(身体相加, 副产品最大) | **只数 = max(身体, 腿, 翅, 头颈 四个资源池)** |
| （无） | **新增：瓶颈资源 = 约束最大的那个资源池** |
| 旧口径 ≥ 新口径 | 保留（实测仍成立：6832 ≤ 11785.45）✅ |
| 其余 4 条 | 不变 ✅ |

### 实测结果变化（欧景花园店 2026-09-01~09-10，门店日报 80 只）
| 模型 | 只数 | 瓶颈 | 偏差 |
|---|---|---|---|
| 旧（料篮，鹅腿=8） | 81.51 | 身体·鹅肉 | +1.9% |
| **新（资源约束）** | **83.00** | **腿·鹅腿** | **+3.8%** |

四个资源池约束：身体 81.51 ｜ **腿 83** ｜ 翅 37 ｜ 头颈 34 —— 腿需求 166 件 = 下庄 63 + 腿饭 85 + 半只 14 + 整只 4 ✅
> 偏差略增但**模型更正确**（符合用户"先把资源关系建对，再用 80 只校准"的原则；旧值 8 本就是为对齐 80 拟合出来的）

### 验证
全部 **168 项 PASS**：`verify-poultry.ps1` **50/50**（新增"四个资源池"+"瓶颈资源"两项断言）、
`verify-poultry-batch.ps1` 29/29、`verify-poultry-scope.ps1` 41/41、`verify-yield-append.ps1` 13/13、
`verify-dish-binding-recommend.ps1` 23/23、`verify-risk-control.js` 12/12（欧景店 83 只 vs 日报 80）、
`crosscheck-poultry.js` 独立复算一致

### 剩余待办（用户已授权，按序推进）
1. **一期**：自检扩三档（❌错误 / ⚠️风险 / ℹ️提示）+ 父子资源重复校验（同一菜品同时配"下庄"与"腿"）+ **MAPE 采购校准面板**（模型理论 vs 门店日报滚动偏差）
2. **三期**：切片肉克重池（`鹅肉/小料/双拼/三宝饭` → 统一到「可切片鹅肉池」用克重 SUM 后除单鹅出成）——**等用户提供克重资料**
3. 部位示意图的"点图新建部位"体验（第二步）

**本次同样未部署云端。**

---

## 2026-09-12 一期：自检三档 + 父子资源重复校验 + MAPE 采购校准

### ① 部位示意图自检扩为三档（`PoultryBodyMap.vue`）
| 档位 | 触发条件 | 示例 |
|---|---|---|
| ❌ **错误**（物理不可能） | 出成数量为 0 / 空；份量型下「一只出份数 < 1」（单份超过一整只） | 「鹅腿」出成数量为 0 或空 |
| ⚠️ **风险**（可能填错） | 上下庄合计偏离 100%；腿占下庄 ≥80% 或 ≤15% | 腿占下庄 100%（该填 8 时） |
| ℹ️ **提示**（✅ 也归此档） | 上下庄合计正确、腿比例正常、切片肉区多部位、副产品口径、未配置部位 | 切片肉区有 4 个部位 → 提示三期会统一按克重折算 |

### ② 父子资源重复校验（`findUsageConflicts`）
**依据**：腿含在下庄里、翅含在上庄里、整只含全部 —— 同一菜品同时配置「父」与「子」部位即重复计量。
- `ZONE_CHILDREN`：`whole → {half,upper,lower,leg,wing,head_neck,belly,meat}`、`half → {upper,lower,leg,wing,meat}`（**半只不含头颈**）、`upper → {wing}`、`lower → {leg}`
- **只报警不拦截**（确实存在"额外加料"的合法场景）
- 三个入口都覆盖：`saveUsageForMenu` / `batchSaveUsage` 返回 `warnings`（前端弹窗提示）；`calculate()` 写入 `coverage.warnings`（覆盖历史数据，同一菜品只检测一次）
- **实测**：自建临时菜品同时配「下庄 + 鹅腿」→ 正确报警
  `⚠️ 同时配置了「下庄（下庄）」与「鹅腿（腿）」—— 腿通常已包含在下庄之内，可能重复计量；若是额外加料可忽略`（临时菜品已回收）

### ③ MAPE 采购校准（`calibrationReport` + `GET /api/poultry/calibration`）
**意义**：评价参数好坏不靠「腿占下庄 15%~80%」这类经验阈值，而看**历史数据拟合** —— 改完系数后滚动 MAPE 若从 7.8% 降到 2.6%，才算真的改善。
- 数据源：`poultry_purchases`（门店实际消耗/采购只数，手动录入，UNIQUE(store_id, month, bird_id)）
- 逐条对比：模型理论只数（按月跑一次核算）vs 实际只数 → 偏差、`MAPE`、平均偏差（带正负）、**中位绝对偏差**（抗异常）
- 分层评价：`< 5%` 拟合良好 / `< 10%` 尚可 / `≥ 10%` 建议校准系数
- **过滤孤儿记录**：所属禽类已被删除的历史遗留采购行（`animal` 为空）不参与校准，只回报 `skipped_invalid` 计数与说明
- 界面：核算页新增「采购校准 · 模型 vs 门店实际」面板（点「开始校准」触发，避免每次进页面都跑核算；默认最多算 12 个「门店 × 月」组合并标注 truncated）

### 验证
**全部 168 项 PASS**：`verify-poultry.ps1` **50/50**、`verify-poultry-batch.ps1` 29/29、`verify-poultry-scope.ps1` 41/41、
`verify-yield-append.ps1` 13/13、`verify-dish-binding-recommend.ps1` 23/23、`verify-risk-control.js` 12/12、
`crosscheck-poultry.js` 独立复算一致
> 过程中出现的一次 `已配置菜品 24→23` FAIL 已定位：是**测试自建临时菜品造成的基线时序问题**（非脚本破坏数据），
> 清理后 50/50 全绿

### 剩余（下次）
- **三期**：切片肉克重池（等用户提供克重资料）
- 示意图「点图新建部位」体验（第二步）
- 云端上线（需用户逐次明确同意）

**本次同样未部署云端。**

---

## 2026-09-12 扩展鸡/鸭：建档、字段简化、混搭菜口径

### 用户口径（关键业务事实）
- 鸡/鸭的部位**比鹅简单**，只有 6 个：**一只 / 半只 / 单份 / 双拼量 / 三拼量 / 腿**
- **没有**：鸡鸭腩、鸡鸭头颈、战斧（翅）
- **鸡翅是单独采购的**（不拆整鸡）→ 不建 wing 部位
- **一份腿饭 = 一整条腿**
- 部分双拼饭/三宝饭是**鸡鸭鹅混搭**，需要 **1+1** 或 **1+1+1** 多行挂载

### 建档结果（经用户同意后执行）
| 禽类 | id | 净重/成本 | 6 个部位（一只出几份） |
|---|---|---|---|
| **鸡·鸡** | 78 | 1.6kg / ¥35 | 一只 1 ｜ 半只 2 ｜ 单份 6 ｜ 双拼量 12 ｜ 三拼量 18 ｜ 鸡腿 5 |
| **鸭·鸭** | 79 | 2.2kg / ¥45 | 一只 1 ｜ 半只 2 ｜ 单份 6 ｜ 双拼量 12 ｜ 三拼量 18 ｜ 鸭腿 5 |
| 鹅·鹅 | 18 | 3.3kg / ¥128 | 12 个部位（**未改动**） |
- 备份：`G:\zhongkong-backend\tools\poultry-birds-backup-before-chicken-duck.json`（3 个禽类 + 24 条出成，可回滚）
- **单份/双拼量/三拼量/腿 的份数是初始占位值**，待用「采购校准」按门店日报反推

### 代码改动：区域识别补充鸡鸭字段名
`单份 / 三拼量` 原会落到 `other` → 已在三处补进 `meat` 规则（`lib/db.js` 迁移 + `normalizeZoneCode` + 前端 `PoultryBodyMap`）：
```
/肉|小料|双拼|三宝|片|单份|三拼|拼量/ → meat
```
实测（建临时"鸡"档案后立即清理）：`一只→whole`、`半只→half`、`单份/双拼量/三拼量→meat`、`鸡腿→leg` 全部正确 ✅

### 模型自适配说明（无需改代码）
鸡/鸭没有头颈与翅部位 → 那两个资源池自动退化为"永不成为瓶颈"：
- 翅需求只来自「一只」(2) 与「半只」(1)，**恒 ≤ 腿需求** → 翅约束不可能超过腿约束
- 头颈需求只来自「一只」(1) → 同理
所以**不会因为"少配头颈/翅"而算不准** ✅ 腿与身体资源正常参与 MAX。

### 口径澄清（用户问"一份一个腿 → 一只出几份填几"）
**「一份 = 一条腿」与「一只出几份」是两件事**：
- `「一只出几份」= 1 ÷ (一份占整只的比例)`，是**份量口径**
- 填 2 等于说"一份腿饭吃掉半只鸡的身体"，会让身体约束严重虚高（当年鹅就踩过这个坑）
- 鸡腿约占整只 **1/5** → 初值填 **5**（鹅腿占 1/8 → 填 8，同一逻辑）

### 混搭菜（1+1 / 1+1+1）的挂法
系统天然支持：`poultry_dish_usage` 唯一键是 `(menu_item_id, yield_id)`，**一个菜可挂多行**，在「菜品耗用」编辑弹窗里逐行加。
- 判据：**菜名里出现的禽类字 = 要挂的行数**；`叉烧 / 烧肉 / 叉` 属猪肉，不挂行
- 实例：`鸡鸭双拼饭` → 鸡·双拼量 + 鸭·双拼量；`烧鹅拼豉油鸡饭` → 鹅·鹅肉（双拼）+ 鸡·双拼量
- 已生成对照表（**只生成未写入**）：
  - `tools\禽类菜品耗用建议v2.tsv`（108 个禽类菜：当前配置 / 建议配置 / 需要补齐）
  - `tools\鸡鸭菜品耗用建议.tsv`（早期版本）
- 现状：含「拼」类菜 **113 个 / 约 6 万份**，分三类
  - **菜名已明确组合**（约 8 个）：`烧鹅拼豉油鸡饭`、`烧鹅拼咸鸡饭`、`太公三宝饭(烧鹅+叉烧+咸鸡)`、`鸡鸭双拼饭`、`叉鸭双拼饭`…
  - **只说双拼/三宝未言明组合**（约 20 个）：`烧鹅双拼饭 7,165`、`太公烧鹅三宝饭 3,700`、`烧鹅拼叉烧饭 1,464`…（需用户确认默认组合）
  - **顾客自选类**（约 20 个 / 约 1 万份）：`自选双拼饭 5,009`、`自选三拼饭 935`、`经典烧味双拼饭(5选2) 541`…
    → 三个可选口径：① 按最热门组合固定 ② 按选品概率分摊 ③ 整类排除（先保证覆盖率真实）

### 用户决定：菜品关联由用户自行在页面完成
AI 只提供对照表与建议，**不代执行批量关联**（用户明确表示自己配）。

### 配完后的两个提醒
1. **外购翅要排除**：`蜜汁鸡翅 5,918`、`蜜汁鸡翅饭 1,732`、`蜜汁鸡翅拼盘 125` 用的是外购翅 → 到「维护禽类菜范围」标为**不算**，否则永远算"未配置"、拉低禽类菜覆盖率
2. **自选类要先给交代**（排除或按热门组合固定），否则约 1 万份长期挂在缺口里

**本次同样未部署云端。**

---

## 数据分析 · 外卖/团购视角门店看板字段调整（前端 UI）

文件：`frontend/src/views/analysis/BusinessAnalytics.vue`（纯前端模板改动，未改任何接口与数据口径）

### 1. 「门店经营指标」表字段顺序
按业务阅读顺序重排为：**营业额（优惠前总额）→ 实收（营业收入）→ 预估毛利 → 毛利占比 → 有效订单 → 客单价 → 曝光人数 → 入店人数 → 下单人数 → 入店转化率 → 下单转化率**；
淘宝闪购的「实际到账」仍紧跟「实收」之后。

### 2. 「门店经营结果」面板
与上方「门店经营指标」字段重合 → 现在只在**总数据视角 · 门店视角**展示，外卖/团购的门店视角不再显示。

### 3. 「菜品销量」新增「成本」列
位置：**本地菜品 SKU 之后、预估成本之前**。
- 显示绑定本地菜品 SKU 的**单份成本**（`menu_items.cost`）
- 未绑定 → `—`；配置了「规格反解」的商品 → `按规格拆分`（其单份成本为 0，成本以规格拆分合计为准）
- 表头悬停有口径说明：`成本 = 单份成本；预估成本 = 单份成本 × 净销量`

### 4. 「预估毛利」悬停查看计算构成
三处都有（外卖门店表、美团团购门店表、菜品销量表），数值带虚线提示可悬停：

| 位置 | 构成 |
|------|------|
| 外卖门店表 | 营业收入（实收）− 菜品成本合计（`cost_amount`）= 预估毛利 |
| 美团团购门店表 | 实际到账金额 − 菜品成本合计 = 预估毛利 |
| 菜品销量表 | 分摊实收（团购为商家应得）− 菜品成本 = 预估毛利；明细显示 `单份成本 × 净销量` |

### 验证
`_tmp/verify-profit-tooltip.js`（跑在 `data/database.sqlite` 的**副本**上，不写线上库）逐项核对提示里的算式与后端 `gross_profit` 完全一致：
- 美团外卖门店（龙岗万科店）：¥80,006.48 − ¥9,812.95 = ¥70,193.53 ✅
- 菜品销量（太公三宝饭招牌经典烧鹅三拼）：¥18,340.21 −（¥12.38 × 699）= ¥9,686.59 ✅
- 美团团购门店（上排店）：¥3,635.32 − ¥198.00 = ¥3,437.32 ✅

**本次未部署云端。** 前端已 `npm run build:frontend` 重建 `frontend/dist`（本地 3456 生效）；上云时按 §一 步骤 2 重新上传 `frontend/dist` 即可。

### 5. 「经营总览」分组：空的时候不再显示
外卖视角·门店视角下该分组已经没有任何面板（经营指标已上移到页面上方、门店对比表只在总数据视角显示）
→ 页签改为按条件渲染：只有确实还有面板时才出现「经营总览」，隐藏时自动落到「菜品销量」页签。
（总数据视角、外卖品牌/平台视角、团购视角都照旧有内容，不受影响。）

### 口径说明：菜品「实收」为什么是分摊值，而不是直接抓真实值
外卖/团购菜品看板的数据源是平台「全部门店商品销量」报表（落库 `business_product_sales`），
该表只有 **销量 + 销售额**，**没有收入列**；平台的真实实收只到「门店 × 日期 × 渠道」粒度
（`business_revenue_records.actual_amount`）。所以菜品级实收只能由
`门店当日平台实收 × (该菜品销售额 ÷ 当日该渠道销售额合计)` 摊出来，
这样能保证 **Σ菜品实收 = 平台实收**，与经营指标口径永远对得上。

真实值确实存在，但在**收银机品项明细**里（`pos_product_sale_details.income_amount` = 报表列「品项收入(元)」），
覆盖不足（区间 2026-08-01 ~ 09-13）：

| 渠道 | 收银机真实品项收入 | 平台实收 | 覆盖率 | 门店×日覆盖 |
|------|------|------|------|------|
| 美团外卖 | ¥214,706.83 | ¥294,717.96 | 72.9% | 554 / 751 |
| 淘宝闪购 | ¥174,844.68 | ¥239,277.87 | 73.1% | 506 / 739 |
| 京东外卖 | ¥24,902.07 | ¥81,515.95 | 30.5% | 227 / 344 |

菜品名也对不齐：美团外卖 243 个平台菜品名中 37 个与收银机名称不一致（如「现烤烧鸭下庄（不含米饭）」）。
→ 直接抓真实值会导致：分母缺 27%~70%、部分菜品匹配不上，单店毛利虚低且跨店不可比。
**总数据视角的「菜品销售分析」用的就是收银机真实「品项收入」，不摊。**

分摊的代价（使用时注意）：假设各菜品优惠率一致；若某菜品单独做了大力度活动或赠品，其真实到手会低于分摊值。
另有 1.7%~2.4% 的门店×日缺平台实收（京东 6 个、淘宝 18 个），这些行的菜品实收为空，
页面目前会显示 ¥0.00 而不是「—」（待确认是否改成「—」）。

### ⚠️ 事故与修复：「经营总览」改动引发的 TDZ 白屏（本次会话引入，已修复）

**现象**：外卖/团购视角页面整页白屏，控制台 `Cannot access 'isMeituanOperationView' before initialization`。

**根因（我引入的）**：为「经营总览」加的条件判断
`overviewTabHasPanels`（computed）引用了 `isMeituanOperationView`，而该变量当时定义在其后 ~143 行；
更关键的是配套的 `watch(tabs, ...)` 会在 **setup 期间就求值一次**（Vue 的非 immediate watch 也会
先 `effect.run()` 取初值），于是 setup 未走完就触发了对后声明 `const` 的访问 → TDZ 抛错 → 组件挂载失败。

**修复**：把 `isGroupOperationView` / `isMeituanOperationView` 两个 computed 上移到
`overviewTabHasPanels` 之前定义，并在原位置留注释说明约束（顺序不能再调换）。
另注：15:43 有一次未提交改动只删掉了这两个定义、没补到前面（我恢复的），**该文件同时被多方编辑过，改前务必先读**。

**教训**：`vite build` 通过 ≠ 页面能跑。这类错误只在实际渲染时暴露，验证必须落到浏览器。

**验证方式（可复跑）**：`_tmp/probe-ui-verify.cjs`
—— 用本机 Chrome 无头 + CDP 打开真实页面（`admin` 本地账号登录、注入 `etaigong_token`），
捕获 `Runtime.exceptionThrown` / console / Log 错误，并读取真实 DOM 与 el-tooltip 弹层内容：
```bash
node _tmp/probe-ui-verify.cjs http://127.0.0.1:5173   # 源码(dev)
node _tmp/probe-ui-verify.cjs http://127.0.0.1:3456   # 构建产物
```
2026-09-14 实测（dev 与 3456 构建产物结果一致，**运行期错误 0 条**）：

| 检查项 | 结果 |
|------|------|
| 外卖视角·门店视角 载入（未选平台） | tabs = 经营总览 / 双向核对，无报错 |
| 选平台=美团外卖后 | tabs = 菜品销量 / 双向核对（**经营总览按预期隐藏**） |
| 门店经营指标表表头顺序 | 门店 → 优惠前总额（营业额）→ 营业收入（实收）→ 预估毛利 → 毛利占比 → 有效订单 → 客单价 → 曝光人数 → 入店人数 → 下单人数 → 入店转化率 → 下单转化率 ✅ |
| 悬停门店「预估毛利」 | 弹层：营业收入（实收）¥667.16 − 菜品成本合计 ¥19.00 = 预估毛利 ¥648.16 ✅（算式自洽） |
| 菜品销量表表头 | 排名 / 美团外卖商品 / 净销量 / 销售额 / 本地菜品 SKU / **成本** / 预估成本 / 预估毛利 ✅（成本列位置正确） |
| 悬停菜品「预估毛利」 | 弹层：分摊实收 ¥306.69 −（单份成本 ¥5.74 × 净销量 18 = ¥103.32）= 预估毛利 ¥203.37 ✅ |
| 总数据视角·门店视角 | tabs 含 经营总览/菜品销售分析，门店经营结果面板保留 ✅ |
| 团购视角·门店视角 | 经营总览保留（渠道营业构成仍在）✅ |

### 6. 「菜品销量」多规格关联行 + 成本列（本次追加）

**a) 多规格关联行（`spec_reverse`，如外卖多规格商品）**
- 「本地菜品 SKU」列此前是**空白**（这类商品没有单品绑定，`menu_name` 为空），现在显示
  **主产品名 · 多规格（N）**，例如「太公烧鹅 · 多规格（4）」，并用**紫色**（`#7a3fd4`）与普通单品绑定（绿色）区分。
- 悬停该单元格提示：按平台价反解到 N 个规格；并提醒「点击改为单品绑定会清除现有规格配置」（单品/规格互斥）。

**b) 成本列**
- 普通绑定：单份成本（原样）。
- 多规格：**并排列出各规格单份成本**，如 `¥42.5 / ¥42.5 / ¥80 / ¥160`（招牌太公烧鹅：上庄/下庄/半只/一只）；
  悬停出规格明细（规格名 · 平台价 → 单份成本）与「预估成本合计」。
  实现：行数据自带 `spec_links`（来自 `business_product_spec_links`），前端 `specCostText()` / `specBindingLabel()`。

**c) 外卖「菜品销量」移除「预估毛利」列**
- 原因（用户口径）：外卖后台无法导出商品实收，菜品实收只能按销售额分摊，据此算出的毛利不可靠。
- **门店经营指标表的「预估毛利」保留不动**（悬停构成提示也在）。
- 美团团购「菜品销量」**仍保留**该列：其收入基数来自团购收益明细的「商家应得」，是真实值。
  （未在本次浏览器验证中跑到：默认日期范围 9/6–9/13 没有团购数据；该列代码除新增 `v-if` 守卫外未改动。）

**验证**：`_tmp/probe-ui-verify.cjs`（跑 3456 构建产物，运行期错误 0 条）
- 表头：`排名 / 美团外卖商品 / 净销量 / 销售额 / 本地菜品 SKU / 成本 / 预估成本`（预估毛利已移除）✅
- 第 2 行（招牌太公烧鹅（不含米饭））：SKU =「太公烧鹅 · 多规格（4）」（计算色 `rgb(122,63,212)` 紫色）✅；成本 = `¥42.5 / ¥42.5 / ¥80 / ¥160`✅；预估成本 ¥250.00 ✅
- 悬停成本 → 规格明细弹层（上庄 ¥42.50 / 下庄 ¥42.50 / 半只 ¥80.00 / 一只 ¥160.00，合计 ¥250.00）✅
- 截图：`_tmp/shot-dish-cost-column.png`、`_tmp/shot-spec-cost-tooltip.png`

### 7. 「门店经营指标」表头与数值改成居中对齐（2026-09-14 追加）

**问题**：数值列是右对齐（表头也跟着右对齐），但表头文字比数值长很多（如「优惠前总额（营业额）」vs「¥1,669.70」），
表头左端会伸出到上一个列的位置，看起来"表头和下面的值没有对齐关系"。

**实测**（`_tmp/probe-align.cjs`，13 列逐列量文本中心与右缘）：

| 列 | 表头 | 值 | 中心差（改前） |
|---|---|---|---|
| 优惠前总额（营业额） | 宽 ~130px | ¥1,669.70 | **+45px** |
| 有效订单 | 宽 ~48px | 25 | **+29px** |
| 下单转化率 | — | 15.9% | **+25px** |

改前右缘是对齐的（差值 0），但中心点相差 11~53px —— 所以肉眼看就是"没对齐"。

**改法**：门店经营指标表（外卖变体 + 团购变体）的数字列统一改为
`align="center" header-align="center"`，即**表头与数值共用同一个中心点**；门店名（第一列）仍左对齐。

**实测（改后，3456 构建产物）**：第 1~12 列**中心差 = 0** ✅，门店列保持左对齐 ✅，运行期错误 0 条。
复跑：`node _tmp/probe-align.cjs http://127.0.0.1:3456 淘宝闪购`

> 其它表（菜品销量 / 菜品销售分析 / 门店经营结果 / 渠道构成）仍是右对齐，未改动；
> 若要一并改成居中，用 `_tmp/apply-center-align.cjs` 的同样方式扩大替换范围即可。

---

## 2026-09-14 筹建门店「门店渲染」：GLB 模型改为服务端缓存（分片上传 + 断点续传 + 本机缓存）

### 起因

「筹建门店 · 门店渲染」原来的模型是**只活在浏览器内存里**的：`el-upload :auto-upload="false"`
拿到 File 对象存在组件的 `modelFiles` ref 里，预览用 `URL.createObjectURL(file)`。
后果：刷新页面就没了、换台电脑看不到、同事看不到，页面上那句"文件仅在本次浏览器会话中使用"
就是这个意思。需求是**在服务里缓存 GLB 文件数据**。

### 三个必须先说清楚的约束（决定了实现方式）

1. **GLB 绝不能作为 BLOB 存进库**。本项目用 sql.js —— 内存态 SQLite，`db.save()` 每次都把
   **整个库**重新写盘一次（现 282MB）。GLB 动辄 100MB+，一旦入库，之后每写一行业务数据都要多写
   100MB+。沿用 `lib/mp/upload.js` 已定下的路线：**文件落盘、库里只存元数据**。
2. **既有的 base64-JSON 上传通道撑不住**。项目约定是 `express.json({ limit: '50mb' })` + base64，
   135MB 文件 base64 后约 180MB，直接超限；就算调大 limit，解析 180MB JSON 字符串也会把内存打爆。
   因此新开**原始二进制分片上传**通道。
3. **`data/uploads/` 当时不在 .gitignore 里**（只忽略了 `data/avatars/`）。模型放进去，
   下次 `git add -A` 就会把上百 MB 模型提交进**公开仓库**。本次已补规则。

### 磁盘布局与命名

```
data/uploads/store-models/<sha256>.glb        成品，按内容寻址
data/uploads/store-models/.tmp/<uploadId>/    分片上传中间态
    session.json                              会话元数据
    0.part 1.part ...                         分片，序号即下标
```

**为什么按内容哈希命名**：内容变了文件名就变，URL 永远不可能指向过期模型，因此可以放心
给浏览器一年期强缓存（`immutable`），从根上避免"换了模型但前端还在用旧模型"；
顺带实现天然去重——多个门店传同一个文件只占一份磁盘（实测 4 条库记录共用 1 个 135.4MB 文件）。

**已收到的分片以磁盘上的 `.part` 文件为准**，不从 `session.json` 读：进程中途重启、
JSON 写了一半，恢复时看到的仍是真实落盘状态。

### 数据库（`lib/db.js`）

新增 `store_models(id, store_id, file_name, stored_name, bytes, sha256, version, is_active,
uploaded_by, uploaded_by_name, created_at)`。同一门店换模型 = 插新版本 + 旧版 `is_active=0`，
**保留历史不覆盖**；删除是软删并自动回退到上一版。

### 接口（`server.js` + 新模块 `lib/store-models.js`）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/store-models` | 全部门店生效模型 + 磁盘/配额统计（列表页一次拿全，不发 25 个请求） |
| GET | `/api/stores/:id/model` | 单门店当前模型 + 历史版本 |
| POST | `/api/store-models/uploads` | 开上传会话（校验门店、大小上限 512MB、磁盘余量） |
| GET | `/api/store-models/uploads/:id` | 查已收到哪些片 → 断点续传依据 |
| PUT | `/api/store-models/uploads/:id/chunks/:index` | 上传一片（幂等，重传覆盖） |
| POST | `/api/store-models/uploads/:id/complete` | 合并 → 校验 → 落盘 → 写库（换版本） |
| DELETE | `/api/store-models/uploads/:id` | 放弃上传 |
| GET | `/api/store-models/:id/file` | 下载（JWT 鉴权，支持 Range 断点下载） |
| DELETE | `/api/store-models/:id` | 软删 + 自动回退上一版 |
| POST | `/api/store-models/maintenance/purge` | 清理孤儿文件与超 24h 的废弃中间态（默认 dry-run，仅管理员） |

设计要点：

- **下载走 JWT 鉴权接口，不挂在 `/uploads` 公开目录**：模型动辄 135MB，公开路径等于
  任何人拿到 URL 就能拖走。下载响应带 `ETag`（内容哈希）+ `Cache-Control: private, immutable`。
  代价是 `GLTFLoader` 拿不到带 Authorization 的地址，前端改为 **fetch → blob → createObjectURL**。
- **首片必须带 GLB 魔数**（`glTF` + version 2）：坏文件在第一步就被拒，不必等传完 135MB。
- 合并分片时**逐片读、逐片写、同步喂 sha256**，峰值内存只有一个分片（8MB），
  且每片之间 `await` 让出事件循环，不会卡住整个服务。
- 校验用的 GLB 头只读前 12 字节（用 `fsp.open` + `read`），不是把 135MB 读进内存。
- 分片大小 8MB（4~64MB 夹逼）：135MB = 17 片，单片失败只重传一片。

### 前端（`frontend/src/views/store3d/index.vue` + 新增 `frontend/src/utils/model-cache.js`）

- 模型信息改为读接口：列显示 文件名 / 大小 / 版本 / 上传时间 / 上传人 / `本机已缓存` 标记。
- 上传改成**隐藏 input + 分片上传进度条**（`第 n/17 片 · 每片 8MB，中断后可续传`）。
  断点记录（`upload_id`）存 localStorage，键含文件名/大小/修改时间，换文件自动失效；
  只有"会话失效"才清断点记录，网络类失败保留，下次点上传继续传。
- **本机缓存用 Cache Storage API**（不是 HTTP 缓存）：模型下载走鉴权接口，浏览器对 100MB+
  大响应会主动淘汰且无法查询/清理；Cache Storage 能查占用、能精确删。
  缓存键是 `<origin>/__model_cache__/<sha256>.glb`（合成地址，从不真的发请求），
  内容变了键就变，不需要写任何失效逻辑。
- 下载带进度（读 `response.body` 逐块累加），135MB 在慢网下不会像卡死。
- 缓存前先查 `navigator.storage.estimate()`，配额不够就只提示不硬塞；写入前申请 `persist()`。
- 页面头部显示 `本机已缓存 N 个 · X MB` + 「清除本机缓存」按钮。
- 删除模型时会判断该文件是否被别的门店共用（内容哈希去重），还有人用就不动本机缓存。

### 验证

后端 10/10 边界用例通过（`_tmp/sm-api-2.js` / `sm-api-3.js` / `sm-api-4.js`）：
135MB 真文件断在第 3 片 → 查缺 14 片 → 续传 → 合并 sha256 与源文件一致 → 中间态已清理 →
带 Range 断点下载 206 → 未登录 401 → 非 GLB 首片 400 → 越界分片 400 → 超 512MB 413 →
分片大小夹逼 → 软删回退 → 同文件跨门店去重（4 条记录 1 份文件）→ 废弃中间态清理且不动成品。

浏览器端到端（`_tmp/probe-store3d.js`）：
真实上传 135MB（17 个分片请求）→ 表格立刻出现模型 → 查看模型下载 1 次并写入 Cache Storage
（135.4MB/条）→ **刷新页面后再次查看：下载请求 0 次**，页面标注"读取本机缓存，未重复下载" →
three.js 渲染出 canvas（截图 `_tmp/shot-store3d-preview.png` 可见门店内部几何）。
构建产物（3456 服务的 `dist`）同样通过（`_tmp/probe-store3d-dist.js`）。

> 控制台仅剩 three.js 自身的弃用警告（`THREE.Clock`、`PCFSoftShadowMap`），
> 来自 `useThreeViewer.js`，**是既有代码、非本次引入**，未改动。

### 已知待办（未动，等用户决定）

1. `frontend/public/models/store-demo.glb`（135.39MB）**代码里没有任何引用**，但 vite build 会把它
   拷进 `frontend/dist`（前端 dist 因此 140MB）。现在模型有了正式存放位置，这个文件已冗余；
   而且它在生产上可通过 `http://134.175.41.247/models/store-demo.glb` **公开下载**（实测 HTTP 200）。
   建议从 `public/` 移除（dist 可回到 5MB 级，部署显著变快），但**未擅自删除**。
2. 模型文件**不随 sqlite 快照同步**，本地与云端各自持有。按用户要求本次**只改本地，未碰云端**。
3. `门店名称` 列带 `fixed="left"`、`#` 序号列不带，Element Plus 会把固定列画在最左，
   视觉上出现「门店名称 → #」的顺序（既有现象，非本次引入）。

---

## 2026-09-15 门店渲染优化：viewer 五项修正 + GLB 离线压缩管线 + 模型磁盘回收策略

### 一、先量后改（拿 135MB 真实模型量的数据）

| 指标 | 实测值 | 怎么量的 |
|---|---|---|
| 文件构成 | 135.4MB = **几何 111.3MB（82%）** + 贴图 23.1MB（47 张） | 解析 GLB 的 JSON chunk + bufferView |
| 规模 | 1639 mesh / **1639 次 draw call** / 1166 万顶点 / **410 万三角面** | 解析 accessor + 浏览器里 `three` 侧计数 |
| 世界尺寸 | **51.4 × 10.1 × 50.2** | 读 three 算出的 boundingBox |
| 生成器 | SimLab GLTF，用了 `KHR_materials_pbrSpecularGlossiness` | GLB JSON |
| 材质 | 130 个全带 `metallicFactor: 0.5`，场景**无环境贴图** | GLB JSON + viewer 源码 |

### 二、viewer 五项修正（`Store3DViewer/useThreeViewer.js`）

**① 阴影相机：原来是"代价照付、结果是错的"**
three 的 `DirectionalLightShadow` 默认是 `OrthographicCamera(-5,5,5,-5,0.5,500)` ——
光照方向上的正交视野只有 **10×10 单位**，而门店模型是 **51×50**。
即阴影只覆盖原点附近一小块，其余全被裁掉。
现在按**模型包围球半径**（0.5×√(51²+50²+10²)=36.2，×1.05）设置，光照位置也从写死的 (18,28,20)
改成按模型尺寸等比摆位（否则 5 米小模型过曝、100 米大模型过暗）。

> ⚠️ 修正一处我自己先前的错误结论：初测时我把 `gl.clear` 的次数当成帧数，
> 阴影 pass 和主 pass 各清一次屏 → 得出"阴影只覆盖 820/1639"的错误结论。
> 改成"只在绑定默认 framebuffer 时计帧"后真实数据是：
> **每帧 3313 次 draw call = 阴影 pass 1639（正好等于 mesh 数，全覆盖）+ 主 pass 1674**。
> 也就是说包围球那次调整**并没有**修掉一个"820"的问题（那个问题不存在），它是稳健性改进
> ——保证任何光照方向下都不会漏。

**阴影值不值那 1639 次/帧？实测：值。** 顶视图下开/关阴影逐像素对比：
差异 >30 的像素占 **19.67%**，模型区域内 **52.4%** 的像素被阴影压暗。

**② 材质发灰发暗 → 加环境贴图**
130 个材质都是 SimLab 给的 `metallicFactor = 0.5`，而 PBR 里金属度高的表面靠环境反射成像，
原场景只有半球光 + 两盏平行光、**没有 `scene.environment`** → 所有半金属表面渲染成死灰。
用 three 自带的 `RoomEnvironment` + `PMREMGenerator`（**零新增依赖、无需外部 HDR 文件**），
`scene.environmentIntensity = 0.85`。
实测无过曝：近纯白(>252) 像素占比改前改后都是 1.83%，模型像素占比 16.0% → 19.9%。

**③ 按需渲染**（原实现每帧无条件 render，静止也在 60fps 烧 GPU）
只有控件变化 / 尺寸变化 / 按键漫游 / 自动旋转才画；没人动就把 `requestAnimationFrame` 循环整个停掉。
配合 `visibilitychange`（切到后台）与 `IntersectionObserver`（滚出视口）暂停。
**验证方式**：页面加载前注入 WebGL draw call 计数器（不用 FPS —— headless 是 SwiftShader 软件渲染，
帧率数字无参考价值；draw call 次数与 GPU 无关）：

| 场景 | draw call |
|---|---|
| 静止 4 秒 | **0** ✅ |
| 拖动旋转 | 恢复（每帧 3313） |
| 松手后 | 阻尼收敛后回到 **0** ✅（软件渲染 7.2fps 下约 23 秒；真机 60fps 约 2.7 秒） |
| 标签页切到后台后即使拖动 | **0** ✅ |
| 恢复可见 | 自动恢复绘制 ✅ |

**④ 四个小债**
`THREE.Clock` → `Timer`（已弃用）；`PCFSoftShadowMap` → `PCFShadowMap`（已弃用，three 会打警告并回退）；
`loadModel` 里新建 GridHelper 时**旧网格没有 `scene.remove`**（原先只 dispose 不移除，被 `:key` 重挂掩盖了）；
错误浮层显示 `blob:` 地址对用户毫无意义 → 改显示模型文件名（`Store3DViewer` 新增可选 `modelName` prop）。
另外注册了 **three 自带的 meshopt 解码器**（28KB，随分包）：否则用 `--meshopt` 压出来的模型会加载失败，
且错误信息完全看不出原因。

**⑤ 顺手加了常驻诊断日志** `[Store3D] 模型规模 { meshes, triangles }` —— 排查"这个模型为什么卡"时这是第一手依据。

### 三、GLB 离线压缩管线（新增 `tools/gltf-optimizer/`）

**为什么是离线工具而不是塞进上传接口**：服务端只有 6 个依赖，压缩这套（sharp 原生库 + meshoptimizer wasm）
有 20 个包 23MB，而压缩是低频操作。依赖刻意装在工具自己的目录里，**没有碰根 `package.json`**
（`node_modules` 已被 `.gitignore` 的 `node_modules/` 规则覆盖，实测 `git check-ignore` 通过）。

```
node --max-old-space-size=4096 optimize.js 输入.glb 输出.glb [--lossless|--no-simplify|--meshopt|--keep-instancing|--ratio|--max-texture]
```

**四个变体的实测账（同一个 135.39MB 模型）**：

| 变体 | 文件 | draw call | 渲染三角面 | 需要改前端 |
|---|---|---|---|---|
| 原始 | 135.39MB | 1639 | 4,102,683 | — |
| **A2 保实例 + 贴图缩放 + 量化** | **36.91MB（3.7x）** | 1639 | 4,102,683 | **不需要** |
| A 无损不量化 | 53.20MB | 1639 | 4,102,683 | 不需要 |
| C2 flatten+join + 减面 + 量化 | 60.12MB | 118 | 1,226,028 | 不需要 |
| B2 flatten+join + 量化 | 117.54MB | 118 | 4,102,683 | 不需要 |

**关键结论：原模型的高效来自"多个节点复用同一网格"**（426 个网格被 1639 个节点引用）。
`flatten + join` 把每个实例的变换烘进几何来降 draw call，代价是文件里要**显式存下每一份实例几何**
（几何 111.35MB → 峰值 316.99MB → weld 后 141.78MB），所以 B2 比 A2 大 3.2 倍。
**draw call 应该在渲染侧解决（按需渲染），不该靠烘几何**。

**推荐 A3（= A2 但**不做 metalRough**）**：3.7x 体积、几何拓扑零损失（同 1639 mesh / 4,102,683 三角面）、
只新增 `KHR_mesh_quantization`（three 原生支持，**前端零改动**）、**材质完全不动** → 与原始渲染一致。

> ⚠️ **事故与更正（2026-09-15）**：上一条里我写"A2 已验证、与原始视觉一致"是**错的**。
> A2 跑了 `metalRough()`，它按 glTF 规范把 specular-glossiness 转成 metallic-roughness，
> 用 **`KHR_materials_ior = 1000`** 编码"彩色高光"——F0 = ((1000-1)/(1000+1))² = **0.996**，
> 是物理介质（ior 1.5 → F0 0.04）的 25 倍。实测 **117 个材质全部被写成 ior=1000**，
> 叠上本页第二节新加的环境贴图后，整个模型变成半镜面、惨白反光，**无法演示**。
> 我的旧验证方法也不成立：逐像素平均差被 84% 的相同背景像素稀释成了 7.9/765，
> 而且两张截图相机状态不一致。用户指出"惨白"后才定位到 ior=1000。
>
> **修正后的客观对比**（三张图同一默认取景、零交互、指标限定在模型区域内）：
>
> | 版本 | 平均亮度 | 近白(>240)占比 | 与原始逐像素差 |
> |---|---|---|---|
> | 原始 135MB | 219.1 | 13.9% | — |
> | A2（带 metalRough/ior=1000） | 224.9 | 15.1% | 23.7 / 765 |
> | **A3（材质未动）** | 219.5 | 14.5% | **4.8 / 765**（明显不同像素 2.39%） |
>
> 结论：`metalRough()` **默认不跑**（要跑用 `--metalrough` 显式打开并自行承担观感变化），
> 因为 three 0.185 完全不支持那个弃用的 SG 扩展，所以"保留原材质"就是"渲染和原来一模一样"。
> 附带说明：`KHR_materials_pbrSpecularGlossiness` 是 Khronos 已弃用扩展，three 忽略它、只走
> pbrMetallicRoughness；A3 保留它无害，输出文件里它和 `KHR_mesh_quantization` 并存。

### 四、模型磁盘回收策略（`lib/store-models.js`）

**原实现的 bug**：孤儿判定把"任何一行 `store_models`"都算作引用，**包括 `is_active=0` 的历史版本**
→ 换模型之后旧文件永远被自己的历史记录钉住，磁盘永远不释放。

**现在的口径**：每个门店保留生效版本 + 最近 (keepVersions-1) 个可回滚历史；
**门店一个生效版本都没有时，其历史全部视为过期**（否则一个已经不显示模型的门店会把历史文件永远占着
—— 这条是跑测试时踩出来的：清完记录磁盘仍压着 135MB，`freed_bytes` 只有 0.8KB）。
接口 `POST /api/store-models/maintenance/purge` 支持 `keep_versions` / `purge_history` / `dry_run`，
默认 dry-run 且仅管理员可用。

实测（用两个 816/820 字节的微型 GLB 做受控测试）：dry-run 只报告不删 ✅、
v1 的文件在 v2 生效后被回收 ✅、当前版本仍可 200 下载 ✅、
`keep_versions=2` 时零过期 / `=1` 时识别出 1 条 ✅。
清理我自己的测试残留：**172.30MB → 36.91MB**。

### 五、待用户决定的遗留

1. `frontend/public/models/store-demo.glb`（135.39MB）**代码零引用**，但 vite build 会拷进
   `frontend/dist`（前端 dist 因此 140MB），且在生产上可**公开下载**（实测 HTTP 200）。
   现在模型有正式存放位置了，这个文件已冗余 —— **仍未删除**。
2. 相机取景偏保守：`frameModel()` 按"最大边长"算距离，而门店模型常是 51×50×10 的扁平体，
   实测模型只占画布 16~20%。按投影后的宽高分别算能放大近一倍，未改。
3. draw call 数 3313/帧（阴影 1639 + 主 1674）是"阴影正确"的代价；若某些老旧机器上卡，
   可考虑减面（C2 变体：118 次 draw call）或关掉阴影。

---

## 2026-09-15 实际采购只数：由「按月」改为「按天」录入

### 起因

采购校准（MAPE）与「理论 vs 实际采购」对比的录入入口原本是**按月**（`poultry_purchases` 唯一键
`store_id + month + bird_id`，`month = YYYY-MM`）。用户反馈两件事：

1. **找不到录入入口** —— 其实入口一直在「理论用量 vs 实际采购」面板右上角「录入采购只数」按钮，
   但采购校准面板的提示语写的是「理论 vs 实际采购」，与真实面板标题「理论用量 vs 实际采购」对不上，
   且校准面板在上、录入面板在下，提示往下指、标题却对不上，造成困扰。
2. **需要按天录入**（实际采购是逐日发生的，按月汇总录入太粗）。

### 改动

**数据表（`lib/db.js`）**：`poultry_purchases.month` → `date`（YYYY-MM-DD），
唯一键 `store_id + date + bird_id`。迁移方式：检测到旧表含 `month` 列且无 `date` 列时直接 DROP 重建。
这么做的依据：旧表里仅有的历史数据是**禽类已被删除的孤儿行**（之前验证脚本事故残留的 12,000 只幽灵采购，
本地早已清、云上一直没清），没有可保留的真实按月数据，无需迁移"月份→某一天"。

**后端（`lib/poultry-accounting.js`）**：
- `savePurchase`：校验 `YYYY-MM-DD`，按 `(store, date, bird)` 幂等 upsert；
- `listPurchases`：支持 `date` / `date_from` / `date_to` 过滤；
- `purchaseComparison`：`WHERE date BETWEEN date_from AND date_to` 精确求和，
  不再有"非整月口径不对齐"问题，`is_whole_month` 恒为 true（保留字段避免破坏消费方）；
- `calibrationReport`：采购按天录入，但校准样本仍以「门店 × 月份 × 禽类」为单位，
  `GROUP BY store_id, substr(date,1,7), bird_id` 把每日 SUM 起来，再与当月理论量比。

**前端（`MenuAccounting.vue`）**：录入弹窗月份选择器 → 日期选择器（默认当前核算区间首日）；
口径文案"每店每月每禽类一条"→"每店每天每禽类一条"；提示语「理论 vs 实际采购」→「理论用量 vs 实际采购」
（与面板标题对齐）；去掉"非整月口径不对齐"的提示。

### 验证

- 本地 10/10：录 3 天 × 10 只 → 列表 3 条按天；对比按区间精确求和 = 30；校准把 3 天汇总成 8 月一条样本 = 30；
  同天重复保存是覆盖不是新增；拒绝 `YYYY-MM` 旧格式。
- 上云后：`poultry_purchases` 已是 `date` 列、0 行（15 条孤儿自然清掉）；stores/dish_sales/menu_items/
  员工/流水/家禽档案行数全部不变；生产核算页加载无报错、录入弹窗为「日期」选择器。

---

## 2026-09-15 口径更正：「采购只数」→「实际消耗只数」（采购退出计算）

### 起因（用户指出的口径错误）

上一轮把录入改成按天，但录的仍是**采购（进货）量**。用户指出：采购量 ≠ 消耗量，中间隔着库存：

```
实际消耗 = 期初剩余 + 期间采购 − 期末剩余
```

例：上月剩 4 只，9.1 采 30、9.4 又采 30（共 60 只），而这 4 天实际只吃掉 6+10+8+7=31 只。
拿「采购 60」去和「理论消耗 31」比，差出来的 29 只**是库存进出、不是模型误差**，用它校准系数毫无意义。
结论：**参与「理论 vs 实际」对比和 MAPE 校准的必须是门店实际消耗只数；采购量只能作参考，不进计算**。

### 改动（概念级改名，全链路）

**数据表**：`poultry_purchases` → `poultry_consumption`
- `date`（YYYY-MM-DD，按天）+ `quantity`（**实际消耗只数**）+ `remark`，唯一键 `(store_id, date, bird_id)`
- **去掉 `amount`**：采购金额对消耗无意义；消耗成本可由 `数量 × poultry_birds.unit_cost` 推导，不必落库
- 旧表 `poultry_purchases` 直接 `DROP TABLE IF EXISTS`（其仅有数据是 15 行孤儿残留）

**后端 `lib/poultry-accounting.js`**：`listPurchases/savePurchase/deletePurchase` → `listConsumption/saveConsumption/deleteConsumption`；
`purchaseComparison` → `consumptionComparison`（返回字段 `purchased_birds`/`has_purchase`/`purchased_amount`
→ `consumed_birds`/`has_consumption`，totals 去掉 `purchased_amount`）；`purgeOrphanPurchases` → `purgeOrphanConsumption`；
`listBirds.purchase_count` → `consumption_count`；`deleteBird` 回收该品种消耗行（返回 `purged_consumption`）；
`calibrationReport` 查询与文档同步改名（仍按「门店×月×禽类」聚合每日 SUM）。

**接口**：`/api/poultry/purchases*` → `/api/poultry/consumption*`；
`/api/poultry/accounting/purchase-comparison` → `/api/poultry/accounting/consumption-comparison`；
`/api/poultry/calibration` 路径不变（内容已是"消耗校准"）。

**前端 `MenuAccounting.vue`**：面板「理论用量 vs 实际采购」→「理论用量 vs 实际消耗」；
「采购校准」→「消耗校准」；列「实际采购」→「实际消耗」，**删掉「采购金额」列与合计项**；
录入弹窗标题/字段/按钮全部改为「实际消耗只数」，去掉采购金额输入；表单类名 `purchase-form` → `consumption-form`。

**文档**：`docs/poultry-accounting-handover.md` 的表结构、接口清单、页面结构三处同步更新，
并写明「采购不进计算」的口径与原因（防以后被改回去）。

### 验证（本地 16/16）

录 6+10+8 只/3 天 → 列表 3 条按天、无 `amount` 字段 → 对比「实际消耗」= 24；
totals 无 `purchased_*` 残留；校准把 3 天汇总成 8 月一条 = 24；同天重复保存=覆盖；
拒绝 `YYYY-MM`、拒绝负数；**旧接口 `/api/poultry/purchases` 返回 404**（确认下线）；删除清理干净。
库结构确认：`poultry_consumption` 9 列、旧表 `poultry_purchases` 已不存在。
前端产物实测已无「采购」字样、命中「实际消耗/消耗校准」新文案。

---

## 2026-09-15 滞后区绑定：从「一键确认候选第一名」改为「看清后自己选」

### 起因（用户指出的"强行绑定"风险）

用户看到滞后区（未绑定品项）里 `明炉烧鸭饭+例汤（1人份） → 疑似 招牌烧鸭（约100g）65%` 这类建议后
提出：**如果它猜的不是我要的菜，我还能改吗？** 排查后发现三个问题：

1. **只认候选第一名**：`bindUnboundItem` 直接取 `recommend_candidates[0]`，弹窗只有"确认绑定 / 取消"，
   **没有改选入口**，想换只能先取消、再自己跳到「堂食菜品绑定」搜。
2. **低相似度也预选**：65% 这种明显错的（一份饭 vs 100g 烧鸭部位）照样当"疑似"显示，
   确认框只报一个百分比，很容易顺手点错 —— 而绑错会把这批销量并进错误菜品，**直接影响成本与毛利**。
3. **有一批品项在绑定页搜不到**：绑定页数据源只查 `dish_sales`（收银机），
   云上实测 **114 个 (名称×规格) 组合 / 18,871 份（占 POS 销量 13.41%）只在 POS 里**，
   大多是带 `*`/`.` 噪声的名字（`*叉鸭双拼饭.` 3,540 份、`招牌烧鹅饭.` 1,416 份…）。
   （本轮**未修**这条，用户选择先只做下面两处。）

### 改动（纯前端 `frontend/src/views/analysis/BusinessAnalytics.vue`）

**① 低相似度不再预选、不再叫「疑似」**：新增 `BIND_TRUST_SCORE = 0.8` 与 `trustedCandidate(row)`。
候选第一名 < 80% 时，单元格显示「不够可靠（最高 77%），需手动选」，按钮变「手动选择」，
打开弹窗后**不预选任何菜品**、`确认绑定` 置灰，必须先自己选（杜绝"顺手确认"）。

**② 「去绑定」改成完整的可选弹窗**（替换原来的 `ElMessageBox.confirm`）：
- 先摊开品项信息：平台/收银机菜名 · 规格 · 来源平台 · 销量/销售额
- 候选 chips（点一下即选中），也可用**可搜索下拉**（复用 `localMenuSkuOptions`，含全部在售菜品）
- 高相似度时提示"已按相似度预选 X（86%），确认前请核对，也可换成别的菜品"
- 页脚写明"将写入 N 条绑定关系（不同平台/编码各自一条）"
- 未选菜品时确认按钮禁用

### 验证（浏览器实测 13/13）

切「自定义时间 2026-09-01~09-10」+「菜品销售分析」tab，滞后区 60 行里 24 行是低相似度：
低相似度行文案/按钮正确 ✅；高相似度行仍显示候选+百分比 ✅；
弹窗标题/4 项品项信息/低相似度警示/3 个候选 chip/153 个可选项 ✅；
手动模式未选时「确认绑定」禁用 ✅，选中后变可用 ✅；
确认后**该品项真的出现在「已绑定」里（命中 4 条 → 招牌烧鹅饭）** ✅；
测试绑定已全部解绑清理 ✅；全程无 JS 报错 ✅。

> 注：`/api/dish-sales/mappings` 返回的 `total` 是 (编码,名称,规格) **分组数**，绑定/解绑不会改变它 ——
> 断言"绑定数增加"是错的，正确断言是查该品项是否出现在 `mapped=bound` 结果里。

## 2026-09-21 补录合同起止时间 + 新增「合同到期提醒」（与转正提醒同一套规则引擎）

### 一、需求

1. 按《门店员工管理》表格（`门店员工管理(1).xlsx`）把**合同起始日 / 合同到期日**补录进人事档案；
2. 参照已有「转正提醒」，**copy 一个「合同到期提醒」**。

### 二、补录（数据变更，已执行）

来源表只有 3 列：`名字 / 合同起始日 / 合同到期日`，65 行数据（表头在第 2 行）。

**先勘察了单元格类型再动手**：B/C 两列是 **Excel 日期序列号**（真日期，如 46082 = 2026-03-01），
不是文本。列显示格式不同（B 列 `3/1/26`、C 列 `2027/3/1`）只是单元格格式差异，底层值一致 ——
因此统一按序列号解析，不依赖显示文本。（若按显示文本猜 `M/D/YY` 还是 `D/M/YY` 会录错日期。）

结果：

| 项 | 数量 |
|---|---|
| 表格数据行 | 65 |
| **已写入** | **38** |
| 无日期（表格里就是空的，跳过） | 27 |
| 未匹配到员工 | 0 |
| 重名无法判定 | 0 |
| 到期日早于起始日 | 0 |

补录前人事库里「合同起始/截止」**全部为空**（96 名员工无一人填过），所以本次是纯新增，未覆盖任何既有值。

**姓名列脏数据已处理**：表格里有 3 行把门店/岗位/手机号写进了姓名列 ——
`李三兰(京基御景店长-[手机号])`、`方洲店-韦泽月-[手机号]`、`欧景店-谢开铭[手机号]`。
工具做保守清洗（剥括号 → 去尾随数字 → 取纯中文且不含机构字「店/部/组/司…」的片段），
清洗后才命中的**单独归类并打印待复核**，绝不静默猜。
> 踩坑记录：清洗规则的机构字过滤是必需的 —— 否则 `方洲店-韦泽月-…` 会先命中「方洲店」。
> 另注后两人在表格里没有日期，本次无数据可录。

### 三、新增工具 `tools/import-contract-dates.js`

```powershell
node tools/import-contract-dates.js --dry-run                # 干跑（默认，只读）
node tools/import-contract-dates.js --apply                  # 写库（需先停服）
node tools/import-contract-dates.js --apply --overwrite      # 覆盖已有值
node tools/import-contract-dates.js --file "D:\x.xlsx"       # 换来源
```

设计要点：

- **默认干跑**，加 `--apply` 才写库；**默认只填空值**，加 `--overwrite` 才覆盖。
- 匹配规则与 `lib/staff-import.js` 一致：按姓名精确匹配 → 命中多条时若只有 1 人在职则取该人 → 仍不唯一则跳过并报告（不猜）。
- 报告落 `_tmp/contract-import-report.json`，含每人的 before/after。
- **⚠️ 关键约束：`--apply` 会先探测 3456 端口，服务在运行时直接拒绝写库。**
  原因：`lib/db.js` 用 sql.js（整库在内存、`save()` 全量重写文件），
  运行中的服务会在下一次 `save()` 时**覆盖掉外部脚本的写入**。
  所以正确姿势是：**停服 → 备份 → `--apply` → 重启**。

本次执行记录（2026-09-21 14:34）：停服（PID 按端口定位）→ 备份
`data/database.sqlite.bak-before-contract-dates-20260921143407` → `--apply` 写入 38 人 → 重启后端 → 接口复验通过。

> 为什么没走 `PUT /api/staff/:id`：该接口每次调用都会**异步同步一次企微智能表格**
> （`config.json` 已配 `staffSmartSheetDocId`，是生产表）。批量补录 38 人会触发 38 次外部写入，
> 而合同字段并不在同步字段清单里 —— 属于纯粹的多余副作用，故改用直连库 + 停服。

### 四、合同到期提醒（`lib/notifications.js`）

**做成了「规则表」而不是复制粘贴第二段代码。** 原来只有转正一条规则写死在 `runChecks` 里，
新增 `RULE_DEFS` 描述表（`dateField / dateLabel / defaultLeadDays / previewTitle / buildTitle / buildContent`），
`runChecks` 与 `previewUpcoming` 改为**遍历它**。因此：

- 两类提醒共用同一套「惰性生成 + `dedupe_key` 幂等 + 提前天数窗口」逻辑；
- 转正提醒的行为**逐字未变**（默认 15 天、文案一致、`payload` 只是补了 `date_field` / `milestone_date`）；
- 将来加第三类提醒（如健康证到期）只需在 `RULE_DEFS` 加一条。

新增内容：

| 项 | 值 |
|---|---|
| 类型码 | `contract_expiring` |
| 日期列 | `employees.contract_end_date` |
| 默认提前天数 | **30 天**（转正仍是 15 天，各自的默认值分开定义） |
| 幂等键 | `contract_expiring:{employee_id}:{contract_end_date}` |
| 标题 | `X 的劳动合同将在 N 天后到期（YYYY-MM-DD）` / 当日为 `X 的劳动合同今日到期` |
| 正文 | `门店 · 岗位｜合同期限 起 至 止。请提前启动续签评估与手续办理。` |
| 默认接收人 | **空**（避免凭空给谁发通知；需在规则页勾选） |

接口侧 `server.js`：把规则响应体抽成 `notificationSettingsPayload()`（GET/PUT 共用），
每条规则带上 `label / description / date_label / default_lead_days / preview_title`，
并新增 `upcoming_by_type`（按类型分组的「近期节点」）。旧的 `upcoming` 字段**保留**，不破坏历史前端。

### 五、顺带修掉的既有缺陷（⚠️ 重要）

**`NotificationRulesView.vue` 的「接收人员」选择框一直存不下去。**
模板绑定的是 `rule.recipient_user_ids`，而后端存/取的都是 `user_ids` —— 两边字段名不一致：

- 打开页面时选择框**显示为空**（尽管 `config.json` 里 `probation_due.user_ids = [1]` 是配好的）；
- 选了人再保存，`user_ids` 还是旧值，**选的接收人被静默丢弃**。

本次改为绑定 `rule.user_ids`，并把规则页改成渲染 `rule.label / description / preview_title`
与按类型分组的节点面板。**建议重新保存一次通知规则确认生效。**

### 六、验证

| 项 | 结果 |
|---|---|
| 语法自检（`node --check`） | `lib/notifications.js` / `server.js` / `tools/import-contract-dates.js` 全部 OK |
| 补录后接口复验 | `GET /api/staff?page_size=100` → 96 人中 **38 人合同起止已填**，抽查李三兰/陈云生/廖汉光一致 ✅ |
| 规则接口 | 返回 2 条规则，`contract_expiring` 的 `date_label=合同到期日`、`lead_days=30` ✅ |
| 近期节点 | `upcoming_by_type.contract_expiring` 正确列出 8 人；转正 1 人 ✅ |
| 生成提醒 | `POST /api/notifications/check` → 生成 **1 条**：`刘珍林 的劳动合同将在 22 天后到期（2026-10-13）` ✅（2026-09-21 + 22 天 = 10-13 正确） |
| 幂等性 | 再跑一次 `check` → **0 条**，未重复 ✅ |
| 列表并存 | `pending` 中 `contract_expiring` 与 `probation_due` 各自出现 ✅ |
| 前端构建 | `frontend/dist` 重新构建成功；产物 `NotificationRulesView-Bw5UMinp.js` 已由 3456 正常托管（HTTP 200）✅ |

> 构建踩坑：首次 `npm run build` 在 `transforming...` 阶段 V8 崩溃（堆内存不足）。
> 加 `$env:NODE_OPTIONS='--max-old-space-size=6144'` 后通过。**以后构建前端记得带上这个环境变量。**
> 另注：用 `npm run build 2>&1 | Select-Object -First N` 会因 PowerShell 提前关闭管道而显示 `EXIT=-1`，是假象，别据此判断失败。

### 七、遗留待办（未动，等确认）

1. **已过期的合同不会提醒。** 规则与转正保持同一语义（只扫未来窗口 `date >= 今天`），
   所以 `陈云生`（`contract_end_date = 2026-09-01`，已过期）**不会**产生提醒。
   若需要「逾期未续签」曝光，应另做一类提醒（口径也不同：是逾期天数而非提前天数），建议单独排期。
2. **27 人表格里没有合同日期**，人事档案仍为空（`张洪芬、曾静怡、王恩明…`）。需要业务补表后再跑一次同样的命令。
3. **接收人默认空**：`合同到期提醒` 已在本地配成接收人 `[1]`（与转正提醒同一人），
   但这是本次为验证而设；上线前请在「系统管理 → 通知规则」按实际责任人调整。
4. **合同时间不在敏感字段清单里**（`STAFF_SENSITIVE_FIELDS`），改动不留生命线事件，也不需要填调整原因。
   若人事希望合同变更也留痕，需要把它加进该清单（会连带要求上传合同附件，属流程变更，需业务确认）。

## 2026-09-22 「合同到期提醒 + 合同起止补录」部署上云（外科式补丁，未整体覆盖）

### ⚠️ 一、部署前勘察发现的重大风险：本地与云端已**双向分叉**

按文档 §一「部署/更新步骤」本应 `scp` 覆盖 `server.js` 与整个 `lib/`。**本次没有这么做**，因为逐文件比对发现两边的差异不是单向的：

| 方向 | 内容 |
|---|---|
| **本地独有**（未上云） | `server.js` 里约 **200 行「钉钉考勤自动同步」**；`lib/db.js`、`lib/permissions.js`、`lib/poultry-accounting.js`、`lib/sync-job-audit.js` 均有本地改动 |
| **云端独有**（本地没有） | **`lib/schedule-wiring.js` / `schedule-core.js` / `schedule-routes.js` / `schedule-store.js` / `daily-report-push.js`（合计 1,073 行）**，且云端 `server.js` 里有 `require('./lib/schedule-wiring')`，**本地 server.js 没有这行** |

**结论：整体覆盖本地 `server.js` 会删掉云端的调度接线**（`require('./lib/schedule-wiring')`），
那 1,073 行云端独有代码会变成死代码，「自动同步与日报计划」子系统直接失效。
这正是文档开头那句「避免被旧版本覆盖丢失功能」和 2026-09-14「不能盲目整库覆盖」的同类陷阱 ——
只是这次发生在**文件级**而不是库级。

### 二、实际做法：外科式补丁，只发布本次功能

| 产物 | 处理方式 | 依据 |
|---|---|---|
| `lib/notifications.js` | **整体上传** | 逐 hunk 比对确认：本地相对云端的差异**100% 等于本次改动**（新增 `RULE_DEFS` / `contract_expiring` / `previewUpcomingAll` 等），无夹带 |
| `server.js` | **只把本次的 19 行改动打到「云端版本」上** | 以云端 `server.js` 为底本，用 `_tmp/patch-cloud-server.js` 精确替换通知设置那一段；脚本内置**强制断言**：必须保留 `require('./lib/schedule-wiring')`、必须不含 `runDingTalkAttendanceSync`，命中次数不为 1 即中止 |
| `frontend/dist` | **整体上传**（tar → `dist.new` → 原子替换） | 比对 chunk 种类：云端 32 / 本地 32，**无云端独有页面、无本地独有页面** ⇒ 前端是干净的版本演进，无丢失风险 |
| `tools/import-contract-dates.js` | 一并上传（新增文件，additive） | 便于云端复跑 |
| `lib/db.js` 等 4 个有差异的文件 | **一律未动** | 与本次功能无关，方向不明，不引入不确定性 |

补丁自检结果：差异**仅 19 增 / 4 删，全部落在通知设置区**（对照：本地 server.js 相对云端是 196 增 / 61 删）。

### 三、执行记录（2026-09-22 10:08–10:11 CST，停机约 70 秒）

前置：SSH 端口 22 曾因**限流/fail2ban 临时封禁**（`kex_exchange_identification: Connection closed by remote host`，
`_syncbot/schedule/K-REPORT.md` §6.2 已记录同一现象，09-21 17:21 起持续复现）。本次重试时**已自行解封**。

```powershell
# 备份（4 项，DB 已做 sha256 校验一致）
/home/ubuntu/app/_bak/database.sqlite.bak-before-contract-20260922100819   # 386,437,120 B, sha256 eab6fec22d3596c4
/home/ubuntu/app/_bak/server.js.bak-before-contract-20260922100819
/home/ubuntu/app/_bak/notifications.js.bak-before-contract-20260922100819
/home/ubuntu/app/_bak/dist.bak-before-contract-20260922100819.tgz
# 顺序：停服 → 安装代码 → node --check → 断言 → 原子替换 dist → 补录数据 → 启服
```

**关键断言实测（停机窗口内）：**

| 检查项 | 结果 |
|---|---|
| `require('./lib/schedule-wiring')` 出现次数 | **1**（调度接线保住）✅ |
| `runDingTalkAttendanceSync` 残留 | **0**（未夹带本地钉钉改动）✅ |
| `notificationSettingsPayload` 出现次数 | 3 ✅ |
| `node --check server.js` / `lib/notifications.js` | 均通过 ✅ |
| server.js 所需 lib 依赖齐全性 | 全部就位（无 MISSING）✅ |
| 上传件 sha256 与本地对照 | server.js `32bd4a11dc50e899` / notifications.js `3a25acb39ed38ad8` 一致 ✅ |

**合同起止补录（云端库）：** 干跑与本地结果**完全一致**（65 行 → 38 可写入 / 27 无日期 / 0 未匹配 / 0 重名 / 0 区间异常），
随后 `--apply` 写入 **38 人**（写库前确认 3456 未监听，工具的服务运行保护已生效）。
抽查（注意**云端员工 ID 与本地不同**，靠姓名匹配落位正确）：
`刘珍林 #84 2026-04-13~2026-10-13`、`李三兰 #125 2026-07-13~2028-07-13`、`陈云生 #120 2026-07-01~2026-09-01`、`廖汉光 #106 2026-06-30~2028-07-01`。

**部署后线上验证：**

| 项 | 结果 |
|---|---|
| `systemctl is-active zhongkong` | active，3456 监听 ✅ |
| `GET /` | 200 ✅ |
| `GET /api/mp/health` | 200 ✅ |
| 公网 `http://134.175.41.247/` | 200，index.html 引用 `NotificationRulesView-Bw5UMinp.js` ✅ |
| 该 chunk 经公网取得 | 4308 B，**sha256 `3a1aa64266c2f769` 与本地逐字节一致**，含「合同到期日」「upcoming_by_type」✅ |
| 云端只读规则验证（sql.js + 最小 queryAll 适配器，不改库） | `RULE_DEFS` = `probation_due, contract_expiring`；转正节点 1 条、**合同节点 8 条** ✅ |
| 云端干跑的合同提醒文案 | `刘珍林 的劳动合同将在 21 天后到期（2026-10-13）`，幂等键 `contract_expiring:84:2026-10-13` ✅ |

> 验证方法上的两个坑（避免下次误判）：
> ① 用 PowerShell `Get-Content -Raw` + `-match '中文'` 搜 UTF-8 产物会**假阴性**（控制台按 GBK 解码），
> 需用 `[System.IO.File]::ReadAllBytes` + `Encoding::UTF8.GetString`，或直接用 `curl` 取回后比 sha256；
> ② PowerShell here-string 传 SSH 会给每行**附 `\r`**，导致 `head -1`/`tail -1`/`journalctl --no-pager` 等
> 报出「invalid trailing option」这类假错误。**远程脚本改为本地写好再 `scp` 执行**可彻底规避。

### 四、⚠️ 遗留：云端提醒**不会自动生成**，需要先配接收人

实测云端 `config.json`：`notifications.rules = { probation_due: { enabled:true, lead_days:30, user_ids: [] } }`
—— **接收人是空的**，且 `contract_expiring` 尚未落进配置（走默认值，同样是空接收人）。

**后果：两类提醒在云端都不会产生任何通知**（`runChecks` 在 `user_ids.length === 0` 时直接跳过）。
这恰好印证了本次修掉的那个前端缺陷（接收人绑定 `recipient_user_ids` → 永远存不下去），
云端那 3 条通知也全是 `manual`、无一条规则生成。

**待办（需业务侧在界面上做，一条即可）**：登录云端 →「系统管理 → 通知规则」→ 为
「转正提醒」「合同到期提醒」各选接收人并保存。保存后点「立即检查」应能立刻看到
`刘珍林` 那条合同提醒。

### 五、本次未做（明确留给后续）

1. **钉钉考勤自动同步**（本地 `server.js` 约 200 行）**未上云**。它需要与 `lib/db.js` 的钉钉表结构配套，
   建议作为**独立一次发布**，并同样按「外科/成套」方式处理，不要与本次混在一起。
2. **云端独有的 schedule 子系统（1,073 行）尚未回流本地**。建议尽快 `scp` 拉回本地并入仓库，
   否则每次部署都要重复本次的手工比对，且随时可能被整体覆盖。**这是当前最大的流程隐患。**
3. `lib/db.js` / `lib/permissions.js` / `lib/poultry-accounting.js` / `lib/sync-job-audit.js` 的本地改动
   未上云（与本次功能无关，方向未核对）。下次发布前建议先补齐与云端的逐文件比对。
4. 云端 `/tmp/zkstage/` 留有本次暂存件，`/home/ubuntu/app/frontend/dist.old-contract` 为回滚用的旧 dist；
   确认稳定后可清理（`_bak/` 下的 4 份备份建议保留一段时间）。

### 六、既有告警澄清（非本次引入）

启动日志有 `[schedule] schedule.schema_not_ready {"error":"Cannot read properties of null (reading 'run')"}`。
已核实为**既有现象**：历史 3 次启动（pid 978505 09-21 16:00、pid 979485 09-21 16:08、本次 1014781）**每次都出现**，
且云端代码注释写明「挂载失败不影响其余功能（try/catch 兜底）」、`default_disabled: true`。
本次补丁未触碰该段（`schedule-wiring` require 计数仍为 1）。

## 2026-09-22（同日续）钉钉考勤自动抓取上云 + 一次「装了不启动」事故与修复

### 一、需求澄清（我先前理解错了，记录以免再犯）

用户原话：**「合同时间数据需要上传，打卡记录不用」**
—— 我最初理解为「钉钉考勤整块都不用上」，**这是错的**。正确意思是：

| 项 | 是否上云 |
|---|---|
| 合同起止**数据** | ✅ 要（已于同日首次部署完成，38 人） |
| 打卡记录**数据**（历史行） | ❌ 不用上传（云端本来就有，靠钉钉拉取） |
| **自动抓取打卡 / 抓取钉钉考勤的「功能」** | ✅ **必须上** —— 该数据关联**工资条制作**与**月数据人效看板** |

故追加第二次部署：只上钉钉考勤自动抓取功能，仍不动任何数据文件。

### 二、部署范围判定（`lib/db.js` 不需要动）

先做了三项预检，确认只需改 `server.js` 一个文件：

1. **`lib/db.js` 差异只有一处**：云端比本地**多 3 张 schedule 表**（`schedule_config` / `schedule_config_audit` / `schedule_job_runs`）
   —— 即云端独有子系统的建表语句，**与钉钉表无关**。⇒ 不动 `lib/db.js`。
2. **钉钉表结构满足自动同步要求**：云端 `dingtalk_attendance_records` 已存在
   `UNIQUE(dingtalk_user_id, check_time, check_type)`，而自动同步用的是 `ON CONFLICT(...)` upsert ⇒ 约束齐备、列齐备。
3. **`lib/dingtalk-attendance.js` 两侧哈希完全一致**（`c7844de3f3c65129`）⇒ 无需上传。

### 三、⚠️ 事故：第一次部署「代码装上了，但永不启动」

第一次补丁用「行号区间（base 行 2200–2300）」筛选 hunk，把**文件末尾的启动调用漏掉了**：

```js
// server.js 末尾 app.listen 回调里（base 行 6377 那个 hunk）
console.log(`📦 数据库: data/database.sqlite`);
+ // 每日自动同步钉钉考勤（默认 01:00 同步上一天，可在 config.json 的 dingtalkAttendance 调整）
+ startAttendanceAutoSync();
```

结果：函数定义在、路由在、但**没有任何地方调用它** ⇒ 定时器不启动、日志无输出、自动同步永不发生。
**发现方式**：部署脚本里 `grep -c startAttendanceAutoSync` 返回 **1**（应为其定义+调用 = 2），我据此判定异常。

修复：把筛选从「行号区间」改为**显式白名单** `[2223, 2268, 6377]`，并新增两道护栏：
- **安全护栏**：保留的 hunk 不得包含 `schedule-wiring` / `/api/bot/status` / `saveConsumptionBatch`，否则直接中止；
- **漏检护栏**：断言 `startAttendanceAutoSync();` 的调用 hunk 必须被覆盖，否则中止（专防本次这类漏检）。

> 同类坑：hunk 解析时用 `split('\n')` 得到的**末尾空串**会被误当成上下文行，导致行数自检失败。
> 已改为只接受 `' '` / `'+'` / `'-'` / `'\'` 前缀的行。

### 四、三个必须排除的 hunk（其中两个是重要发现）

| base 行 | 内容 | 结论 |
|---|---|---|
| 79 | 本地把 **`/api/bot/status` 重新加入免鉴权名单**（线上版明确写着「**不再**免鉴权」，要求带 JWT + `enterprise-settings.manage`） | ❌ 不上。**见下方「遗留风险」** |
| 3306 | `poultryAccounting.saveConsumptionBatch`（禽类消耗批量录入） | ❌ 非本次范围 |
| 5996 | 本地**删除了** `require('./lib/schedule-wiring').mountSchedule({...})` 整块 | ❌ **绝不能上** —— 再次印证整体覆盖会毁掉云端调度子系统 |

### 五、执行与验证（10:32:29 起，停机约 12 秒）

补丁结果：相对线上版 **恰好 5 个 hunk**，全部落在钉钉区段（2225–2274）与文件末尾启动调用（6379）。

| 断言 | 结果 |
|---|---|
| `startAttendanceAutoSync` 出现 **2** 次（定义 + 调用） | ✅（第一次部署此处为 1，即事故） |
| `runDingTalkAttendanceSync` 4 处 / `dingtalk/auto-sync` 2 处 / `setInterval` 1 处 | ✅ |
| `require('./lib/schedule-wiring')` = **1** | ✅ 云端调度子系统完好 |
| `notificationSettingsPayload` = 3 | ✅ 上次通知改动完好 |
| `req.path === '/api/bot/status'` = **0** | ✅ 未带上本地的安全回退 |
| `saveConsumptionBatch` 未出现 | ✅ 未夹带无关改动 |
| `node --check` + lib 依赖齐全 | ✅ |
| server.js sha256 与本地一致 | `8adc14ac8243b1b0` ✅ |
| 三个钉钉路由未认证返回 | **401**（存在且受保护，非 404）✅ |

### 六、✅ 自动回扫实测成功，并补上了薪资要用的数据缺口

云端钉钉凭据本就配好（`DINGTALK_APP_KEY/SECRET/CORP_ID` 经 systemd drop-in 注入，实测三个都已进进程环境；
`ATTENDANCE_AUTO_*` 未设 ⇒ 走默认 **启用 / 每天 01:00 / 回扫 30 天**）。

启动约 8 秒后触发补跑，约 7 分钟完成：

```
批次 #4  2026-08-23 ~ 2026-09-21  success  记录 4102  已匹配 4102   @2026-09-22 10:31:12
        已同步 63 名员工的 4102 条打卡记录
```

| 指标 | 部署前 | 部署后 |
|---|---|---|
| 打卡记录总数 | 2,611 | **4,102** |
| 最新工作日期 | 2026-09-18 | **2026-09-21** |
| 覆盖区间 | 09-01 ~ 09-18 | **08-23 ~ 09-21** |

**关键收益：09-19 ~ 09-21 这段原本缺失的打卡数据已补齐**（正是工资条与月人效看板所需的区间）：

```
2026-09-18  144 条 / 46 人
2026-09-19  167 条 / 53 人
2026-09-20  155 条 / 50 人
2026-09-21  130 条 / 43 人
```

> 幂等性已验证：批次表里没有重复行；新进程启动时 `checkAttendanceAutoSync` 会因
> 「上次成功批次已覆盖窗口」直接返回 `already_synced`，不会重复拉取。

### 七、⚠️ 遗留风险：本地 `server.js` 把 `/api/bot/status` 改回了免鉴权

本地相对线上多出这一改动（**不建议上生产，需业务/开发确认**）：

```js
// 线上（当前生产）—— 要求带 JWT 并接受 enterprise-settings.manage 校验
if (req.path === '/api/auth/login' || req.path === '/api/geo/bound') return next();
// 本地 —— 把 /api/bot/status 又放回免鉴权名单
if (req.path === '/api/auth/login' || req.path === '/api/bot/status' || req.path === '/api/geo/bound') return next();
```

而本地 `lib/permissions.js` 里**仍然保留着** `['/api/bot', 'enterprise-settings.manage']` 规则 ——
即本地这份规则因为路径被整体放行而**永远不会生效**（死规则）。

**这属于「云端已收紧、本地未回流」的又一处分叉**，与 §一、§四 的 schedule 代码同源。
建议：把云端 `server.js` 里这段鉴权逻辑回流本地，让本地与生产一致。

### 八、操作过程记录：SSH 限流再次触发

本次 SSH 连接较密集（多次 scp/ssh），过程中**再次触发 sshd 限流 / fail2ban 封禁**
（`kex_exchange_identification: Connection closed by remote host`），
在 10:33 前后持续约 4 分钟，期间 `scp` 静默失败（被我 `| Out-Null` 吞掉，导致一次「模块不存在」误报）。
**经验：**
1. 远程操作尽量**合并为单次 ssh**，脚本本地写好再传，不要反复小连接；
2. `scp` 不要用 `| Out-Null` 掩盖退出码，否则会把上传失败误判为执行失败；
3. 期间可用**公网 80 端口**判断服务健康（不受 SSH 限流影响）。

### 九、本次未做

1. `lib/db.js` / `lib/permissions.js` / `lib/poultry-accounting.js` / `lib/sync-job-audit.js` 的本地改动仍未上云（与本次无关）。
2. 云端 schedule 子系统（1,073 行 + 3 张表 + 上述鉴权收紧）**仍未回流本地** —— 分叉面比首次部署时更大了，建议优先处理。
3. 云端 `/home/ubuntu/app/frontend/dist.old-contract` 与 `/tmp/zkstage/` 暂存件未清理（留作回滚）。
   `_bak/` 下现有备份：`database.sqlite.bak-before-contract-*`、`server.js.bak-before-contract-*`、
   `notifications.js.bak-before-contract-*`、`dist.bak-before-contract-*.tgz`、
   `server.js.bak-before-dingtalk-*`（两份，含第一次的事故版本）。

## 2026-09-22 门店闭店与迁址：可录入、可追溯、口径明确

> 完整方案（业务口径 / 数据模型 / 流程 / 接口 / 验证）见 **`docs/门店闭店与迁址方案.md`**。
> 本节只记录「改了什么、为什么、验证结果、踩过的坑」。

### 一、需求与业务口径（业务确认，三条）

用户提出：门店基本信息里**没有闭店时间录入，也没有迁址的原店↔新店关联**。经问答确认口径：

1. **「同一家店」= 同一个老板对同一家门店的运营。** 换法人但门店未停业 **≠ 闭店**，只记一条「法人变更」。
2. **迁址后收银机构编码会变** ⇒ 一律「**老店闭店 + 新建门店 + 双向关联**」；**新店只记新店数据，不做跨店合并**，
   关联仅作凭证与追溯（知道它不是全新门店、以及从哪家店迁来）。
3. 状态收敛为**三态** `筹建中 / 正常营业 / 已闭店`；「迁址」是闭店原因而非状态。
4. 闭店门店归入**全国统一的独立分组「闭店门店」**，默认不显示。
5. **闭店前员工必须全部处置**（随迁 / 调岗 / 离职）。
6. 迁址时**默认继承老店**（店型 / 法人 / 收款性质 / 区域归属），用户可自行微调。

> 现状利好：上线时全库 25 家门店都是正常营业 / 筹建中，**历史上没有任何闭店或迁址记录** ⇒ 纯新增，零存量迁移。

### 二、改动清单

| 层 | 改动 |
|---|---|
| 表结构 `lib/db.js` | `stores` +5 列（`closed_date/closed_type/closed_reason/relocated_to_store_id/relocated_from_store_id`）；新建 `store_lifecycle_events`（门店履历，模式对齐已有的 `employee_lifecycle_events`）；`store_regions` +`code` 列并种子出系统分组「闭店门店」；启动时幂等清理门店孤儿关联行 |
| 新模块 `lib/store-lifecycle.js` | 业务口径与共同逻辑集中一处：状态别名、闭店类型枚举、闭店分组识别、履历读写、员工待处置判定 |
| 接口 `server.js` | 新增 `GET /api/store-lifecycle/meta`、`GET /api/db/stores/:id/lifecycle`、`POST .../close`、`POST .../relocate`、`POST .../reopen`、`POST/GET /api/store-attachments`；`stats` 增加迁址店口径；**`PUT` 禁止直接改状态绕过闭店/重开**；**`DELETE` 增加经营数据保护**；抽出 `sendAttachmentFile` / `saveAttachmentFile` 供员工附件与门店凭证共用 |
| 数据正确性 `lib/business-analytics.js` | `resolveStore()` 分层：精确名称**允许**命中闭店门店（补录历史），模糊兜底**只允许**命中未闭店门店，并加**顺延保护**；`resolveStore` 导出以便单测 |
| 权限 `lib/permissions.js` | 新增 `['/api/store-attachments', 'store.manage']` |
| 前端 | `StoreBasic.vue` 重写（三态 + 闭店字段 + 员工处置表 + 迁址 + 履历时间轴 + 重开 + 凭证上传）；`StoreRegionManage.vue`「闭店门店」分组默认隐藏 + 开关、系统分组禁编辑；`StoreRegionSelect.vue` 排除闭店门店；`api/index.js` 新增 6 个封装 + 把响应体字段挂到 error 上 |

### 三、验证

| 套件 | 结果 |
|---|---|
| `_tmp/verify-store-lifecycle.js`（接口端到端，含自建自清） | **45/45 通过** |
| `_tmp/verify-store-matcher.js`（导入匹配规则单测，内存假 db） | **10/10 通过** |
| 前端构建 | 通过，新文案均在产物中 |
| 覆盖到的关键行为 | 未处置员工时拒绝闭店并返回清单 · 迁址双向指针与互指凭证 · 随迁员工确实改到新店 · **重开恢复原区域归属** · 删除保护（有数据拒绝、无数据连履历一起删） · 系统分组禁改禁删 · 三态枚举 · 凭证上传/取回/非法类型拒绝 · 员工附件端点重构后未回归 |

### 四、⚠️ 本次踩到的三个坑（都已修，值得记住）

1. **`lib/db.js` 里的 `db` 是 sql.js 原生对象，没有 `queryOne` 方法。**
   我写成 `db.queryOne(...)` 抛了 `db.queryOne is not a function`，而外层是 `catch {}` ——
   **异常被静默吞掉，表现是「列建出来了、种子行永远不出现」**，排查了很久。
   修法：用同文件末尾的模块级包装函数 `queryOne/insert/run`；并把静默 `catch {}` 改成打印告警。
   **教训：迁移代码里的空 catch 会把「不生效」伪装成「已完成」。**
2. **`ALTER TABLE` 的位置必须紧跟对应建表之后。**
   我把 `store_regions.code` 的 ALTER 放在了 `stores` 段（即 `store_regions` 建表**之前**），
   对全新库会因「表不存在」失败，连带后面的种子 `SELECT` 引用不存在的列也失败 —— 两处又都被静默吞掉。
3. **`ON DELETE CASCADE` 在本项目实际不生效。**
   删除门店会留下 `store_region_members` 孤儿行，表现为「闭店门店组显示 1 家、点进去却没人」
   （计数用 LEFT JOIN、成员列表用 INNER JOIN，于是数量对不上）。
   修法：删除接口显式清理关联行 + 启动时幂等自愈（实测清掉 3 行孤儿）。

### 五、遗留与建议

1. **`store.close` 独立权限码**：闭店不可逆且影响历史口径，目前与其他门店档案变更共用 `store.manage`，建议后续单独出码。
2. **平台绑定不继承、也未联动提示**：迁址后新址在美团/京东/抖音通常是新门店，需人工到「门店平台」页重新绑定。
3. **报表口径按业务决定不做跨店合并**，迁址店在报表里表现为「新店无历史」—— 这是**有意为之**，不是缺陷。
4. 闭店门店目前在门店选择器里**不可选**；若将来要「按日期查闭店门店历史数据」，给 `StoreRegionSelect` 加 `includeClosed` 属性即可。
5. **本次只在本地完成**，尚未上云（云端部署需按前述「外科式补丁」流程处理，且注意本地与云端仍有分叉）。




