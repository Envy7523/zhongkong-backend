# 中控后台 · 改动与部署记录

> 本文档记录本项目相对"基础版"的所有自定义改动，以及云服务器部署信息。
> **更新项目/重新部署前先读本文档**，避免被旧版本覆盖丢失功能。

> 📌 **接手「菜品核算」功能请看 → [`docs/poultry-accounting-handover.md`](docs/poultry-accounting-handover.md)**
> 那份是自包含的交接文档：领域知识（一只鹅怎么拆）、料篮算法、已确认口径、18 个接口、
> 页面结构、11 个踩坑记录、待办清单、验证脚本使用方法、安全须知。
> 验证脚本在 `tools/poultry-verify/`。

---

## 一、云服务器部署（上云）

### 服务器信息
| 项目 | 值 |
|------|-----|
| 公网 IP | 134.175.41.247（腾讯云轻量，实例 lhins-c2q2pyy7） |
| 访问地址 | http://134.175.41.247（nginx 80 → node 3456） |
| SSH | 端口 22，用户 ubuntu |
| 项目目录 | `/home/ubuntu/app` |
| 数据目录 | `/home/ubuntu/app/data`（database.sqlite） |
| 服务 | systemd `zhongkong.service`（崩溃自动重启） |

### 连接工具（本地）
- 目录：`server-access/`（私钥 `zhongkong_company`、`sshrun.py`、备份 `backup/`）
- 私钥等同服务器钥匙，**切勿外传**
- 快捷连接：`ssh -i server-access\zhongkong_company ubuntu@134.175.41.247`

### 部署 / 更新步骤（重要）
```bash
# 1. 上传源码（每次改完代码都做）
scp -i server-access\zhongkong_company server.js package.json package-lock.json config.json stores.json ubuntu@134.175.41.247:/home/ubuntu/app/
scp -i server-access\zhongkong_company -r lib css js tools demo ubuntu@134.175.41.247:/home/ubuntu/app/

# 2. 上传前端（先本地 npm run build）
scp -i server-access\zhongkong_company -r frontend\src frontend\dist frontend\public ubuntu@134.175.41.247:/home/ubuntu/app/frontend/
scp -i server-access\zhongkong_company frontend\package.json frontend\package-lock.json frontend\vite.config.js frontend\index.html ubuntu@134.175.41.247:/home/ubuntu/app/frontend/

# 3. 上传数据库（仅在需要同步数据时）
scp -i server-access\zhongkong_company data\database.sqlite ubuntu@134.175.41.247:/home/ubuntu/app/data/database.sqlite

# 4. 云端重启
ssh -i server-access\zhongkong_company ubuntu@134.175.41.247 "cd /home/ubuntu/app && sudo systemctl restart zhongkong"
```

### 云端注意事项
- 服务器上没有 git，直接覆盖部署
- 覆盖前建议备份云端文件：`ssh ... "cp /home/ubuntu/app/data/database.sqlite /home/ubuntu/app/data/database.sqlite.bak-$(date +%Y%m%d%H%M%S)"`
- 云端旧数据库已备份到本地 `server-access/backup/server-database.sqlite`

---

## 二、2026-08-28 · 全国地图修复（DataV 防盗链）

### 问题
云端地图完全空白，控制台报：
```
Failed to load resource: 403
Uncaught (in promise) SyntaxError: Unexpected token '<', "<!DOCTYPE "... is not valid JSON
```
本地正常，上云后失效。

### 根因
`StoreMap.vue` 画省份色块用的 GeoJSON 来自**阿里云 DataV**：
`https://geo.datav.aliyun.com/areas_v3/bound/*.json`
DataV 校验浏览器 **Referer 防盗链**：本地 localhost 放行，云域名被拒 → 403 HTML → 前端 JSON 解析崩溃 → 地图空白。
（**不是高德问题**——高德 key 白名单已配好，脚本加载正常）

### 修改文件
1. **`frontend/src/views/store/StoreMap.vue`**
   - `GEO_BASE` 从 `https://geo.datav.aliyun.com/areas_v3/bound` 改为 `/api/geo/bound`
   - 3 处 fetch 改为带参形式：
     - `` fetch(`${GEO_BASE}?path=100000_full.json`) ``
     - `` fetch(`${GEO_BASE}?path=${adcode}_full.json`) ``（2 处）
2. **`server.js`**
   - JWT 中间件放行 `/api/geo/bound`（地图边界数据无需登录）
   - 新增代理接口 `GET /api/geo/bound?path=xxx_full.json`：
     - 后端转发到 DataV（服务器请求无 Referer 限制，实测 200）
     - 路径白名单校验（只允许 `xxx_full.json`，拦截目录穿越）

### 验证
- 本地/云端 `GET /api/geo/bound?path=100000_full.json` → 200，582KB GeoJSON
- 非法路径 → 400

---

## 三、历史功能改动清单（防止更新覆盖丢失）

> 以下功能都是在本项目基础版上新增的，**更新代码时务必保留这些改动**。

### 1. 菜品分类（菜品管理 → 菜品分类）
| 文件 | 改动 |
|------|------|
| `lib/db.js` | 新增 `menu_categories` 表 + 历史分类迁移 |
| `server.js` | `/api/menu-category/*` CRUD、`/api/menu-categories` 改造 |
| `frontend/src/views/menu/MenuCategory.vue` | 新增页面 |
| `frontend/src/views/menu/MenuModuleHeader.vue` | 导航加「菜品分类」 |
| `frontend/src/App.vue` / `stores/app.js` / `router/index.js` | 菜单与路由 |

### 2. 菜品销售明细（dish_sales 表 + 数据）
| 文件 | 改动 |
|------|------|
| `lib/db.js` | 新增 `dish_sales` 表（门店/菜品编码/名称/规格/数量/金额/优惠/收入/时间/退款/订单号/结账方式/退款金额）+ 索引 |
| `server.js` | `/api/dish-sales`（分页查询）、`/api/dish-sales/stats`、`/api/dish-sales/analytics`（菜品分析聚合，支持门店筛选/分页） |
| 数据 | 已导入 190,437 条菜品销售明细（5/1–8/9），门店名称对齐后台 23 家 |
| `frontend/src/views/analysis/BusinessAnalytics.vue` | 总数据视角新增「菜品销售分析」面板 |

### 3. 堂食菜品绑定（总数据视角）
| 文件 | 改动 |
|------|------|
| `lib/db.js` | 新增 `dish_sales_mappings` 表，唯一键 `(product_code, product_name, spec)`（**规格参与唯一键，上庄/下庄不混绑**） |
| `server.js` | `/api/dish-sales/mappings` 列表（含 mapped 状态筛选）、`POST` 绑定、`POST /batch` 批量、`POST /auto-bind` 一键、`DELETE` 解绑 |
| `frontend/src/views/analysis/DishSalesBinding.vue` | 新增页面（筛选/多选批量/一键绑定/规格区分） |
| 路由/菜单 | `/analysis/total/binding`、「堂食菜品绑定」入口 |

### 4. 团购/外卖菜品绑定
| 文件 | 改动 |
|------|------|
| `lib/business-analytics.js` | 商品分析返回 `mapping_id`；新增 `deleteProductMapping` |
| `server.js` | `DELETE /api/business-analytics/mappings/:id`、`POST /batch`、`POST /auto-bind` |
| `frontend/src/views/analysis/ProductBinding.vue` | 绑定页（筛选/多选批量/一键绑定） |
| 路由/菜单 | `/analysis/group-buy/binding`、`/analysis/delivery/binding` |

### 5. 其他
- `lib/db.js` 的 `save()`：写盘失败重试 3 次（Windows 防病毒瞬时锁文件）
- `frontend/index.html`：加载高德 JS API（key `95fc48463fa6945f4c5a2380dd549d7b`，需在高德控制台配域名白名单，含云 IP）

---

## 四、更新项目时的注意事项（防覆盖）

1. **不要用旧版覆盖这些文件**（含上述所有自定义功能）：
   - `lib/db.js`、`lib/business-analytics.js`、`server.js`
   - `frontend/src/views/analysis/*`、`frontend/src/views/menu/MenuCategory.vue`
   - `frontend/src/App.vue`、`stores/app.js`、`router/index.js`、`api/index.js`
2. **前端 dist 每次更新需重新构建**：`cd frontend && npm run build`（dist 是构建产物，别用旧 dist）
3. **数据库**：`data/database.sqlite` 是核心数据（88MB，19 万条销售明细），**不要用旧库覆盖**
4. **上云后**：按「一、部署/更新步骤」重新上传修改过的文件并重启服务
5. 高德白名单：云 IP `134.175.41.247` 需在 key 的域名白名单里（已配）
6. `config.json` 含真实凭据（corpid/botSecret/AI key），部署时保留

---

*记录日期：2026-08-28*

## 2026-09-02 淘宝闪购“账单汇总”真实到账导入（真实实收口径修正）

背景：此前导入的“营业额收入单量”实收未扣第三方费用（8月=182,995.64），平台真实到账需扣除推广/保障/余额变动等费用。
- 新增表 taobao_flash_bills（账单汇总原始行：结算入账ID/门店ID/门店名称/账单日期/结算金额/结算日期/账单类型 + 整行 raw_json，未来新增列自动收录；UNIQUE(门店ID,账单日,账单类型)，可重复导入覆盖）
- lib/business-analytics.js：importTaobaoFlashWorkbook 自动识别“账单汇总”子表（仅该子表入库）→ importTaobaoFlashBillWorkbook：每店每日净额=当日全部账单类型金额之和，覆盖写入 business_revenue_records（channel=taobao_flash, actual=真实实收, gross=外卖结算金额, 有效订单数延续旧记录）；对“整月商品销量汇总”文件给出明确报错不入库；日期范围已有账单汇总时拒绝旧“营业额收入单量”误覆盖
- 前端 BusinessDataImport.vue：淘宝闪购入口新增“真实到账·账单汇总（推荐）”卡片与字段说明，导入结果显示 净额合计/原始明细
- 2026-08 导入结果：账单汇总 1449 行、18 门店全匹配、479 条每店每日净额、净额合计 ¥169,393.83（旧口径 ¥182,995.64 − 第三方费用 ¥13,601.81）

## 2026-09-02 记账本覆盖更新（删除 ≥8/10 旧记账 → 导入 Sheet1 权威数据）
- 新增 lib/bookkeeping-import.js + POST /api/bookkeeping/import-replace：解析含 日期/门店名称/大类/小类/金额 的记账流水子表（按有效数据行数自动选主表，仅 Sheet1 这类流水表；门店按名称匹配本地 stores；大类/小类按名复用或自动创建；早于 replace_from 的行拒绝）
- 处理：先 DELETE bookkeeping_entries date>=2026-08-10（删除 733 条旧数据），再导入 记账本更新92.xlsx Sheet1 全部 2396 条（8/10~8/31，21 家门店全匹配，0 跳过）
- 核验：本地记账总量 6852（<=8/9 保留 4456 + >=8/10 新 2396）；8/10-8/31 各大类合计与文件逐项一致（销售收入 +1,451,877.22 / 原材料 -420,650.02 / …），合计 609,090.25

## 2026-09-02 美团外卖“真实到账”结算账单导入（与淘宝闪购同法）
- 新表 meituan_delivery_bills：按(美团门店ID, 账单日期, D交易类型)汇总 R商家应收款（含交易笔数/描述快照，未来新列自动收录）；来源“订单明细”导出（表头不固定在第1行亦可识别）
- lib/business-analytics.js：importMeituanDeliveryWorkbook 自动识别“订单明细”结算表 → importMeituanDeliveryBillWorkbook；rebuildMeituanDeliveryRevenue：营业额/优惠后收入/单量来自美团营业额日报，真实到账=当日全部交易类型商家应收款之和，费用按类型归入 推广/保险/退款/服务；只重建 source_type='platform'，保留收银机(pos)记录
- 补充绑定：美团外卖 platform_id 保利上城(26)=34251847、未来花园(27)=34251852（档案原为空）
- 2026-08 导入结果：1918 条(门店,日,类型)账单、18 店全匹配、521 条每店每日净额；真实到账合计 ¥218,536.74（与官方“账单明细”按店日汇总 521/521 完全一致），费用合计 ¥17,832.19
- 前端：美团外卖入口新增“真实到账 · 结算账单”卡片与字段说明，导入结果显示 优惠后收入/平台费用/真实实收

## 2026-09-10 总数据视角「菜品销量」替换为「菜品销售分析」（去重复）
背景：总数据视角（品牌/门店）里，页签「总数据菜品销量」与「经营总览」底部的「菜品销售分析」面板功能重合。按用户决定，保留字段更完善的菜品销售分析，删掉总数据菜品销量。
- 前端 frontend/src/views/analysis/BusinessAnalytics.vue：
  - 页签「菜品销量」在总数据视角改名为「菜品销售分析」（外卖/团购平台看板仍叫「菜品销量」，未受影响）
  - 该页签内容换成菜品销售分析口径：菜品数/总销量/销售额/菜品收入/优惠金额/退款金额/预估成本(已绑定 x/y)/净收入 + 排序(按收入/销量/金额) + 服务端分页
  - 并入原总数据菜品销量的两项独有能力：来源订单明细展开（按 菜品编码+名称 从收银机品项明细取数）、行内绑定本地菜品 SKU（写 dish_sales_mappings，与「堂食菜品绑定」同表同唯一键）
  - 删除「经营总览」内重复的菜品销售分析面板（时段订单走势图保留）
  - 保留赠品维度：新增「赠品数量 / 赠品金额」两列与两张指标卡。dish_sales 表没有赠品列，故按 (菜品编码,菜品名称) 从同一批收银机品项明细（来源订单明细）汇总到菜品行上；口径与总数据菜品销量一致——赠品已含在销量/销售额内，只标注不重复相加
- server.js：GET /api/dish-sales/analytics 的列表行补充 mapping_id/menu_item_id/menu_sku_label/unit_cost 等已绑定 SKU 字段（原字段与 estimated_cost、net_income 口径不变），供行内展示与改绑使用
- 核验：_tmp/verify-dish-analytics-fields.js 直连库跑新 SQL（5/1-8/9 区间 mapped 行返回 sku="招牌烧鹅饭 · 常规"、unit_cost=13.5）；_tmp/verify-gift-join.js 核对赠品行级取数（9/10 超爽柠檬茶 11 份 / ¥88.00，与汇总一致）；_tmp/probe-total-dish.cjs 经 CDP 渲染总数据品牌/门店视角，页签=菜品销售分析、16 列表头（含赠品数量/赠品金额/净收入/退款金额）、展开列与绑定选择器各 20 个、来源明细可展开、无 console 错误；外卖平台视角仍为「菜品销量」

### 追加修正：本地菜品自动识别（不再要求「本地菜品绑本地菜品」）
问题：上线后总数据视角 361 行全部显示「选择本地菜品 SKU」——没有手工记录时只认 dish_sales_mappings，把本来就取自本地菜品库的品项当成未关联。
- server.js `/api/dish-sales/analytics` 改为两段式：SQL 只做分组汇总（不设 LIMIT，分组数在千级），本地 SKU 识别 / 排序 / 翻页在 JS 完成
- 识别优先级（与旧「总数据菜品销量」同一套规则）：手工绑定 → 菜品编码=menu_items.skuid（名称互校，防错归）→ 名称+规格唯一 → 名称唯一；手工绑定可指向已下架菜品，另按 id 补查
- 预估成本 / 净收入 /「已关联 x/y 款」随识别结果计算（口径不变：数量 × 单位成本；订单量仍走 SQL 全区间去重，不由分组相加）
- 排序：已关联在前，未关联自动沉到列表末尾（与外卖/团购看板对齐），再按收入/销量/金额倒序
- 前端：绑定列显示识别出的 SKU 名称，自动识别的按钮提示「已按菜品编码/名称自动识别，点击可改绑」；指标卡文案改为「已关联 x/y 款 · 含自动识别」
- 9/10 实测：361 分组自动识别 126 个（35%），预估成本 ¥23,242.20、净收入 ¥40,392.81（改前均为 0）；其余 208 个是外卖套餐/别名（如「限量!招牌烧鹅腿饭 [一人份]」「【太公烧鹅饭】含时蔬+海咸鸭蛋 [1人份]」），本地菜品库无此名称，**故意不做模糊自动绑**（会把套餐成本算错），仍走智能推荐人工确认
- 附带发现：部分已识别菜品在菜品管理里成本为 0（叉鸭双拼饭 / 太公烧鹅·半只 / 招牌烧鹅（约100克）），这几行净收入=收入，需补维护成本
- 核验：_tmp/verify-dish-automatch.js 直连库统计识别覆盖率；CDP 渲染三条路由各自独立会话：总数据品牌/门店页签=菜品销售分析且顶部行显示「招牌烧鹅饭 · 常规」「太公烧鹅 · 下庄 · 份」，外卖平台仍为「菜品销量」，均无 console 错误

## 2026-09-11 新增「菜品管理 → 菜品核算」（选门店+时间段反推禽类消耗只数）
背景：需要知道某门店某时间段大概消耗了多少只鹅/鸭/鸡。系统里只有菜品销量，没有任何「一份菜用掉多少只禽」的数据，所以先补三层基础档案再算。
> 位置说明：按需求暂挂「菜品管理」下，功能打通后整体迁往「成本核算」。

### 数据模型（lib/db.js 新增 4 张表）
三层模型，全部以「只」为最终锚点：
- `poultry_birds` 禽类档案：一只禽的品种/单只净重(kg)/单只成本/状态
- `poultry_yields` 整只出成拆解：一只禽出哪些部位、每个部位出几个（`parts_per_bird`）、单份克重
- `poultry_dish_usage` 菜品耗用：每份该菜品吃掉几个该部位（`usage_qty`），UNIQUE(menu_item_id,yield_id)，**一菜可挂多行 → 双拼/套餐/鸡鸭双拼天然支持**
- `poultry_purchases` 实际采购只数：手动录入，UNIQUE(store_id,month,bird_id)，用于理论 vs 实际对比

公式：`消耗只数 = Σ(菜品销量 × 每份耗用部位数 ÷ 一只出该部位数)`；折重 = 只数 × 单只净重；理论成本 = 只数 × 单只成本。

### 口径（已与业务确认）
- 退款行整体排除：`refunded IN ('部分退','是','1')` 不计入（报表只有退款金额、无退菜数量，无法精确扣减）
- 渠道由 `dish_sales.payment_detail` 关键字判定，优先级 **团购 > 外卖 > 堂食 > 其他**（「店内销售|收银POS|POS|抖音团购」属团购核销，不算纯堂食）
- ⚠️ 实测约 65% 销量（17.5 万份）的 payment_detail 形如「扫码支付-微信 25.00」，**不含渠道信息**，归入「其他/未分类」并在页面单独标注，不参与渠道筛选
- 只数保留 2 位小数；只数先定稿再由它推折重/成本，保证页面上「只数 × 净重/单价」能对上（行级舍入 vs 合计舍入会差出 0.6 元那类错位）

### 菜品匹配三级兜底（提升覆盖率）
① `dish_sales_mappings` 绑定 → ② 名称 + 归一化规格直配 → ③ 名称唯一命中 → ④ 计入「未绑定」清单。
两个易踩的坑已处理：
- **空菜名不得参与匹配**（报表/档案里的空名行会互相误挂）
- **孤儿绑定视为未绑定**：库里有 10 条 `dish_sales_mappings` 指向已被删除的菜品（旧库遗留，FK 未生效），否则缺口清单会出现没有菜品名、也无法去配置的空行；现已退回名称兜底，兜不住才按未绑定报出

### 后端
- 新模块 `lib/poultry-accounting.js`（三层档案 CRUD + 核算 + 采购对比 + Excel 模板/导入）
- `server.js` 新增 17 个 `/api/poultry/*` 接口：species CRUD、yields 整表保存、dish-usage（单菜品配置 + 全菜品总览 + 已配置列表）、accounting 主核算、accounting/purchase-comparison、purchases CRUD、template、import
- Excel 模板**预填本地全部菜品行**（只需补品种/部位/每份耗用），导入逐行容错：非法禽类值报错跳过、不存在品种/菜品报错回报、预填空行静默跳过

### 前端
- 新增页面 `frontend/src/views/menu/MenuAccounting.vue`：筛选条（门店/日期默认本月/渠道/禽类）→ 只数汇总卡（鹅鸭鸡各多少只+折重+成本）→ 覆盖率提示条（覆盖销量占比、未绑定/未配出成缺口下钻、>1只/份 复核提醒、渠道构成）→ 按菜品下钻表（每份折合只数、消耗只数、占比）→ 理论 vs 实际采购对比表 + 采购录入 → 基础档案抽屉（禽类档案/整只出成/菜品耗用三页签 + 模板下载 + 导入）
- 5 处注册：`router/index.js`（`/menu-management/accounting`）、`App.vue`（COMPONENT_MAP + 弹窗菜单「菜品数据」组 + **WORKSPACE_TAB_ROUTES** + **workspaceRouteTabId**）、`stores/app.js`（SUB_LABELS）、`MenuModuleHeader.vue`（navItems）
- 新增页面漏注册会「地址栏正确但内容区空白/落到数据概括」，详见下方修复第 1 条
- **迁移说明**：只需改 tab id 前缀 `menu-management-accounting` → `cost-accounting-accounting`、路由 path、文件目录，业务代码与接口无需改动（文件头已写迁移步骤）

### 同期修复的两个既有缺陷
1. **`App.vue` `workspaceRouteTabId` 漏登记独立静态路由**（本次由 UI 探针发现）：`/menu-management/accounting`（以及**既有的 `/menu-management/category`**）走的是静态路由而非 `:section` 通配，反查不到 tab id → 地址栏正确但**刷新/直接打开/分享链接时内容区落到数据概括**。已在 `workspaceRouteTabId` 的简单页面映射里补上这两条，并加注释提醒后续新增同类页面要同步登记。
2. 核算模块自身修掉的缺陷：采购对比按月录入却按 `YYYY-MM-01` 查询（永远查不到，对比恒为 0）；页面在「档案已加载但核算未返回」时会读空 `summary` 报错（已加结果守卫）；导入把非法禽类值（如「猪」）静默改写成「鹅」。

### 验证（`_tmp/verify-poultry.ps1`，43 项全部 PASS，跑完自动清理）
- 公式对照：招牌烧鹅饭 9806 份 ÷ 出成 16 = **612.88 只**；折重 2757.96kg = 612.88×4.5；成本 ¥73,545.60 = 612.88×120
- **独立交叉核对**（`_tmp/crosscheck-poultry.js` 直连库用 sql.js 复算）：博罗店 201 份 → 12.56 只、全区间总销量 269,135.14、四个渠道 174801.1/62711.48/24635.57/6987 与接口逐项完全一致
- 门店/渠道筛选、采购同键覆盖与整月判定、Excel 三页签真实导入（含错误行回报）、一菜挂鹅+鸭两行（鸭 9806÷2 = 4903 只）
- 数据库已验证回到空态（species/yields/configured/purchases 全 0），未残留测试数据
- ⚠️ **未经真实浏览器渲染验证**：本机沙箱阻止 Chrome（CDP 端口起不来、headless 无法写文件），页面渲染只在构建层面确认过（vite 编译通过、产物含全部页面与接口路径、路由返回 200、无 console 异常无从取得）。UI 探针脚本 `_tmp/probe-accounting-ui.cjs` 已写好，在非沙箱环境可直接跑（先以 `--remote-debugging-port=9335` 启动 Chrome）

### 追加优化：菜品耗用改为「分组选菜 + 批量关联」（2026-09-11 同日）
问题：160 个菜品平铺在一个列表里逐个配耗用，实际同一分组的菜（如「烧鹅·濑粉」30 个）通常用同一套部位耗用，逐个点太慢。
- 后端 `listUsageOverview` 增加**分组（菜品分类）筛选**并返回每个分组的「已配置/总数」进度 + `all_total`；分组进度基于全部菜品统计，不受关键词/分组筛选影响，左侧分组条进度始终稳定
- 新增 `POST /api/poultry/dish-usage/batch`（`lib/poultry-accounting.js` 的 `batchSaveUsage`）：把同一套「部位 × 每份耗用」套用到多个菜品
  - `mode=replace` 覆盖：先清空这些菜品的原有配置（适合整组用同一套部位），并**回报被清掉的条数 `removed` / 受影响菜品数 `replaced_dishes`**，避免静默删掉手工配置
  - `mode=append` 追加：upsert 保留原配置（适合给已有配置的菜再加一条双拼部位）
  - 校验：空菜品列表 / 空耗用行 / 无效部位 / 耗用量≤0 / 同一部位重复 / 菜品不存在 均按 400 拒绝；单次上限 300 菜品、30 条关系
- 前端 `MenuAccounting.vue`「菜品耗用」页签：**分组选择条**（每个分组显示 `已配置/总数`，配满打 ✓）→ 关键词 + 状态筛选 → 表格加 **多选列** → 勾选后出现**批量操作条**（已选 N 个 / 选中本组全部 / 清空选择 / 批量关联禽类耗用）→ 批量弹窗（**应用方式：覆盖 / 追加** 用 el-segmented，逐行配部位×耗用，实时折算「每份 X 只」，底部预览将套用到哪些菜品）。单个菜品仍可「编辑」精调
- 细节：批量弹窗会**取选中项里第一个已配置的菜品作为初值**，减少重复录入；切换分组/筛选会**自动清空勾选**，避免把上一次的选择连同新条件一起提交
- 验证：`_tmp/verify-poultry-batch.ps1`，**31 项全部 PASS**（自清理），覆盖分组统计=160、分组筛选无越界、覆盖/追加两种模式、覆盖时 `removed`/`replaced_dishes` 回报、6 项参数校验、以及批量结果参与核算与手算对照（1273.15 份 ÷ 出成 4 = 318.29 只）；原 `_tmp/verify-poultry.ps1` 43 项回归仍全部 PASS

### ⚠️ 事故与修复：验证脚本曾整表清空用户数据（2026-09-11）
现象：验证脚本为了「可重复执行」，收尾时执行了**整表删除**（所有禽类档案 / 所有已配置的菜品耗用 / 所有采购记录），把用户在此期间手工录入的数据一并删掉了。
- 已定位责任范围：**只影响 `poultry_birds` / `poultry_yields` / `poultry_dish_usage` / `poultry_purchases` 四张新表**。`menu_categories`（菜品分类）与 `menu_items` 全程未被本功能的任何代码写入或删除 —— 与 12:30 备份逐行比对，160 个菜品及其分组完全一致，分类仅少一个 `#24 外卖菜品`（非本功能所致，本功能没有任何删除分类的调用路径；排序接口还带「必须提交全部分类完整排序」的校验且只 UPDATE 不 DELETE）。
- 已修：两个验证脚本改为**最小回收** —— 只删按名称前缀（`验证用*` / `批量验证用*` / 金丝雀）自己建的行，采购按 remark 标识回收；对真实菜品的耗用配置改为**先快照、收尾原样还原**；断言由「整表为空」改成「与基线一致」。
- 已证明：`_tmp/canary-safe-teardown.ps1` 先埋一份「用户数据」（禽类+出成+挂在 206 上的耗用+采购），再跑两个验证脚本，结果金丝雀**全部存活且数值未变**（禽类/出成/耗用 usage_qty=2/采购 555 均保留），脚本自身数据全部回收。
- 教训：**验证脚本绝不允许整表 DELETE**；沙箱里脚本可能被中断在收尾之前，所以「收尾」不能是唯一的清理屏障，删除范围必须一开始就限定在自己创建的行上。

### 部署记录：菜品核算上线云端（2026-09-11 17:05）
- 脚本 `_deploy-poultry.ps1`（logs: `_deploy_poultry_log.txt`）。上传：`server.js`、`lib/db.js`、`lib/poultry-accounting.js`（新增）、`lib/business-analytics.js`、`PROJECT_CHANGES.md`、`frontend/dist`（先清旧目录）、`frontend/src`。
- **刻意未上传**：`config.json`（云端凭据）、`data/database.sqlite`（云端业务数据）。云端新表由服务启动时的 `createTables()` 自动创建。
- 云端备份：`server.js.bak-20260911170553`、`lib/db.js.bak-…`、`lib/business-analytics.js.bak-…`、`PROJECT_CHANGES.md.bak-…`、`data/database.sqlite.bak-cloud-20260911170553`。
- 上线前差异核对：先用 md5 + 行级 diff 确认云端没有本地缺失的实现（云端多出的 11 行 server.js / 6 行 business-analytics.js 经核对都是**已被本地重写掉的旧版本片段**）；`db.js`、`PROJECT_CHANGES.md`、`frontend/src` 云端独有内容为 0。**结论：本地为严格更新的版本，覆盖安全。**
- 上线后验证：服务 active；4 张表已建且唯一键 `UNIQUE(menu_item_id, yield_id)` 正确；公网 `GET /api/poultry/{species,dish-usage,accounting,template}` 全部 200 且返回正常（分组 11 个 / 菜品 160 个 / 模板 77,668 base64）；页面 `/menu-management/accounting` 200 且引用新分包 `MenuAccounting-BmQr6aPu.js`；**15 个既有接口（门店/菜品/销售分析/绑定/成本构成/记账/协同/人事/用户/看板/业务分析/推送/机器人）全部正常**，未影响线上。
- ⚠️ **云端数据缺口（需业务决定）**：云端 `dish_sales` 仅覆盖 **2026-05-01 ~ 2026-08-09**（190,437 行），而本地已到 **2026-09-10**（229,613 行）。因此菜品核算在云端目前只能算到 8/9，算 8 月整月或 9 月会缺数据。**不要直接上传本地 database.sqlite 覆盖云端库** —— 云端的用户、协同事项、记账等是线上持续写入的，覆盖会丢（本项目历史上就出现过上云后要单独恢复云端用户的情况）。建议只补 8/10–9/10 的销量数据（走收银 POS 导入，或做一次只针对 `dish_sales` 的定向同步）。

### 修复：追加出成部位后，「菜品耗用」选不到新部位（2026-09-11）
现象（用户报告）：把一只鹅拆成 4 个规格并绑好菜品后，返回「整只出成拆解」追加 2 个规格，再回到「菜品耗用」，部位下拉里**看不到新增的 2 个规格**。
- 根因（纯前端缓存）：`MenuAccounting.vue` 里部位下拉的数据源是 `yieldOptions`，它只在页面挂载时加载一次；`saveYields()`（保存出成）后只刷新了出成表格与禽类档案，**没有刷新 `yieldOptions`**；而 `openUsageDialog` / `openBatchUsageDialog` 又写成「仅当列表为空时才去拉取」，导致一旦加载过就永远是旧快照。
- 修复（4 处，让数据源始终同步）：
  1. `saveYields()` 保存后追加 `await loadYieldOptions()`
  2. `openUsageDialog` / `openBatchUsageDialog` 改为**每次打开都重新拉取**（不再依赖空判断）
  3. `openArchive()` 打开抽屉时刷新部位下拉
  4. 新增 `onArchiveTabChange`：抽屉内切到「整只出成」重读出成表、切到「菜品耗用」重读部位下拉 + 耗用总览 —— 覆盖「在抽屉内来回切页签」这条路径
  5. 附带：`removeBird` 删品种后会级联删出成，一并刷新部位下拉，避免留下指向已删部位的失效选项
- 顺带优化：部位下拉改为按「禽类 · 品种」分组的 `el-option-group`（一个品种挂十来个部位时平铺很难找），并同步用于单个配置与批量关联两个弹窗
- 验证：`_tmp/verify-yield-append.ps1` 复现用户场景（自建临时禽类+临时菜品，收尾全回收），**11 项全部 PASS**：4 规格保存 → 绑定菜品 → 追加到 6 规格 → 接口回读 6 个部位 → **下拉数据源含全部 6 个且新规格 id/出成有效** → 原有绑定未丢失 → 新规格可直接绑定（双拼式两行）。结论：后端数据完全正确，问题只在前端缓存。
- 已部署：云端只更新 `frontend/dist` 与 `frontend/src`（后端无需改动），新分包 `MenuAccounting-J3jY6qQr.js`，公网校验通过。

### 口径重构：料篮模型（2026-09-11，用户反馈「鹅头带颈 + 战斧饭 = 3 只鹅」不合理）
问题（用户实测）：一只鹅身上同时产出 2 份上庄 / 2 份下庄 / 2 条腿 / 2 只翅 / 1 个头颈 / 11 份肉，但旧公式是 `Σ(各菜品销量 × 每份耗用 ÷ 一只出成)`，等于**每个菜品各自买一只鸟**，把同一只鸟的多个部位重复计了鸟。实测 11,000.26 只，按料篮算只有 6,586.74 只（多算 1.67 倍）。

- **正确模型**：
  - **身体部位**（上庄/下庄/腿/腩/切片肉/整只/半只）＝ 同一块身体的不同卖法，互相抢鸟 → 需求**相加**
  - **副产品**（翅/头颈/掌/杂）＝ 随鸟附带产出，不额外占鸟 → 需求**取最大**；只有超过身体需求时才成为瓶颈
  - `只数 = max(身体各部位折鸟数之和, 各副产品折鸟数)`
  - 边界：全部取最大也会错（把互斥的整只/半只当成同一批鸟，7,497 只严重偏低）
- `lib/db.js`：`poultry_yields` 新增 `part_kind`（身体/副产品），并按部位名自动预判做迁移（翅/战斧/头/颈/掌/爪/杂/肝/肠/汁 → 副产品），用户无需重录
- `lib/poultry-accounting.js`：核算改为按「禽 + 部位」汇总需求再套料篮模型；返回新增
  - `summary.birds[].body_birds / byproduct_birds / governing / governing_part`（说清瓶颈在身体还是副产品）
  - `summary.birds[].birds_count_linear`、`summary.total_birds_linear`（旧口径参考值）
  - `parts[]` 部位汇总（部位/类别/一只出成/总需求/折鸟数/是否瓶颈）
  - 明细行带 `part_kind` 与 `bird_id`
- 页面：筛选条公式说明改为料篮口径；新增「按部位汇总 · 瓶颈在哪」面板（含**口径对照条**：新口径主数 + 旧口径参考值及多算倍数）；「整只出成」表格新增**部位类别**下拉 + 类别含义图例
- **验证**：`_tmp/verify-poultry.ps1` 重写为**非破坏性**（只用自建临时禽类 + 临时菜品，核算公式改为只读校验内部不变量），**49 项全部 PASS**：每行只数 = 销量×每份耗用÷一只出成（59 行一致）、部位折鸟 = 总需求÷出成（12 个一致）、`只数 = max(身体相加, 副产品最大)`（逐禽一致）、合计 = 逐禽之和、每种禽唯一瓶颈标注、旧口径 ≥ 新口径、占比合计 100%、临时数据不污染结果；`_tmp/verify-poultry-batch.ps1` **29 项全 PASS** 回归

### 顺带修掉项目级隐患：ON DELETE CASCADE 实际未生效
排查删品种被拦时发现：`lib/db.js` 里的 `PRAGMA foreign_keys=ON` **没有真正生效**（实测 `PRAGMA foreign_keys` 返回 0），因此全库所有 `ON DELETE CASCADE` 都不会执行。这是**既有问题**，也解释了之前 `dish_sales_mappings` 里那 10 条指向已删菜品的孤儿绑定。
- 影响本功能：删除菜品后 `poultry_dish_usage` 会残留孤儿行，导致 ① 品种的「挂菜数」虚增 ② 删品种时报「已被 N 个菜品引用」而那个菜品其实已不存在
- 处理（不改动全局，避免波及其它模块）：新增 `purgeOrphanUsage()`，在 `deleteBird` / `deleteYield` 前显式清理孤儿行；`listBirds` 的挂菜数改为 JOIN `menu_items` 只算还存在的菜品；`configured_dish_count` 同样过滤
- **建议后续单独处理**：全库启用外键（或给 `menu_items` 的删除接口补显式清理），并清理历史遗留的孤儿 `dish_sales_mappings`

### 部署记录（2026-09-11 17:4x，料篮模型 + 部位类别）
- 上传：`lib/db.js`、`lib/poultry-accounting.js`、`PROJECT_CHANGES.md`、`frontend/dist`（先清旧目录）、`frontend/src`；后端无 `server.js` 改动
- 云端新表由启动时 `createTables()` 自动补 `part_kind` 列并执行迁移
- 上线前同样先做差异核对，`_deploy-poultry.ps1` 已保留全部备份路径可回滚

---

## 2026-09-12 覆盖率口径拆分：新增「禽类菜品覆盖率」+ 判定规则可维护（交接文档 §9.1 已修）

### 问题（用户提出的口径缺陷）
覆盖率原本 = 已配系数销量 ÷ **全部**销量，分母含柠檬茶、白米饭、打包盒等**永远不需要配禽类系数**的品类，
所以永远到不了 90%，拿它当风控可信度门槛没有意义。

实测欧景花园店 2026-09-01~09-10：
| 口径 | 数值 |
|---|---|
| 全量分母 | 2,079.15 份（含超爽柠檬茶 328、太公白米饭 80 …） |
| 禽类菜分母（含鹅/鸭/鸡） | 1,173 份 |
| 已配系数销量（分子） | 675 份 |
| 全量覆盖率 | 32.47% |
| **禽类菜品覆盖率** | **57.54%** |
| 只数 | 76.5 只（门店记录 80，口径未变） |

### 做法：拆成两个数，判定依据做成可维护配置
- `summary.poultry_covered_rate` **禽类菜品覆盖率** = 已配销量 ÷ **禽类菜销量** → 风控可信度看这个
- `summary.covered_rate` **全量覆盖率** 保留作参考；页面并列展示并注明分母差异
- 页面所有「覆盖率 < 90% 告警」与指标卡 tone 改用**禽类口径**（否则告警永远亮着，等于没有告警）

### 为什么不把「哪些菜算禽类菜」写死在代码里
名称判定会同时**误收**与**漏收**：
- 误收：`咸鸭蛋`（含「鸭」但是蛋制品）、`牛肉滑蛋饭`（含「蛋」）
- 漏收：`太公三宝饭` 这类名称不含禽类字、但实际含烧鹅的菜

所以判定依据做成用户可维护的配置，遇到边界不用改代码：
- `poultry_scope_keywords` 关键词表：include（算）/ exclude（不算，**优先级更高**）/ 可停用 / 带备注
- `poultry_scope_dishes` 显式名单：逐个菜品指定，支持「强制算 / 强制不算 / 清除标记」

**判定优先级（后端固定顺序：配置可改，顺序不可改）**
1. 显式名单 `in_scope=1` → 算
2. 显式名单 `in_scope=0` → 不算（用户明确意志，压过关键词与已配状态）
3. **已配禽类耗用 → 算**（它确实在消耗禽类；否则覆盖率分子会大于分母）
4. 名称命中 include 且不含 exclude → 算（档案名与报表名**任一**命中即可；任一名命中排除词则否掉）
5. 其余 → 不算

### 口径说明（重要，避免误读）
- 禽类菜口径**不受 `animal` 筛选影响**：与 `covered_quantity` 保持同一口径（都是「已配 / 禽类菜全量」），
  否则分子分母会不一致。按禽种细分覆盖率留作后续（需按菜品所挂禽类分别统计）。
- 默认关键词 `鹅/鸭/鸡` + 排除词 `蛋`，**首次启动才写入**（表非空时不覆盖，用户改过的配置优先）。
  默认口径下欧景店分母是 1,173 份；若只想看鹅，把「鸭」「鸡」停用即可（分母变 791 → 85%）。
- 关键词表被清空或全部停用时，后端回退内置默认，页面显示提示。

### 接口（+4，共 22 个）
| 接口 | 说明 |
|---|---|
| `GET /api/poultry/scope` | 关键词表 + 显式名单 + 生效规则 |
| `PUT /api/poultry/scope/keywords` | 关键词整表保存（至少留一条启用中的 include，否则 400） |
| `POST /api/poultry/scope/dishes` | 批量标记（`in_scope`: true / false / null=清除标记） |
| `GET /api/poultry/scope/preview` | 规则预览（全部菜品 × 全区间销量，带判定依据，用于核对规则） |

### 核算返回新增
- `summary.poultry_covered_rate / poultry_covered_quantity / poultry_scope_quantity / poultry_uncovered_quantity / poultry_scope_dish_count / poultry_scope_rules`
- `coverage.poultry_scope.{ include_keywords, exclude_keywords, source_summary, dishes[], unbound[], no_usage[] }`
  —— 其中 `unbound` / `no_usage` 是**禽类菜范围内**的缺口，比原来混着饮料/包材的全量缺口清单更可直接拿去补配
  （欧景店实测：金牌烧鸭饭 170、蜜汁鸡翅 43、限量!招牌烧鹅腿饭 9 …）

### 前端
- 覆盖率面板：主徽标 →「禽类菜品覆盖 X%」，并列「全量覆盖 Y%」（注明分母含无需配系数的品类）
- 文案带上当前判定依据（`名称含 鹅 / 鸭 / 鸡，排除 蛋`），一眼知道数字怎么来的
- 新增「维护禽类菜范围」弹窗：关键词增删改停（include/exclude 分栏）+ 规则预览统计（判定菜品数/销量/
  来源构成/被排除词挡掉数）+ 菜品级判定明细表（可搜索、可只看禽类菜）+ 批量改判（标为禽类菜/不算/清除）
- 指标卡新增「全量覆盖」一格

### 验证
- 新增 `tools/poultry-verify/verify-poultry-scope.ps1` —— **41 项全 PASS**：关键词读取/新增/停用/回读、
  4 项参数校验、显式名单三种模式与优先级（含「名含禽类词也能被手动排除」）、已配耗用强制计入、
  双覆盖率 7 项自洽校验（分子÷分母、≤100%、分母≤全量、禽类≥全量、未覆盖=分母−分子、缺口为子集）、
  「只数与改造前一致」、收尾还原与基线断言
- **关键词表快照自愈**：脚本先把关键词表快照到 `_tmp/scope-keywords-backup.json`，收尾**原样还原**；
  若上次异常中断留下备份，下次启动会**先自动还原再测试** —— 落实「收尾不能是唯一屏障」的教训
- 回归全绿：`verify-poultry.ps1` 49/49、`verify-poultry-batch.ps1` 29/29、`verify-risk-control.js` 12/12、
  `verify-yield-append.ps1` 13/13、`crosscheck-poultry.js` 独立复算与接口逐项一致（总销量 269,135.14、
  四渠道 174801.1/62711.48/24635.57/6987、博罗店 201 份 → 12.56 只）
- 交接文档 §11 的 **7 条核算不变量全部仍成立**（`verify-poultry.ps1` 第 5 节逐项断言，只数 76.5 与改造前一致）

### ⚠️ 顺带发现的既有脚本缺陷（本次**未改**，待确认后处理）
`canary-safe-teardown.ps1` 收尾断言写成「清理后禽类档案=0 / 已配置菜品=0」，即**假设数据库为空**。
现在库里有真实档案（禽类 1 + 已配置 22），该断言必然 FAIL；但：
- 5 项金丝雀存活检查**全部 PASS**（禽类/出成/206 耗用 usage_qty=2/采购 qty=555 全部存活且数值未变）
- 实测清理结果恰好等于「埋金丝雀之前」的状态（禽类 1 / 已配置 22 / 采购 0）→ 清理是干净的
- 该脚本本次**未被改动**（`git diff` 为空），FAIL 来自断言假设过时，与本改动无关
- 建议改为「与埋金丝雀前的基线一致」而不是「归零」

### ⚠️ 事故与恢复：`canary-safe-teardown.ps1` 在非空库上会破坏真实配置（本次踩到，已恢复）
在本地库（已填充真实档案）上运行该金丝雀脚本后：
- **菜品 206「招牌烧鹅饭」的耗用配置（`鹅肉 usage_qty=1`）被清空** → 欧景店核算只数 76.5 → **62.59**
- 根因：脚本第 31 行 `PUT /api/poultry/dish-usage/206`（整表替换）**覆盖**了 206 的真实配置，
  第 79 行收尾 `PUT rows=@()` 是**清空**而不是还原；第 82 行断言还假设「库为空」
  （`禽类档案=0 且 已配置=0 且 采购=0`）—— 该脚本编写时数据库是空的
- **它的 5 项金丝雀存活检查全 PASS 却有误导性**：只验证「自己埋的数据还在」，不验证「自己动过的真实配置有没有被改坏」
- **恢复方式**：从原始数据包库读出 206 原始配置（`menu_item_id=206, yield_id=52, usage_qty=1`）后写回，
  只数回到 **76.5**；同时清理了验证脚本残留的 3 条孤儿采购（`remark='自动化验证临时数据'`、bird 已删）
- **核对方式改进**：只比总数不够（总数不变但内容被换掉），应拿原始库与当前库**逐表逐行比对**
  `poultry_birds` / `poultry_yields` / `poultry_dish_usage` / `poultry_purchases`
- **建议修法（待确认，未擅自改）**：① 加守卫：库非空时拒绝运行；或 ② 先快照 206 原配置、收尾原样还原，
  并把断言改成「与运行前基线一致」。**该脚本不要在有真实数据的库上运行**。详见交接文档 §9.8
- 附带发现（脚本既有缺陷，未擅自改）：`verify-poultry.ps1` 收尾**先删禽类、再回收采购**，
  而 `listPurchases` 会 JOIN `poultry_birds` 过滤孤儿行 → 采购一旦失去禽类就**查不到**，回收必然失败。
  实测每次运行残留 1 条孤儿采购（本次共 3 条，已手工清理）。
  建议改成「**先回收采购、再删禽类**」，或回收时按 `remark` 直连库查询而不是走列表接口。

### 安全（仓库卫生）
- 补充 `.gitignore`：忽略 `data/*.sqlite-wal`、`data/*.sqlite-shm`
  —— sql.js 开了 `journal_mode=WAL`，运行时会生成这两个附属文件，同样含业务数据片段，
  此前会出现在 `git status` 里，容易被 `git add -A` 带进公开仓库
- **本次未部署云端**（云端仍是旧版；上云必须逐次获得用户明确同意）

---

## 2026-09-12 堂食菜品绑定接入「智能推荐」（复用菜品销量的识别规则）

### 需求
用户在「堂食菜品绑定」页手工逐个选 SKU 太慢，希望**直接复用「外卖/平台视角 → 菜品销量」里那套智能识别**来辅助关联菜品。

### 问题根因
两个页面各有一套匹配逻辑，能力差距很大：
| 页面 | 匹配方式 | 效果 |
|---|---|---|
| 菜品销量（`/api/dish-sales/analytics`） | 手工绑定 → 编码=skuid（名称互校）→ 名称+规格唯一 → 名称唯一，且**菜名归一化去掉【】（）[] 与空白** | 能识别 `【太公推介】…` 这类带前后缀的菜名 |
| 堂食菜品绑定（`/api/dish-sales/mappings`） | 只看 `dish_sales_mappings` 手工记录 | 全部显示「未绑定」，逼用户「本地菜品绑本地菜品」 |
| 「一键绑定」（`auto-bind`） | 只按**严格同名**（`by_name` 完全相等）+ 规格精确 | 带 `（外）`/`限量!`/`(迷你)` 前后缀的一律跳过 |

### 做法：抽成共享函数，规则只维护一处（不是复制代码）
`server.js` 新增（放在 `withDishMenuSku` 之后）：
- `dishMenuKey(value)` —— 菜名归一化：`replace(/[【】\[\]（）()\s]/g, '')`
- `buildDishMenuIndex(extraIds)` —— 载入在售菜品（排除员工餐），建 `menuById / menuBySkuid / menusByName / menusByNameSpec` 四个索引；`extraIds` 补入已下架但被手工绑定的菜品
- `resolveDishMenuSmart(index, group)` —— 返回 `{ menu, reason }`，`reason ∈ manual | skuid | name_spec | name | ''`
- `RECOMMEND_REASON_LABELS` —— 依据中文标签

**`/api/dish-sales/analytics` 改为调用同一套函数**（删掉内联的重复实现）→ 以后改识别规则只改一处，不会两页漂移。

### 接口
1. `GET /api/dish-sales/mappings` 每行新增推荐字段（**仅未绑定行返回**）：
   `recommend_reason / recommend_reason_label / recommend_menu_item_id / recommend_menu_name / recommend_sku_label / recommend_sku_variant / recommend_sku_price_summary / recommend_category / recommend_cost`
   并新增全量统计（不受分页/搜索影响）：`recommend_summary { pending, recommendable, unmatched }`
2. 新增 `POST /api/dish-sales/mappings/auto-bind-smart` —— 用同一套规则批量绑定未识别项，
   回报 `bound / skipped / reasons{skuid,name_spec,name,unmatched} / samples(前 20 条样例)`
   - **老接口 `auto-bind` 保持不动**（严格同名版），两个入口并存、各司其职
   - 事务保护（BEGIN/COMMIT/ROLLBACK + 失败回滚）

### 前端（`DishSalesBinding.vue`）
- 「对应本地 SKU」列三态：**已绑定**（蓝卡）/ **有推荐**（绿卡：推荐 SKU + 依据标签 + 成本 + 「采用推荐」/「另选」）/ **无推荐**（提示名称差异大，给「手工绑定」）
- 顶部按钮拆成两个：「**严格同名绑定**」（原逻辑，最保守）与「**⚡ 智能采用全部推荐（N）**」（N 为后端全量统计的可绑定条数，0 时禁用）
- 确认弹窗写清规则（忽略括号前后缀 / 编码互校 / 名称或规格唯一；**只做精确匹配不做模糊匹配**）
- `loadData` 保存 `recommend_summary` 供按钮显示条数

### 实测（本地全量）
- 待绑定分组 **1,345** 个 → **268 个可智能推荐**（`name_spec` 190 / `name` 77 / `skuid` 1），**1,077 个仍需手工**
- 典型生效：`打包盒`、`太公白米饭`、`烧鹅双拼饭`、`超爽柠檬茶`、`自选双拼饭`（名称+规格匹配）
- 仍无法识别的大项：`烧鹅饭` 9,710、`烧鸭饭` 5,083、`至尊鹅肶饭` 3,517（档案里叫「招牌烧鹅饭」等，**名称差异真实存在**）
  → 按项目既定方针（§9.3）**不做模糊自动绑**，保留人工处理；后续可选做「疑似候选提示」（只提示不自动绑）

### 验证
新增 `tools/poultry-verify/verify-dish-binding-recommend.ps1` —— **18 项全 PASS**：
- 推荐统计自洽（可推荐 + 无推荐 = 待绑定）、未绑定行带推荐、已绑定行**不**返回推荐
- 识别规则只读断言：**编码匹配必须编码+名称双一致**（名称互校防错绑）、名称类推荐与档案名归一化后一致
- 单行采用 → 变已绑定 → 解绑还原（用自建临时菜品「验证临时绑定菜-智能推荐」）
- **批量智能绑定快照回滚**：跑前记录全部绑定键 → 跑（实测新增 268 条）→ **只删本次新增** → 断言回到基线 54 条且无多删
- 收尾回收临时菜品；脚本自带**自愈**（上次中断残留会在下次启动时先清理）

> 安全：写操作只作用于自建临时菜品与「本次新增的绑定行」，用户的 54 条真实绑定全程未变。

**本次同样未部署云端。**

---

### 追加：疑似候选（2026-09-12 深夜，用户反馈「搜太公三宝饭还是老样子」后补做）
**现象**：用户在绑定页搜「太公三宝饭」，6 行全是「无自动推荐」→ 体感"功能没生效"。
实际是那些行的销售名（`【太公三宝饭】招牌经典烧鹅三拼`、`太公三宝饭 太公三宝饭`）与档案名
`太公烧鹅三宝饭` **归一化后仍不相等**，精确规则按设计不给推荐 —— 页面于是退化成手工绑定。

**做法：精确推荐之外再加一层「疑似候选」（只提示、绝不自动绑定）**
- 新增 `dishNameSimilarity(a, b)` —— 四项加权，对中文菜名稳定可解释：

  | 因子 | 权重 | 作用 |
  |---|---|---|
  | Jaccard（字符集交并比） | 0.45 | 基础相似度 |
  | containment（交集 ÷ 短名长度） | 0.25 | 销售名常带套餐描述，Jaccard 会被长名稀释 |
  | 最长公共连续子串占比 | 0.15 | **顺序信息**，区分「烧鹅饭 ⊂ 招牌烧鹅饭」与「烧肉拼烧鹅饭」 |
  | 长度比 | 0.15 | **防短名噪声**，否则「…含时蔬+海咸鸭蛋+例汤」会把「咸鸭蛋」当候选 |

  整体包含时额外 +0.1（最强信号）；阈值 0.6
- `suggestDishCandidates()`：候选名长度 < 销售名长度 × 0.4 时跳过；排除员工餐菜品；同分按名称短者优先
- `GET /api/dish-sales/mappings` 每行新增 `recommend_candidates[]`（最多 3 条，含 `menu_item_id/menu_name/sku_label/category/cost/score`）
  与 `recommend_candidate_label` —— **精确推荐与疑似候选互斥**（有精确推荐就不给候选）
- `POST /api/dish-sales/mappings/auto-bind-smart` 新增回报 `written_keys[]`（本次实际写入的绑定键，供精确回滚）
- `GET /api/dish-sales/mappings` 的 `ORDER BY` 补唯一键次级排序 → 原先同收入行顺序随机，**翻页会漏行/重复**（既有缺陷，已修）

**实测效果**
| 销售名 | 结果 |
|---|---|
| `太公三宝饭 太公三宝饭` | 疑似 → **太公烧鹅三宝饭(0.741)** > 【套餐】太公三宝饭（靓汤+柠檬茶）(0.613) |
| `【太公三宝饭】招牌经典烧鹅三拼` | 疑似 → **太公烧鹅三宝饭(0.658)** |
| `【太公三宝饭】含时蔬+海咸鸭蛋+例汤` | 无候选（`咸鸭蛋` 噪声已被长度比挡掉 ✅） |
| `烧鹅饭` | 疑似 → **招牌烧鹅饭(0.86)** > 烧肉拼烧鹅饭(0.845) > 烧鹅双拼饭(0.71) |
| `烧鸭饭` | 疑似 → **金牌烧鸭饭(0.86)** > 金牌烧鸭腿饭(0.65) |
| `【新鲜出炉】明炉烧腊双人餐(含时蔬/例汤)` | 疑似 → **【套餐】明炉烧腊双人餐(0.6)** ✅ 正是该绑的目标 |
| `太公白米饭` / `超爽柠檬茶` | 精确推荐自身（互不干扰） |

**前端**（`DishSalesBinding.vue`）：无精确推荐但有候选时显示**橙色「疑似」卡片**
（候选名 + 相似度 + 分类 + 成本 + 「采用」/「其他候选」下拉/「另选」）；
`openBindDialog` 打开时**自动预选**（已绑定 → 精确推荐 → 首个疑似候选），省去手敲搜索（下拉仍可改）。

**验证**：`verify-dish-binding-recommend.ps1` 扩到 **23 项全 PASS**，新增候选断言：
结构完整 / 不含员工餐 / 按相似度降序 / 与精确推荐互斥 / 相似度均在阈值之上。

### ⚠️ 事故与修复：验证脚本批量绑定曾残留 152 条（2026-09-12 深夜）
- **现象**：两次跑批量智能绑定后，`dish_sales_mappings` 从 80 条涨到 232 条，其中 **01:34 那批 152 条是脚本残留**
- **根因（两个）**：
  1. 首轮回滚用「总数差」推断新增键 —— 用户在页面上并发绑定（01:30–01:33 他自己绑了 16 条）污染差值，回滚范围错
  2. 拼接回滚键时用了接口返回的**原始 spec**，而接口写入用**归一化 spec**（`''` 与 `'--'` 等价）→ 键对不上 → 漏删
- **修法**：① 接口回报 `written_keys[]`，脚本据此**精确回滚**（不靠总数差）；② 脚本加 `NormSpec()` 与后端口径对齐；
  ③ 断言改为**并发容错**（只断言「本次写入的已删干净」+「基线行一条没丢」，不断言总数相等）
- **清理**：按 `created_at` 时间窗（01:34 同一秒写入 152 条，与用户操作时段可区分）精确删除，
  **删前已备份**到 `G:\zhongkong-backend\tools\mapping-residue-backup-2026-09-12_0134.json`；清理后总数回到 **80 条**（用户真实数据完好，其中 27 条是用户当晚自己用新功能绑的）
- **教训**：① 验证脚本**不要用总数差推断自己写了什么**，要让接口回报写入键；
  ② 键匹配必须与后端**同一归一化口径**；③ 跑写库验证前先确认用户是否正在页面上操作

### 追加：整只部位示意图 + 份量自检（2026-09-12，用户提议）

**用户三条反馈与处理**
| # | 反馈 | 处理 |
|---|---|---|
| 1 | 从缺口明细「去绑定」绑完后回来，缺口清单没刷新，已绑的菜还在 | ✅ **已修**（bug，见下） |
| 2 | 鹅胚重 6.6 斤（3.3kg）→ 烤制失水出汁 → 成品约 2kg，是否有参考价值 | ⏸ **暂缓**：有价值（出成率≈60.6% 可用于损耗监控 + 部位克重交叉校验），但**用户尚未拿到具体克重资料**，改了没法填会让测试失败 —— 等资料齐了再做 |
| 3 | 能否做鹅的 demo，在图上点选部位 | ✅ **已做第一步**（本次） |

**修复：缺口明细不实时刷新**
- 根因两处叠加：① `App.vue` 用了 `<KeepAlive :max="30">`，从绑定页返回核算页时组件不重新 mount，`onMounted` 的取数不再执行；②「查看缺口明细」按钮只做 `gapVisible = true`，不重新取数
- 修法：新增 `onActivated()`（每次页面激活即重取核算）＋「查看缺口明细」改走 `openGapDialog()`（开弹窗前先刷新）

**新增组件 `frontend/src/components/PoultryBodyMap.vue`（整只部位示意图）**
- **SVG 俯视示意**：头+颈 / 翅（左右） / 上庄 / 下庄 / 腩 / 腿（左右） / 底部横条「切片肉区」（鹅肉·小料·双拼·三宝饭 共用）＋ 整只/半只虚线外框
- **配色即语义**：身体类=蓝、副产品（翅/头颈）=橙、切片肉区=绿、**未配置=灰虚线**
- **点选联动**：点图上部位 → 高亮下方表格对应行（`:row-class-name`）＋ 顶部提示「已定位：鹅腿（一只出 8 份）」；未配置区域提示去「新增部位」
- **份量自检**（右侧面板，本次核心价值）：
  1. **上庄 2 块 + 下庄 2 块 = 一整只身体**（应恰好 100%，>100% 说明把「数块数」当成份量口径）
  2. **腿 2 条占下庄的比例**（正常 30~70%；≥80% 提示"检查是否该填一只出 8"；≤15% 提示系数填大了）
  3. 副产品标注「数块数口径，不参与份量守恒」
- 部位名归类用关键词（头/颈、翅/战斧、上庄、下庄、腩、腿/肶、整只/半只、肉/小料/双拼/三宝/片），**兼容用户自取的名字**
- 集成位置：基础档案 → **② 整只出成拆解** 页签顶部（表格上方）

**自检有效性实测**（无需浏览器，用真实配置复算）：
| 配置 | ① 上下庄合计 | ② 腿占下庄 |
|---|---|---|
| 当前（鹅腿 = 8） | 100% ✅ | 50% ✅ 正常 |
| 旧值（鹅腿 = 4） | 100% ✅ | **100% ⚠ → 提示"检查是否该填 8"** |
→ 若当初有此面板，填 4 时就能当场报警，不必等算完只数才发现那 +15% 偏差

**本次同样未部署云端。**

---

## 2026-09-12 用门店日报校准系数：鹅腿「一只出 4 → 8」（欧景店偏差 15.2% → 1.9%）

### 背景（用户提供的关键业务事实）
- **「上庄自带鹅翅，下庄自带鹅腿」** —— 翅/腿不是独立于上下庄的部位，而是它们的组成部分
- **「一份鹅腿饭用掉一整个鹅腿」**
- **「门店日报是今天共消耗了多少只鹅，不存在口径误差」** —— 80 只是可靠的校准基准

### 偏差现象
欧景店关联完鹅类菜品后（鹅类菜覆盖率 95.27%），9/1–9/10 算出 **92.13 只**，门店日报 **80 只**，偏差 **+15.2%**。

### 定位方法：单系数敏感性分析（以 80 只为基准）
只动「鹅腿」的「一只出几份」，其余配置不变：

| 鹅腿 | 总只数 | 偏差 |
|---|---|---|
| 4（原值） | 92.13 | +15.2% |
| 6 | 85.05 | +6.3% |
| **8** | **81.51** | **+1.9%** ✅ |
| 10 | 79.38 | −0.8% |
| 12 | 77.96 | −2.6% |

### 结论与依据
**鹅腿应为 8** —— 物理推导：

```
一只鹅 = 1 只份量
├─ 上半身 1/2 只 → 2 个上庄（每个 1/4 只，各含 1 只翅）
└─ 下半身 1/2 只 → 2 个下庄（每个 1/4 只，各含 1 条腿）
                  └─ 腿约占下庄一半份量 → 一条腿 = 1/4 × 1/2 = 1/8 只
⇒「一份腿饭 = 一整条腿」⇔「一份占 1/8 只」⇔ 系统里填 8
```
填 4 等于说"一份腿饭吃掉半个下庄"，把腿的份量算成了 **2 倍** —— 这就是那 15% 虚高的来源。
（历史脉络：用户最初填 2 属"数条数"口径 → 算 118.5 只；改 4 后 76.5 只，方向对但未到位；**8 才与物理一致**。）

### 改动（2026-09-12 经用户确认后执行）
- `poultry_yields`：`bird_id=18`（鹅·鹅）的「鹅腿」`parts_per_bird` 由 **4 → 8**，其余 11 个部位不变
- 改前快照备份：`G:\zhongkong-backend\tools\yields-backup-bird18-before-leg8.json`（可原样回退）
- 结果：欧景店 9/1–9/10 **92.13 → 81.51 只**（门店日报 80，偏差 **+1.9%**），瓶颈由「鹅腿」变为「**鹅肉**」

### 校准后的部位贡献（欧景店 9/1–9/10）
| 部位 | 一只出 | 需求 | 折鸟 |
|---|---|---|---|
| 鹅肉 | 11 | 205 份 | 18.64 只 ★瓶颈 |
| 下庄 | 4 | 63 份 | 15.75 只 |
| 上庄 | 4 | 46 份 | 11.50 只 |
| 鹅腿 | **8** | 85 份 | 10.63 只 |
| 半只 / 三宝饭 / 双拼 / 小料 / 整只 / 腩 | 2 / 29 / 23 / 17.5 / 1 / 4 | — | 7 / 5.79 / 4.61 / 4.34 / 2 / 1.25 只 |
| 鹅头带颈（副产品，不占鸟） | 1 | 32 份 | 32 只 |

### 遗留待办（已与用户说明）
1. **鹅肉「一只出 11」是新瓶颈**（18.64 只，占 23%）—— 需用户按实际单份给量复核
2. **剩余 40 份缺口**（`烧肉拼烧鹅饭` 24 份、`招牌烧鹅腿（单）` 16 份）补配后只数还会略涨（预计 ~83 只）
3. **禽类档案的「单只净重 / 单只成本」仍为 0** → 折重与理论成本显示 0，需补填
4. 副产品的「一只出」目前按**数块数**填（头颈 1、战斧 2）—— 在「取最大」逻辑下不影响总数 ✅，
   但风控对账会用 `只数 × 一只出` 推算"应有份数"（80 只鹅应有 80 个头颈 vs 实际卖出 32 份），属风控的预期行为
5. **切片肉四个部位的互斥关系待确认**：`鹅肉 11` / `小料 17.5` / `双拼 23` / `三宝饭 29` 各自都是"一只出 N 份"，
   若它们取自同一块切片肉（仅装盒不同），当前"相加"会偏高；若为不同等级/部位则相加正确
6. 数据边界：`dish_sales` 只到 **2026-09-10**，9/10 之后的销量尚未导入

**本次同样未部署云端。**

---

## 2026-09-12 模型升级：料篮模型 → 资源约束模型（四期）+ zone_code / yield_mode 落库

### 起因：外部评审指出的模型缺陷
1. **副产品「取最大」过于宽松** —— 一只鹅 2 只翅，战斧卖 120 份本需 60 只鹅，但当身体需求更大时，翅的约束被完全忽略
2. **一个字段混两套口径** —— `parts_per_bird` 同时承载"份量倒数"（上庄 4）与"物理件数"（头颈 1）
3. **整只/半只/切片肉的资源关系模糊**

### 用户确认的业务规则（建模依据）
- **「上庄自带鹅翅，下庄自带鹅腿」**
- **「一份鹅腿饭用掉一整个鹅腿」**
- **「卖 2 个下庄 + 2 份腿饭 = 消耗 4 条腿 = 2 只鹅」**；**「下庄可拆成鹅肉和鹅腿，但鹅腿不能反推成下庄」**
- **「一只自然是一整只鹅，半只就不包含鹅头带颈」**
- **「切片肉四部位同源」**
- 门店日报 = 当天实际消耗整鹅数，**无统计误差**（校准基准）

### 新模型：一只禽 = 若干固定容量的资源池，各算需求只数取 MAX
| 资源池 | 每只容量 | 谁消耗它 |
|---|---|---|
| 身体 | 1 只（份量） | 整只 1 / 半只 1/2 / 上庄 1/4 / 下庄 1/4 / 腿 1/8 / 腩 / 切片肉 1/N → **相加** |
| **腿** | **2 条** | 下庄(自带)、腿饭、半只、整只(2) |
| **翅** | **2 只** | 上庄(自带)、战斧、半只、整只(2) |
| **头颈** | **1 个** | 头颈菜品、整只（**半只不含**） |

```
N = max(身体份量合计, 腿需求÷2, 翅需求÷2, 头颈需求÷1)    ← 瓶颈即取到最大值的那一池
```
**用用户例子验证**：2 个下庄(各 0.25 只身体 + 1 条腿) + 2 份腿饭(各 0.125 只 + 1 条腿)
→ 身体 0.75、腿 4÷2 = **2 只** ✅ 与用户口径完全一致。

### 数据结构（`lib/db.js` 新增两列 + 自动迁移）
- **`zone_code`**：图示区域枚举（whole/half/upper/lower/leg/wing/head_neck/belly/meat/other）
  —— 「正则负责猜、枚举负责真相」：核算靠它判定消耗哪些限量资源，改名不影响；正则只在新增部位时做推荐
- **`yield_mode`**：口径类型（fraction 份量型 / count 件数型 / weight 重量型）
  —— fraction 与 count 计算式相同（需求 ÷ N），**引入它不改变任何结果**，只把语义显性化
- 迁移：按部位名预判写入（实测 12 个部位全部正确归类）

### 后端（`lib/poultry-accounting.js`）
- 新增 `ZONE_LIMITED_CONSUME`、`RESOURCE_CAPACITY`、`RESOURCE_LABELS`、`normalizeZoneCode()`、`normalizeYieldMode()`
- `calculate()`：`partTotals` 增加 `limited` 累加；`parts[]` 新增 `zone_code / is_byproduct / body_birds / limited`；
  `perBird` 改为资源池汇总 → `constraints{body,leg,wing,head_neck}` → `N = max(...)`
- 返回字段变更：**移除 `byproduct_birds`**；新增 `constraint_birds{body,leg,wing,head_neck}`、
  `resource_demand{leg,wing,head_neck}`、`governing_resource` / `governing_resource_label`（governing 现为"腿/翅/头颈/身体"）
- `saveYields()` 支持保存 `zone_code` / `yield_mode`

### 前端
- 只数卡说明改为：`瓶颈：腿（鹅腿）—— 身体 81.51 ｜ 腿 83 ｜ 翅 37 ｜ 头颈 34 只`
- 「按部位汇总」页脚改为资源池约束文字
- 「整只出成拆解」表格新增 **「图示区域」「口径类型」两列**（可手工修正）
- 示意图 `PoultryBodyMap.vue` 改用落库的 `zone_code`（缺列时才回退按名称猜）

### 核算不变量调整（先报告、经用户确认后才改）
| 原 | 现 |
|---|---|
| 只数 = max(身体相加, 副产品最大) | **只数 = max(身体, 腿, 翅, 头颈 四个资源池)** |
| （无） | **新增：瓶颈资源 = 约束最大的那个资源池** |
| 旧口径 ≥ 新口径 | 保留（实测仍成立：6832 ≤ 11785.45）✅ |
| 其余 4 条 | 不变 ✅ |

### 实测结果变化（欧景花园店 2026-09-01~09-10，门店日报 80 只）
| 模型 | 只数 | 瓶颈 | 偏差 |
|---|---|---|---|
| 旧（料篮，鹅腿=8） | 81.51 | 身体·鹅肉 | +1.9% |
| **新（资源约束）** | **83.00** | **腿·鹅腿** | **+3.8%** |

四个资源池约束：身体 81.51 ｜ **腿 83** ｜ 翅 37 ｜ 头颈 34 —— 腿需求 166 件 = 下庄 63 + 腿饭 85 + 半只 14 + 整只 4 ✅
> 偏差略增但**模型更正确**（符合用户"先把资源关系建对，再用 80 只校准"的原则；旧值 8 本就是为对齐 80 拟合出来的）

### 验证
全部 **168 项 PASS**：`verify-poultry.ps1` **50/50**（新增"四个资源池"+"瓶颈资源"两项断言）、
`verify-poultry-batch.ps1` 29/29、`verify-poultry-scope.ps1` 41/41、`verify-yield-append.ps1` 13/13、
`verify-dish-binding-recommend.ps1` 23/23、`verify-risk-control.js` 12/12（欧景店 83 只 vs 日报 80）、
`crosscheck-poultry.js` 独立复算一致

### 剩余待办（用户已授权，按序推进）
1. **一期**：自检扩三档（❌错误 / ⚠️风险 / ℹ️提示）+ 父子资源重复校验（同一菜品同时配"下庄"与"腿"）+ **MAPE 采购校准面板**（模型理论 vs 门店日报滚动偏差）
2. **三期**：切片肉克重池（`鹅肉/小料/双拼/三宝饭` → 统一到「可切片鹅肉池」用克重 SUM 后除单鹅出成）——**等用户提供克重资料**
3. 部位示意图的"点图新建部位"体验（第二步）

**本次同样未部署云端。**

---

## 2026-09-12 一期：自检三档 + 父子资源重复校验 + MAPE 采购校准

### ① 部位示意图自检扩为三档（`PoultryBodyMap.vue`）
| 档位 | 触发条件 | 示例 |
|---|---|---|
| ❌ **错误**（物理不可能） | 出成数量为 0 / 空；份量型下「一只出份数 < 1」（单份超过一整只） | 「鹅腿」出成数量为 0 或空 |
| ⚠️ **风险**（可能填错） | 上下庄合计偏离 100%；腿占下庄 ≥80% 或 ≤15% | 腿占下庄 100%（该填 8 时） |
| ℹ️ **提示**（✅ 也归此档） | 上下庄合计正确、腿比例正常、切片肉区多部位、副产品口径、未配置部位 | 切片肉区有 4 个部位 → 提示三期会统一按克重折算 |

### ② 父子资源重复校验（`findUsageConflicts`）
**依据**：腿含在下庄里、翅含在上庄里、整只含全部 —— 同一菜品同时配置「父」与「子」部位即重复计量。
- `ZONE_CHILDREN`：`whole → {half,upper,lower,leg,wing,head_neck,belly,meat}`、`half → {upper,lower,leg,wing,meat}`（**半只不含头颈**）、`upper → {wing}`、`lower → {leg}`
- **只报警不拦截**（确实存在"额外加料"的合法场景）
- 三个入口都覆盖：`saveUsageForMenu` / `batchSaveUsage` 返回 `warnings`（前端弹窗提示）；`calculate()` 写入 `coverage.warnings`（覆盖历史数据，同一菜品只检测一次）
- **实测**：自建临时菜品同时配「下庄 + 鹅腿」→ 正确报警
  `⚠️ 同时配置了「下庄（下庄）」与「鹅腿（腿）」—— 腿通常已包含在下庄之内，可能重复计量；若是额外加料可忽略`（临时菜品已回收）

### ③ MAPE 采购校准（`calibrationReport` + `GET /api/poultry/calibration`）
**意义**：评价参数好坏不靠「腿占下庄 15%~80%」这类经验阈值，而看**历史数据拟合** —— 改完系数后滚动 MAPE 若从 7.8% 降到 2.6%，才算真的改善。
- 数据源：`poultry_purchases`（门店实际消耗/采购只数，手动录入，UNIQUE(store_id, month, bird_id)）
- 逐条对比：模型理论只数（按月跑一次核算）vs 实际只数 → 偏差、`MAPE`、平均偏差（带正负）、**中位绝对偏差**（抗异常）
- 分层评价：`< 5%` 拟合良好 / `< 10%` 尚可 / `≥ 10%` 建议校准系数
- **过滤孤儿记录**：所属禽类已被删除的历史遗留采购行（`animal` 为空）不参与校准，只回报 `skipped_invalid` 计数与说明
- 界面：核算页新增「采购校准 · 模型 vs 门店实际」面板（点「开始校准」触发，避免每次进页面都跑核算；默认最多算 12 个「门店 × 月」组合并标注 truncated）

### 验证
**全部 168 项 PASS**：`verify-poultry.ps1` **50/50**、`verify-poultry-batch.ps1` 29/29、`verify-poultry-scope.ps1` 41/41、
`verify-yield-append.ps1` 13/13、`verify-dish-binding-recommend.ps1` 23/23、`verify-risk-control.js` 12/12、
`crosscheck-poultry.js` 独立复算一致
> 过程中出现的一次 `已配置菜品 24→23` FAIL 已定位：是**测试自建临时菜品造成的基线时序问题**（非脚本破坏数据），
> 清理后 50/50 全绿

### 剩余（下次）
- **三期**：切片肉克重池（等用户提供克重资料）
- 示意图「点图新建部位」体验（第二步）
- 云端上线（需用户逐次明确同意）

**本次同样未部署云端。**

---

## 2026-09-12 扩展鸡/鸭：建档、字段简化、混搭菜口径

### 用户口径（关键业务事实）
- 鸡/鸭的部位**比鹅简单**，只有 6 个：**一只 / 半只 / 单份 / 双拼量 / 三拼量 / 腿**
- **没有**：鸡鸭腩、鸡鸭头颈、战斧（翅）
- **鸡翅是单独采购的**（不拆整鸡）→ 不建 wing 部位
- **一份腿饭 = 一整条腿**
- 部分双拼饭/三宝饭是**鸡鸭鹅混搭**，需要 **1+1** 或 **1+1+1** 多行挂载

### 建档结果（经用户同意后执行）
| 禽类 | id | 净重/成本 | 6 个部位（一只出几份） |
|---|---|---|---|
| **鸡·鸡** | 78 | 1.6kg / ¥35 | 一只 1 ｜ 半只 2 ｜ 单份 6 ｜ 双拼量 12 ｜ 三拼量 18 ｜ 鸡腿 5 |
| **鸭·鸭** | 79 | 2.2kg / ¥45 | 一只 1 ｜ 半只 2 ｜ 单份 6 ｜ 双拼量 12 ｜ 三拼量 18 ｜ 鸭腿 5 |
| 鹅·鹅 | 18 | 3.3kg / ¥128 | 12 个部位（**未改动**） |
- 备份：`G:\zhongkong-backend\tools\poultry-birds-backup-before-chicken-duck.json`（3 个禽类 + 24 条出成，可回滚）
- **单份/双拼量/三拼量/腿 的份数是初始占位值**，待用「采购校准」按门店日报反推

### 代码改动：区域识别补充鸡鸭字段名
`单份 / 三拼量` 原会落到 `other` → 已在三处补进 `meat` 规则（`lib/db.js` 迁移 + `normalizeZoneCode` + 前端 `PoultryBodyMap`）：
```
/肉|小料|双拼|三宝|片|单份|三拼|拼量/ → meat
```
实测（建临时"鸡"档案后立即清理）：`一只→whole`、`半只→half`、`单份/双拼量/三拼量→meat`、`鸡腿→leg` 全部正确 ✅

### 模型自适配说明（无需改代码）
鸡/鸭没有头颈与翅部位 → 那两个资源池自动退化为"永不成为瓶颈"：
- 翅需求只来自「一只」(2) 与「半只」(1)，**恒 ≤ 腿需求** → 翅约束不可能超过腿约束
- 头颈需求只来自「一只」(1) → 同理
所以**不会因为"少配头颈/翅"而算不准** ✅ 腿与身体资源正常参与 MAX。

### 口径澄清（用户问"一份一个腿 → 一只出几份填几"）
**「一份 = 一条腿」与「一只出几份」是两件事**：
- `「一只出几份」= 1 ÷ (一份占整只的比例)`，是**份量口径**
- 填 2 等于说"一份腿饭吃掉半只鸡的身体"，会让身体约束严重虚高（当年鹅就踩过这个坑）
- 鸡腿约占整只 **1/5** → 初值填 **5**（鹅腿占 1/8 → 填 8，同一逻辑）

### 混搭菜（1+1 / 1+1+1）的挂法
系统天然支持：`poultry_dish_usage` 唯一键是 `(menu_item_id, yield_id)`，**一个菜可挂多行**，在「菜品耗用」编辑弹窗里逐行加。
- 判据：**菜名里出现的禽类字 = 要挂的行数**；`叉烧 / 烧肉 / 叉` 属猪肉，不挂行
- 实例：`鸡鸭双拼饭` → 鸡·双拼量 + 鸭·双拼量；`烧鹅拼豉油鸡饭` → 鹅·鹅肉（双拼）+ 鸡·双拼量
- 已生成对照表（**只生成未写入**）：
  - `tools\禽类菜品耗用建议v2.tsv`（108 个禽类菜：当前配置 / 建议配置 / 需要补齐）
  - `tools\鸡鸭菜品耗用建议.tsv`（早期版本）
- 现状：含「拼」类菜 **113 个 / 约 6 万份**，分三类
  - **菜名已明确组合**（约 8 个）：`烧鹅拼豉油鸡饭`、`烧鹅拼咸鸡饭`、`太公三宝饭(烧鹅+叉烧+咸鸡)`、`鸡鸭双拼饭`、`叉鸭双拼饭`…
  - **只说双拼/三宝未言明组合**（约 20 个）：`烧鹅双拼饭 7,165`、`太公烧鹅三宝饭 3,700`、`烧鹅拼叉烧饭 1,464`…（需用户确认默认组合）
  - **顾客自选类**（约 20 个 / 约 1 万份）：`自选双拼饭 5,009`、`自选三拼饭 935`、`经典烧味双拼饭(5选2) 541`…
    → 三个可选口径：① 按最热门组合固定 ② 按选品概率分摊 ③ 整类排除（先保证覆盖率真实）

### 用户决定：菜品关联由用户自行在页面完成
AI 只提供对照表与建议，**不代执行批量关联**（用户明确表示自己配）。

### 配完后的两个提醒
1. **外购翅要排除**：`蜜汁鸡翅 5,918`、`蜜汁鸡翅饭 1,732`、`蜜汁鸡翅拼盘 125` 用的是外购翅 → 到「维护禽类菜范围」标为**不算**，否则永远算"未配置"、拉低禽类菜覆盖率
2. **自选类要先给交代**（排除或按热门组合固定），否则约 1 万份长期挂在缺口里

**本次同样未部署云端。**
