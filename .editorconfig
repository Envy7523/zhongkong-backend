# EditorConfig — 统一项目编码规范
# 确保所有编辑器（VS Code / WebStorm / Notepad++ 等）使用 UTF-8
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
indent_style = space
indent_size = 2
trim_trailing_whitespace = true

[*.md]
trim_trailing_whitespace = false

[*.bat]
end_of_line = crlf
